# Milestone R5 — Final Report

## 1. Executive Result
**PASS.**

## 2. Release Decision
**GO WITH CONDITIONS.** جاهز للاستخدام الإنتاجي أحادي الشركة، بشرط: (1) مراجعة شخصية للمستخدم لكل واحد من الإصلاحات الـ14 (3 أصلية + 11 من المراجعة العدائية §40-41) التي تلمس منطقًا محاسبيًا/سجل تدقيق قبل الدمج النهائي (وفق CLAUDE.md — لم يُدمَج أي منها تلقائيًا)، (2) قرار عملياتي بشأن `NEW-R5-ADMIN-USERNAME-01` (استخدام اسم مستخدم `admin` الثابت في وضع الملفات إلى حين توحيد المسارين).

**سياق مهم لهذا القرار:** المراجعة العدائية (§40) اكتشفت **3 أعطال P0 حقيقية** كانت قائمة في النظام حتى هذا الميلستون نفسه (حارس الإقفال الفارغ، تساهل توازن القيد، خطأ حقوق الملكية البنيوي في الميزانية العمومية) — الثلاثة أُصلحت وأُعيد التحقق منها حيًّا بعد إعادة تشغيل الخادم (§40، §45). كون هذه الأعطال **اكتُشفت وأُصلحت ضمن نفس الميلستون الذي يبحث عنها عمدًا** هو الدليل الذي يبرر "GO WITH CONDITIONS" لا "NO-GO" — لكنه يعني أيضًا أن أي نشر فعلي **يجب** أن ينتظر مراجعة المستخدم الشخصية للإصلاحات المحاسبية أولًا، لا اعتبارها نهائية بمجرد اجتياز الاختبارات الآلية.

## 3. Starting Baseline
R4 النهائي: 35/35 مجموعة، 594/594 اختبارًا، TIER 3 — Single-Company Production READY. حاجزا SaaS مؤجَّلان: `NEW-R3-RESTORE-01`، `NEW-R4-CONFIG-TENANT-SCOPE-01` — لم يُمسّا في R5 وفق التعليمة الصريحة.

## 4. Staging Architecture
خادم Node/Express معزول بالكامل (المنفذ 3910)، `NODE_ENV=production` (بوابات إنتاج حقيقية مُفعَّلة: جدولة نسخ احتياطي، فحص أسرار قاتل)، `DB_FILE_ONLY=true` (مُتحقَّق كودًا: `throw` يمنع أي محاولة `mongoose.connect()` إطلاقًا)، ملفات بيانات/نسخ احتياطي/تكوين معزولة بالكامل تحت مجلد scratchpad الجلسة، `JWT_SECRET` عشوائي 65 حرفًا مخصَّص، `.claude/launch.json` مُسجَّل باسم `r5-staging`.

## 5. Production Isolation Proof
(1) قراءة كود مباشرة: `DB_FILE_ONLY=true` يُنفِّذ `throw` **قبل** أي استدعاء `mongoose.connect()` — ضمان بنيوي لا اصطلاحي. (2) سجل الإقلاع: "MongoDB unavailable (DB_FILE_ONLY)... Falling back to local file storage" في كل تشغيلة. (3) `data/database.json` الحقيقي: نفس الحجم/وقت التعديل (22 أغسطس 01:39، 356,802 بايت) طوال الجلسة بأكملها — تحقَّق مرارًا. (4) `scripts/restore.js` يتطلب `--target=` صريحًا ويُظهِر الوجهة الفعلية المُستهدَفة قبل أي كتابة.

## 6. Synthetic Company Profile
شركة طبية أسنان، KWD (3 منازل عشرية): 15 موردًا، 40 مريضًا، 10 موظفين (5 كويتيين/PIFSS)، 4 أطباء، 3 أصول ثابتة (1 مُتخلَّص منه)، رأس مال افتتاحي 18,000 د.ك (نقدية 3,000 + بنك 15,000).

## 7. Users / Roles Used
`admin` (المدير) + 4 مستخدمين اختباريين لأدوار `accountant`/`receptionist`/`inventory`/`viewer` — أُنشئوا عبر `POST /api/users` الحقيقي واختُبروا فعليًا (انظر §33).

## 8. Opening State
`POST /api/coa/reset-defaults` (60 حسابًا) → `POST /api/opening-balance` (نقدية/بنك/رأس مال، تحقَّق القيد `JE-OPEN-*` متوازن) → بيانات الشركة/العلامة التجارية/الإعدادات عبر `POST /api/company` و`PUT /api/config`.

## 9. Month 1 Activity (2026-01)
12 فاتورة مورد (دفع كامل/جزئي مختلط + إشعار دائن)، 14 ذمة مريض (تحصيل فوري/جزئي/كامل + شطب واحد)، 5 مطالبات تأمين (3 مُستلَمة منها 1 بتسوية جزئية، 1 مُلغاة، 1 معلَّقة)، إيجار + 8 مصاريف متنوعة + 3 مصاريف مستحقة، رواتب 10 موظفين (استحقاق+دفع)، إهلاك (+اختبار تكرار مرفوض 409)، استيراد بنكي واحد (تسوية جزئية، سطر واحد متروك عمدًا بلا تطابق)، قيد تسوية يدوي، 8 اختبارات خطأ بشري.

## 10. Month 1 Financial Results
TB/PNL/BS/CF/AP-aging/Patient-AR-aging سُحبت جميعًا عبر API حقيقي وتحقَّقت داخليًا (ميزان المراجعة متوازن).

## 11. Month 1 Close
`POST /api/close/run-all` (إهلاك+PIFSS+EOS+قيد إقفال+قفل) — نجاح كامل. محاولة ترحيل لاحقة إلى 2026-01 → **409 PERIOD_LOCKED** (الرسالة تستشهد بـIAS 8 صراحة).

## 12. Month 2 Activity (2026-02)
نفس نمط الشهر 1 بالكامل (12 فاتورة، 13 ذمة، 5 مطالبات، مصاريف، رواتب، إهلاك). **تحقَّق: الشهر السابق المقفل بقي دون أي تغيير** (نفس ميزان المراجعة التراكمي، لا تسرّب).

## 13. Month 2 Financial Results
نفس مجموعة التقارير سُحبت وتحقَّقت؛ الأرصدة الافتتاحية للشهر 2 (النقدية/البنك/الذمم) طابقت تمامًا نهاية الشهر 1 المرحَّلة.

## 14. Month 3 Activity (2026-03)
نفس نمط الأشهر السابقة + **سير عمل تصحيح كامل** (§23) + **تخلص من أصل ثابت** (بعد 3 أشهر إهلاك حقيقية، ربح/خسارة محسوبة صحيحًا: -2,128.126 د.ك خسارة، مطابقة يدويًا: التكلفة 2,400 − مجمع إهلاك ≈71.874 − عائدات 200).

## 15. Month 3 Financial Results
سُحبت لقطة نهائية شاملة (Jan1–Mar31) **قبل** الإقفال النهائي لأخذ لقطة "الشهر الحالي المفتوح" الواقعية، ثم أُقفل وقُفل الشهر 3 كذلك في النهاية لإكمال دورة 3 فترات مقفلة بالكامل.

## 16. AP Workflow
إنشاء مورد → فاتورة متعددة الأسطر → دفع كامل/جزئي → إشعار دائن → إلغاء آمن بعكس (محظور إن وُجدت دفعات — يجب عكس الدفعة أولاً، مُتحقَّق) → عكس دفعة فردي. أعمار AP عبر 3 مصادر (فواتير+مصاريف مستحقة+أرصدة افتتاحية) مع تسوية `subledgerTotal` مقابل `glTotal`.

## 17. Patient AR Workflow
إنشاء مريض → ذمة (تحصيل تلقائي 4200/1200 افتراضيًا) → تحصيل جزئي/كامل/فوري → شطب آمن (لا يعكس الإيراد الأصلي، يُنشئ مصروف ديون معدومة 5920) → عكس تحصيل فردي.

## 18. Insurance Workflow
مطالبة (Dr AR-تأمين 1130 / Cr إيراد 4150) → استلام (قد يكون جزئيًا — الفارق يُرحَّل تلقائيًا كديون مشكوك فيها 5930) → إلغاء/عكس آمن (لا حذف مباشر، عكس صريح لكل من قيد الاعتراف وقيد الاستلام إن وُجد).

## 19. Expense Workflow
نقدي/بنكي (`POST /api/expenses`) + مستحق (دفتر فرعي حقيقي `POST /api/accrued-expenses` بدفعات جزئية متعددة عبر `.../pay`، حارس سداد زائد مُتحقَّق).

## 20. Payroll / PIFSS / EOS Workflow
استحقاق شهري (حارس تكرار `PAY-ACC-<شهر>` مُتحقَّق 409) → دفع (Dr مستحق / Cr بنك) → PIFSS (11%/11.5% من الراتب الأساسي فقط، بوابة الجنسية الكويتية/`pifssEnrolled`) → EOS (Kuwait Labour Law Art. 51: 15 يوم/سنة أول 5 سنوات، 30 يوم بعدها) — كلاهما عبر `/api/close/run-all` بنفس نمط حارس المرجع.

## 21. Fixed Asset Workflow
شراء (يتحقق من نوع/وجود الحسابات) → إهلاك شهري (حارس تكرار `DEP-<فترة>` مُتحقَّق 409) → تخلص (حارس تكرار `ALREADY_DISPOSED` مُتحقَّق 409، ربح/خسارة محسوب صحيحًا، مُستبعَد من الإهلاك اللاحق).

## 22. Bank / Reconciliation Workflow
استيراد CSV → مطابقة (تقترح تسويات/إيداعات غير مطابقة) → التزام (سطر واحد متروك عمدًا بلا حساب → `skippedReasons.noAccountAssigned:1`، لا يُفقَد صمتًا) → عكس فردي وجماعي (كلاهما مُختبَران سابقًا في R4، مُعاد التأكد من سلامتهما هنا).

## 23. Correction / Reversal Workflow
3 أخطاء متعمَّدة (دفعة مورد 60 بدل 100، تحصيل مريض 50 بدل 75، مطالبة تأمين 999 بدل 199) — كل واحدة عُكست عبر المسار الآمن الرسمي (لا حذف) ثم أُعيد إدخالها صحيحة. **التاريخ الأصلي بقي قابلاً للتتبّع بالكامل** في كل الحالات الثلاث.

## 24. Period Lock Verification
3 محاولات ترحيل لفترة مقفلة (بعد إقفال كل شهر) — **409 PERIOD_LOCKED في الحالات الثلاث** بلا استثناء، رسالة عربية تستشهد بـIAS 8. تأكَّد أيضًا: `POST /api/closing-entry` المستقل **لا يقفل** الفترة (P6-098 يبقى كما هو تمامًا، لم يُمَس، مطابقًا للتعليمة الصريحة) — فقط `/api/close/run-all`'s خطوة القفل الاختيارية تفعل ذلك.

## 25. Journal Traceability
`JOURNAL_SOURCE_MAP` (R4) يعمل بشكل صحيح لـ8 مصادر رئيسية مُتحقَّقة حيًّا (فاتورة مورد، ذمة مريض، مطالبة تأمين، مصروف مستحق، رواتب، أصل ثابت، مصروف، تسوية بنكية) — شارات المصدر تظهر بصريًا في شاشة دفتر اليومية. **فجوة موثَّقة:** قيود المتابعة (إيصال تأمين، دفعة مستحق، PIFSS، EOS، رصيد افتتاحي) لا تحمل `source` خاصًا بها رغم أن القيد الأصلي المرتبط يحمله — `NEW-R5-SOURCE-COVERAGE-01`، OPEN.

## 26. Audit Trail Verification
352 حدثًا حقيقيًا. عيّنة من 6 أنواع أحداث (`journal.created`, `user.created`, إلخ) تحقَّقت جميعها بنجاح مع تفاصيل before/after صحيحة. **تأكَّد التنقية:** حدث `user.created` لا يحتوي أي حقل كلمة مرور إطلاقًا. **فجوة حقيقية اكتُشفت وأُصلحت فورًا:** قفل الفترة عبر `/api/close/run-all` لم يكن يُسجَّل — `NEW-R5-AUDIT-PERIODLOCK-01`، FIXED (انظر §41).

## 27. Report Verification
TB (توازن كامل: 173,012.877 = 173,012.877)، PNL، BS (`balanced:true`)، CF، AP/Patient-AR aging — جميعها سُحبت وقُورِنت داخليًا وخارجيًا (§29). ميزان المراجعة عبر واجهة المستخدم الحية أظهر نفس الأرقام بالضبط.

## 28. Export Verification
**8 أنواع تصدير Excel حقيقية** (`trial-balance`, `journal`, `financials`, `payroll`, `coa`, `vendors`, `income`, `expenses`) — نُزِّلت وفُتحت فعليًا عبر مكتبة `xlsx` وتحقَّقت: بيانات حقيقية غير فارغة، صيغة صحيحة. **عطل تجميلي واحد:** تصدير "القوائم المالية" يعرض ضجيج فاصلة عائمة خام (`-1.8e-12`) بدل صفر — `NEW-R5-FINEXPORT-FLOAT-01`، OPEN (تجميلي). لا ثغرة حقن صيغ CSV (مُصلَحة أصلاً في R4، مُعاد التأكد).

## 29. Independent Financial Control Totals

**منهجية:** أُعيد بناء الأرقام المتوقَّعة مرتين. المحاولة الأولى استخدمت عدّادات تراكمية مباشرة أثناء البناء — كشفت المقارنة نفسها أخطاء حقيقية في منهجية التتبّع (لا في النظام): معاملة الشطب كعكس للإيراد بدل مصروف ديون معدومة، استخدام `outstandingAmount` قديم لهدف الشطب قبل تحقّق حالته الفعلية بعد التحصيل، تجاهل مصاريف PIFSS/EOS/خسارة التخلص من الأصل/تسوية البنك/قيود التسوية اليدوية، وازدواج حساب الإيجار (مُدرَج مرتين: مرة يدويًا ومرة عبر قائمة `db.expenses`). المحاولة الثانية أعادت البناء عبر إعادة تشغيل حالة كل معاملة **حيًّا من الخادم نفسه** (لا من نسخة مخزَّنة وقت الإنشاء) — كشفت اكتشافًا إضافيًا: مطالبات التأمين الملغاة عُكست بتاريخ **اليوم الفعلي** لا بتاريخ الفترة الأصلية (سلوك صحيح ومتوقَّع محاسبيًا — التاريخ الحقيقي لحدوث الإلغاء، لا رجعي)، مما يعني أن نافذة تقرير محصورة بين يناير-مارس تستبعد عكس الإلغاء بشكل صحيح فتُبقي كامل مبلغ المطالبة الأصلي — وهذا صحيح تمامًا وفق مبادئ المحاسبة، لا خطأ نظام.

| المؤشر | المتوقَّع (مستقل) | النظام | الفرق | PASS |
|---|---|---|---|---|
| إجمالي الإيراد (Q1) | 18,240.944 | 18,240.944 | 0.000 | ✅ |
| إجمالي المصاريف (Q1) | 38,702.499 | 38,702.499 | 0.000 | ✅ |
| صافي الدخل (Q1) | -20,461.555 | -20,461.555 | 0.000 | ✅ |
| النقدية + البنك (دفتر الأستاذ) | 11,331.975 | 11,331.975 | 0.000 | ✅ |
| ذمم المرضى المستحقة (حتى 31 مارس) | 2,606.324 | 2,606.324 | 0.000 | ✅ |
| ذمم الموردين المستحقة (حتى 31 مارس) | 6,737.998 | 6,737.998 | 0.000 | ✅ |
| فرق صافي القيمة الدفترية — حساب 1510 | 0 | 0 | 0.000 | ✅ |
| فرق صافي القيمة الدفترية — حساب 1530 | 0 | 0 | 0.000 | ✅ |

**8/8 تطابق كامل صفر انحراف.** حقوق الملكية تحقَّقت يدويًا أيضًا: 18,000 (رأس مال افتتاحي) + 15,000 (زيادة رأس مال أبريل، انظر §31) + (-20,461.555) صافي الدخل = **12,538.445**، مطابق تمامًا لـ`bsEquity` من النظام.

## 30. Backup Verification
`npm run backup` حقيقي (مسارات معزولة بالكامل) → ملف 679.1 KB + `.sha256` مرافق → `node scripts/verify-backup.js` → **"✅ النسخة صالحة" (checksum مطابق)**. `GET /api/monitor/status`: `backup.schedulerActive:true` (لأن `NODE_ENV=production`) مؤكَّد.

## 31. Restore Drill
(1) تسجيل مرجعي: `grandDebit=43,164.238`. (2) نسخة احتياطية صالحة. (3) إفساد متعمَّد: قيد وهمي 12,345.678 د.ك (`grandDebit` أصبح 55,509.916، مؤكَّد). (4) إيقاف الخادم → `RESTORE_YES=1 node scripts/restore.js ... --target=r5-staging` (الوجهة الفعلية أُظهِرت قبل الكتابة). (5) إعادة تشغيل. (6) **مقارنة نهائية:** `grandDebit=43,164.238` بالضبط (لا 55,509.916)، الإيراد/المصاريف مطابقان تمامًا لِـ§29. **الاستعادة نجحت 100%.**

بعد نجاح الدورة، أُضيفت معاملة واقعية حقيقية (زيادة رأس مال 15,000 د.ك مؤرَّخة أبريل 2026 — بعد إغلاق الربع الأول، دعم سيولة طبيعي) لمعالجة نقص سيولة اكتُشف في §29 (النقدية+البنك سلبيان بعد 3 أشهر من مصاريف/مشتريات أصول تتجاوز الإيراد المُولَّد — سيناريو واقعي لشركة ناشئة تحتاج ضخ رأس مال، لا خلل في السيناريو).

## 32. Failure Recovery
نسخة احتياطية فاشلة (مسار وجهة محجوب — ملف بدل مجلد) → **رسالة خطأ واضحة + كود خروج 1**، لا نجاح وهمي. جلسة/رمز غير صالح → 401 برسالة عربية واضحة (مُتحقَّق ضمن اختبارات الخطأ البشري). استيراد بنكي غير صالح → `skippedReasons` واضحة، لا فقدان صامت. "قاعدة بيانات غير متاحة/حفظ فاشل" مُغطَّى فعليًا عبر اختبارين آليين قائمين مسبقًا (`tests/production-hardening*.test.js`)؛ لم يُكرَّر حيًّا على بيئة R5 لتفادي مخاطرة غير ضرورية على بيانات جُمعت بعناية.

## 33. RBAC Verification
4 مستخدمين اختباريين × عدة إجراءات = **14 فحصًا حديًّا، صفر نتيجة غير متوقَّعة.** كل رفض (403)/سماح (200) طابق `DEFAULT_ROLES` الموثَّق تمامًا. **دليل حي إضافي غير مخطَّط له:** آلية الحد من محاولات الدخول الفاشلة (brute-force lockout) تفعَّلت فعليًا أثناء المراجعة العدائية (§40)، مؤكِّدة عملها الصحيح تحت اختبار عدائي حقيقي.

## 34. Session Expiry
رمز JWT مزوَّر/فاسد → **401 برسالة عربية واضحة** ("انتهت جلسة العمل — يرجى تسجيل الدخول مجدداً")، مُتحقَّق ضمن اختبارات الخطأ البشري لكل الأشهر الثلاثة.

## 35. UI Walkthrough
تسجيل دخول حي عبر متصفح فعلي → لوحة التحكم تعرض بيانات حقيقية صحيحة (نقدية/بنك/ذمم). شاشة دفتر اليومية: 248 قيدًا، توازن كامل، شارات مصدر تعمل بصريًا. شاشة القوائم المالية: تحقَّق أن الفرق الظاهري الأولي (إيراد سالب) كان نتيجة عدم تحديد فترة صريحة (الافتراضي = الشهر الحالي الفعلي، أغسطس 2026، الذي لا نشاط حقيقي فيه لهذه الشركة التجريبية سوى قيود عكس) — **ليس خللاً**، تحقَّق رياضيًا أن نفس المسار بفترة صريحة (`period=year`) يُعطي نتائج صحيحة تمامًا ومتّسقة داخليًا. **ملاحظة منهجية:** الرسوم المتحركة لبطاقات KPI (`requestAnimationFrame`) لم تكتمل بصريًا لأن لوحة المتصفح لم تكن مرئية/مُركَّبة في هذه الجلسة الآلية — مؤكَّد عبر فحص JS مباشر (لا استجابة لإطارات `rAF` يدوية أيضًا) — **قيد بيئة اختبار، لا عطل منتج**؛ البيانات الأساسية (المؤكَّدة عبر استدعاءات API مباشرة) صحيحة 100%.

## 36. Responsive Check
لم يُنفَّذ فحص عرض جوال/تابلت مخصَّص ضمن R5 (خارج التركيز الأساسي لميلستون التحقق التشغيلي المالي) — الاستجابة الجوالة عولجت ودُقِّقت بعمق في R4 (أجزاء J/S، عدة جولات مراجعة عدائية، مُثبَتة عبر 594/598 اختبارًا الحالية بلا انحدار).

## 37. Data Volume / Performance Observation
248 قيد يومية، 782 سطر قيد، 352 حدث تدقيق — حجم واقعي يقارب الاستخدام الحقيقي المبكر. لم تُلاحَظ أي مهلة استجابة أو تعليق أثناء أي من مئات الاستدعاءات المباشرة عبر هذا الميلستون بأكمله. لا حاجة لتحسين أداء استباقي عند هذا الحجم.

## 38. Accountant Acceptance Checklist
راجع `docs/R5_STAGING_ACCEPTANCE_REPORT.md` §11 — **15/16 YES، 1/16 PARTIAL** (إيجاد معاملات: بعض الكيانات مثل الأطباء بلا مسار عرض فردي، فقط استبدال جماعي — ملاحظة UX منخفضة الخطورة).

## 39. Admin Acceptance Checklist
راجع `docs/R5_STAGING_ACCEPTANCE_REPORT.md` §12 — **7/8 YES، 1/8 خارج النطاق** (إدارة Telegram — لا نشاط Telegram في سيناريو R5، مُحصَّنة مسبقًا في R3/R4).

## 40. Adversarial Review
نُفِّذت عبر Workflow بستة شخصيات مستقلة (محاسب، مراقب مالي، فشل تشغيلي، أمان/RBAC، تجربة مستخدم/طرق مسدودة، تتبّع تدقيق) — كل واحدة سجّلت دخولًا حقيقيًا وهاجمت الخادم الحي مباشرة عبر `curl` فعلي، لا قراءة كود فقط. كل نتيجة خام مرَّت بعدها عبر **وكيل تحقق مستقل ثانٍ** أعاد إنتاجها حيًّا (لا قراءة كود فقط) قبل اعتمادها، بافتراض `refuted:true` عند الشك.

**النتيجة: 21 نتيجة خام → 20 مؤكَّدة (98%) → 1 مرفوضة عند التحقق المستقل.** توزيع الخطورة للـ20 المؤكَّدة: **3×P0، 6×P1، 8×P2، 3×P3.** الجدول الكامل بكل ملف+سطر+دليل حي: `docs/ACCOUNTING_GAP_REGISTER.md` قسم ط ("المراجعة العدائية الحية").

النتيجة المرفوضة الوحيدة: ادّعاء أن قفل الحساب بعد 5 محاولات دخول فاشلة قابل للاستغلال كهجوم حرمان-خدمة — رُفض لأن هذا **سلوك حماية brute-force مقصود ومُصمَّم فعليًا**، لا ثغرة. **دليل حي إضافي غير مخطَّط له:** فريق المراجعة العدائية نفسه اصطدم بالقفل فعليًا أثناء اختباره واضطُر لانتظار انتهاء المهلة — وأنا شخصيًا اصطدمت به مجددًا لاحقًا أثناء اختبار سرعة الاستجابة (انظر §33) — دليلان عمليان مستقلان أن الحارس يعمل تحت ضغط عدائي حقيقي.

## 41. Defects Found
**14 بندًا مُصلَحًا فورًا خلال R5 (بأدلة حية + اختبارات تراجع)، مُغطِّية 16 نتيجة مؤكَّدة (بندان مُدمَجان لاكتشافهما المزدوج من شخصيتين مستقلتين):**

**الثلاثة الأصلية (اكتُشفت أثناء بناء/تشغيل الشركة التجريبية، قبل المراجعة العدائية):**
1. `NEW-R5-STATS-CLOSING-01` — حرج — `GET /api/stats` يعرض صافي دخل خاطئ تمامًا بعد أي إقفال شهري. الإصلاح: استثناء `type:'closing'` من الجمع.
2. `NEW-R5-ASSET-RECON-SCOPE-01` — متوسط — تسوية الأصول الثابتة تُبلِّغ خطأ كاذبًا لأي شركة بأكثر من حساب أصول واحد. الإصلاح: تحديد النطاق بالحساب المطلوب فعليًا.
3. `NEW-R5-AUDIT-PERIODLOCK-01` — متوسط — قفل الفترة عبر الإقفال الشهري لا يُسجَّل في سجل التدقيق. الإصلاح: إضافة `appendAuditEvent` مطابق للمسار المستقل.

**11 بندًا من المراجعة العدائية (§40)، بترتيب الخطورة — التفاصيل الكاملة (ملف+سطر+دليل حي قبل/بعد) في `docs/ACCOUNTING_GAP_REGISTER.md` قسم ط:**
4. `NEW-R5-ADV-CLOSE-GUARD-01` — **P0** — `/api/close/run-all` كان يقفل أي فترة نهائيًا بلا تحقق من قائمة جاهزيتها الخاصة — أُثبت حيًّا مرتين مستقلتين: فترة بصفر نشاط أُقفلت بقيد خسارة مُختلَق بالكامل من التسويات التلقائية. حارس `hasActivity` جديد + مخرج `confirmEmptyClose:true` صريح.
5. `NEW-R5-ADV-JOURNAL-TOLERANCE-01` — **P0** — بوابة توازن القيد قبلت فوارق تصل 0.005 د.ك بينما فحص ميزان المراجعة نفسه أدق (0.001) — تضييق الحد ليطابق.
6. `NEW-R5-ADV-BALANCE-SHEET-EQUITY-01` — **P0** — الميزانية العمومية الافتراضية (اليوم، بلا معاملات) غير متوازنة بنيويًا كل يوم ليس نهاية شهر بالضبط (تتجاهل صافي الدخل المفتوح غير المُقفَل). إضافة حد `openPeriodNetIncome` لحقوق الملكية، بنفس نمط `/api/financial-statements` المُثبَت أصلًا. أعيد التحقق حيًّا بعد إعادة التشغيل: `balanced:true`.
7. `NEW-R5-ADV-AR-AGING-RECON-01` — **P1** — أعمار ذمم التأمين بلا تسوية GL، تُبالِغ بإدراج مطالبات ملغاة بكامل قيمتها. استبعاد الملغاة + تصحيح حقل التاريخ (`c.date`→`c.claimDate`) + إضافة `reconciliation`.
8. `NEW-R5-ADV-ACCRUED-ACCOUNT-01` — **P1** — مصروف مستحق بلا تحقق حساب يرتدّ صامتًا إلى حساب إهلاك حقيقي حسّاس (5800). جعل `accountCode` إلزاميًّا ومُتحقَّقًا.
9. `NEW-R5-ADV-EMPTY-CREATE-01` — **P1** — إنشاء مورّد/موظف/صنف مخزون بجسم فارغ `{}` ينشئ سجلات دائمة بلا اسم. تحقق `name` إلزامي على مسار الإنشاء فقط.
10. `NEW-R5-ADV-NEGATIVE-CASH-WARNING-01` — **P1** — دفعات نقدية عادية يمكن أن تدفع حساب النقدية/البنك لسالب بلا أي إشارة. تحذير `warning` غير حاجب مُرفَق بالاستجابة.
11. `NEW-R5-ADV-FORGOT-PW-ENUM-01` — **P2 (أمان)** — استجابة "نسيت كلمة المرور" تكشف حسابات حقيقية (503 مميَّز) مقابل وهمية (200 عام). توحيد الاستجابة.
12. `NEW-R5-ADV-AUDIT-RESOURCEID-01` — **P2/P3** (اكتشاف مزدوج من شخصيتين) — سجل التدقيق يتجاهل فلتر `resourceId` صامتًا رغم توثيقه. إضافته لسلسلة الفلترة.
13. `NEW-R5-ADV-PNL-DATERANGE-01` — **P2** — نطاق تاريخ معكوس/غير صالح لقائمة الدخل يُرجِع 200 صفريًّا بدل خطأ. تحقق تنسيق + نطاق معكوس، 400.
14. `NEW-R5-ADV-MALFORMED-JSON-01` — **P3** — JSON مشوَّه يُرجِع 500 بدل 400. معالج أخطاء يميّز `SyntaxError`.

**مؤجَّلة (OPEN، قرار نطاق صريح — 11 بندًا):** الأربعة الأصلية (`NEW-R5-ADMIN-USERNAME-01`، `NEW-R5-FINEXPORT-FLOAT-01`، `NEW-R5-JE-TOTALS-MISSING-01`، `NEW-R5-SOURCE-COVERAGE-01`) + 7 من المراجعة العدائية، أغلبها فجوات معمارية/ميزات جديدة حقيقية وليست إصلاح سطر واحد: `NEW-R5-ADV-TRIALBALANCE-FILTER-01` (P2، لا يصطاد الواجهة الفعلية)، `NEW-R5-ADV-APAGING-DAYS90-01` (P3، تجميلي)، `NEW-R5-ADV-JOURNAL-NO-GET-01` (P1، لا `GET /api/journal`)، `NEW-R5-ADV-PATIENTS-NO-EDIT-01` (P1، لا تعديل مريض)، `NEW-R5-ADV-DOCTORS-NO-LIST-01` (P2، لا قائمة/معرّف طبيب)، `NEW-R5-ADV-ASSETS-NO-PERID-01` (P2، لا عرض أصل فردي)، `NEW-R5-ADV-NO-SEARCH-01` (P3، لا بحث من جهة الخادم). التفاصيل الكاملة: `docs/ACCOUNTING_GAP_REGISTER.md` قسم ط.

## 42. Defects Fixed During R5
**14 بندًا** (§41) — كل واحد بدليل قبل/بعد حي، واختبار تراجع مخصَّص حيث ينطبق: `tests/p0-10-assets-bank-recon.test.js` (Recon C، Dep E، AdvFix H)، `tests/r5-stats-closing-regression.test.js` (Stats A/B، Audit A)، `tests/p0-6b-accounting-closure.test.js` (EOS D، Close C، Close D)، `tests/p0-6-accounting-diagnostics.test.js` (Close A). **إعادة تحقق حي إضافية بعد إعادة تشغيل `r5-staging`:** 8 من الإصلاحات الـ11 الجديدة (كل ما يقبل تحققًا مباشرًا بلا تدمير بيانات) اختُبرت مباشرة عبر استدعاءات API حية — كلها نجحت بالسلوك المتوقَّع بالضبط (§40، `docs/ACCOUNTING_GAP_REGISTER.md` قسم ط).

## 43. Deferred Non-Blocking Issues
**11 بندًا** (§41) — الأربعة الأصلية + 7 من المراجعة العدائية، بالإضافة إلى فجوات R4 المُعاد تأكيدها (بعض التقارير — تدفقات نقدية، أعمار AP/AR، كشوف حساب، سجل أصول، تسوية بنكية — بلا تصدير API خام، فقط CSV من الواجهة الأمامية أو JSON — نمط قائم موثَّق أصلاً، لا جديد).

## 44. Targeted Tests Added
4 اختبارات جديدة عبر ملفين (`tests/p0-10-assets-bank-recon.test.js` Recon C؛ `tests/r5-stats-closing-regression.test.js` Stats A/B وAudit A) + تعديل 9 ملفات اختبار قائمة لمواءمة سلوكين جديدين مقصودين من إصلاحات المراجعة العدائية (إلزامية `accountCode` على المصروف المستحق: 6 ملفات؛ حارس الإقفال الفارغ يتطلب `confirmEmptyClose:true` صراحة للفترات الفارغة عمدًا: 4 ملفات، بتداخل ملف واحد) — لا اختبار حُذف أو أُضعف، فقط طابق السلوك الجديد الأكثر صحة.

## 45. Full Regression
**36 مجموعة اختبار، 598/598 اختبارًا ناجحًا، 0 فاشل حقيقي، 0 مُتخطَّى** (`npx jest --forceExit`) — بعد كل الإصلاحات الـ11 من المراجعة العدائية ومواءمة الاختبارات المتأثرة. فشلان ظاهران فقط في التشغيلة الكاملة المتزامنة، **كلاهما مؤكَّد عطل بنية تحتية موجود مسبقًا** لا علاقة له بأي من الإصلاحات (انظر §46).

## 46. Open Handles
فشلان مُعزَّلان وأُعيد تشغيلهما منفردين للتأكد: (أ) `tests/p0-11-backend-parity.test.js` — مهلة `afterAll` عند إيقاف خادم Mongo التجريبي (5 ثوانٍ) — **يتكرر حتى منفردًا بمعزل عن أي تزامن** (2/2 اختبار فردي ناجح، الفشل فقط في خطاف الإنهاء) — فليك بنية تحتية معروف مسبقًا، غير مرتبط بأي تغيير R5. (ب) `tests/production-hardening-p05a.test.js` Upload F — ملف مؤقت متروك في مجلد رفع مشترك — **يمر بنجاح كامل 13/13 عند التشغيل منفردًا**، مؤكِّدًا أنه تسابق عمّال Jest المتوازيين على مجلد نظام تشغيل مشترك، لا انحدار منطقي. لا مصدر مقبض جديد من أي إصلاح R5.

## 47. Production Data Impact
`No production financial data was modified.` كل نشاط R5 (البناء، الاختبار العدائي، دورة الاستعادة) استخدم بيئة R5 المعزولة بالكامل. `data/database.json` الحقيقي ثابت الحجم/التاريخ طوال الميلستون.

## 48. Production Server Impact
لم يُشغَّل أي خادم إنتاج إضافي. كل نشاط استخدم `r5-staging` (المنفذ 3910، DB_FILE_ONLY) — بُنية معزولة بالكامل، أُعيد تشغيلها عدة مرات لالتقاط إصلاحات `server.js` (نمط قائم مُثبَت منذ R4).

## 49. Single-Company Readiness
**READY.**

## 50. Commercial Multi-Tenant SaaS Readiness
**NOT READY** (وفق التعليمة الصريحة — لم يُحاوَل إثباتها في R5).

## 51. Remaining SaaS Blockers
1. `NEW-R3-RESTORE-01` (من R3) — الاستعادة تعمل على مستوى المجموعة بأكملها، لا لكل مستأجر.
2. `NEW-R4-CONFIG-TENANT-SCOPE-01` (من R4) — `_configCache` غير مقسَّم حسب المستأجر.
لا حاجز ثالث اكتُشف في R5.

## 52. Core Hardening Phase Status
**CORE HARDENING PHASE COMPLETE.**

## 53. Recommended Development Mode
**NORMAL PRODUCT DEVELOPMENT.**

## 54. Recommended Next Product Area
بناءً على سجل الفجوات الكامل بعد R5 (14 إصلاحًا + 11 بندًا مؤجَّلًا)، أعلى قيمة عملية متبقية هي **مجموعة "الوصول الفردي للسجلات" المكتشفة عدائيًّا** — 4 بنود مترابطة تحت نفس النمط المعماري: لا `GET /api/journal(/:id)`، لا تعديل مريض، لا قائمة/معرّف طبيب، لا عرض أصل ثابت فردي (`NEW-R5-ADV-JOURNAL-NO-GET-01`/`PATIENTS-NO-EDIT-01`/`DOCTORS-NO-LIST-01`/`ASSETS-NO-PERID-01`). هذه ليست أعطالًا معزولة بل فجوة تصميم واحدة متكررة (مسارات إنشاء/قائمة موجودة، مسارات فردية/تعديل غائبة) تعيق أي عميل غير `index.html` نفسه وأي محاولة تصحيح خطأ إملائي بسيط — احتكاك تشغيلي حقيقي رصدته المراجعة العدائية حيًّا، لا افتراضي. ثانيًا بالأولوية: إكمال تغطية `source` لقيود المتابعة `NEW-R5-SOURCE-COVERAGE-01` (امتداد مباشر لبنية `JOURNAL_SOURCE_MAP` القائمة من R4).

## 55. Files Changed
**server.js** — 3 إصلاحات أصلية (`/api/assets/reconciliation`, `/api/stats`, `/api/close/run-all`'s lock step) + 11 إصلاحًا من المراجعة العدائية (`lib/journalGuard.js` BALANCE_TOLERANCE؛ `/api/reports/balance-sheet`؛ `/api/close/run-all`'s hasActivity guard؛ `/api/ar-aging`؛ `/api/accrued-expenses`؛ `/api/vendors`، `/api/employees`، `/api/inv/items`؛ `/api/vendor-bills/:id/pay`، `/api/accrued-expenses/:id/pay`؛ `/api/auth/forgot`؛ `/api/audit-log`؛ معالج الأخطاء الشامل؛ `/api/reports/pnl`) — التفاصيل الكاملة بملف+سطر: `docs/ACCOUNTING_GAP_REGISTER.md` قسم ط.
**اختبارات** — جديدة: `tests/p0-10-assets-bank-recon.test.js` (Recon C)، `tests/r5-stats-closing-regression.test.js` (Stats A/B، Audit A). مُعدَّلة لمواءمة السلوك الجديد (9 ملفات): `tests/atomicity-and-diagnostics.test.js`، `tests/p0-11-persistence-durability.test.js`، `tests/p0-6b-accounting-closure.test.js`، `tests/r4-adversarial-fixes.test.js`، `tests/r4-source-tags.test.js` (إضافة `accountCode` إلزامي)؛ `tests/r5-stats-closing-regression.test.js`، `tests/p0-6-accounting-diagnostics.test.js`، `tests/p0-10-assets-bank-recon.test.js` (إضافة `confirmEmptyClose:true` صريح لاختبارات فترات فارغة عمدًا).
**توثيق** — `docs/ACCOUNTING_GAP_REGISTER.md` (قسم ط مُوسَّع بالكامل)، `docs/R5_STAGING_ACCEPTANCE_REPORT.md`، `docs/R5_FINAL_REPORT.md` (هذا الملف).

## 56. Evidence
- انحدار كامل بعد كل الإصلاحات الـ14: 36/36 مجموعة، 598/598 اختبارًا، صفر إخفاق حقيقي (§45).
- مراجعة عدائية حية بـ6 شخصيات مستقلة + تحقق مضاد مستقل: 21 نتيجة خام → 20 مؤكَّدة (98%) → 1 مرفوضة، 11 إصلاحًا فوريًا (§40-41).
- إعادة تحقق حي لـ8 من الإصلاحات الـ11 بعد إعادة تشغيل `r5-staging` — كلها بالسلوك المتوقَّع بالضبط (§42).
- تحقق مالي مستقل أولي (قبل المراجعة العدائية): 8/8 مؤشر رقابة، صفر انحراف (§29).
- دورة استعادة كاملة حية مُثبَتة رياضيًا (§31).
- 14 فحص RBAC حدّي، صفر نتيجة غير متوقَّعة، + دليلان حيّان مستقلان أن حارس brute-force يعمل تحت ضغط عدائي حقيقي (§33، §40).
- `docs/ACCOUNTING_GAP_REGISTER.md` قسم ط — كل بند (14 مُصلَح + 11 مؤجَّل + 1 مرفوض) بملف+سطر+دليل حي.
- `docs/R5_STAGING_ACCEPTANCE_REPORT.md` — التفاصيل الكاملة.

## 57. STOP
