# Research: Tenant Isolation + Safe Backup/Restore Hardening (P4)

**Feature**: `002-tenant-isolation-backup-hardening` | **Phase**: 0 (research, `/speckit-plan`) — revised through **twelve** rounds of `/speckit-analyze` + remediation. Pass 1 closed 17 findings (8 CRITICAL, 6 HIGH, 3 MEDIUM) from the first `/speckit-analyze`. Pass 2 closed 13 findings (5 CRITICAL, 6 HIGH, 2 MEDIUM) a re-verification found in Pass 1 itself. Pass 3 closed 9 findings (3 CRITICAL, 2 HIGH, 4 MEDIUM) a re-verification found in Pass 2 itself — including, embarrassingly, getting some of its own citation "fixes" wrong. Pass 4 closed 11 findings (1 CRITICAL, 6 HIGH, 4 MEDIUM) a re-verification found in Pass 3 itself: correcting Pass 3's own wrong citation fixes, adding two previously-unconsidered decisions (19: live-server cache staleness; 20: idempotency-claim survival), correcting the config-file shutdown-flush design, and fixing several stale cross-references — Pass 4's own summary of *which* citations it fixed was itself imprecise (corrected in Pass 5). Pass 5 closed 9 findings (1 CRITICAL, 5 HIGH, 3 MEDIUM) a re-verification found in Pass 4 itself: extending Decision 19 to cover the live server racing the restore *during* apply, not only after completion; extending Decision 20 with a restore-time reportable count rather than documentation alone; a corrected `docs/PRODUCTION_RUNBOOK.md` claim about scheduled jobs; a required cleanup step for the local staging file's plaintext secrets; an inverted `quickstart.md` assertion; a missing default-tenant bullet added directly to Decision 12; and an attempted fix to this very header line's own citation-completeness which introduced a *different* citation-completeness error of its own. Pass 6 closed 8 findings (0 CRITICAL, 1 HIGH, 7 MEDIUM) a re-verification found in Pass 5 itself, mostly stale cross-references left by Decisions 8/11/14/19/20's own extensions. Pass 7 closed 9 findings (1 CRITICAL, 2 HIGH, 6 MEDIUM) a re-verification found in Pass 6 itself: an open-ended sweep found spec.md's own original FR-007 was never actually implemented across six passes and was foreclosed by Decision 10's own "never queried" language — closed by new Decision 21; the same sweep found every illustrative operator-facing message this feature drafted was in English, contradicting `scripts/restore.js`'s own established Arabic convention — closed by new Decision 22; plus five remaining stale-reference findings. Pass 8 closed 11 unique findings (0 CRITICAL, 7 HIGH, 4 MEDIUM — 14 raw findings before deduplicating across independently-overlapping review dimensions) a re-verification found in Pass 7 itself: Decision 21's own seventh-pass file-mode fallback was itself unsafe and was withdrawn; and — the largest-scope finding of the process to that point — an open-ended sweep checked this feature against every principle in this project's binding constitution for the first time and found Principle XI (Auditability) had never been examined, closed by new Decision 23. **Pass 9** closes 19 findings (2 CRITICAL, 9 HIGH, 8 MEDIUM) a re-verification found in Pass 8 itself — this header line itself, unforgivably, was one of them: it had not been updated to even mention Pass 8's existence, despite the document's own body containing extensive Pass-8 content, an error this line's own history (see Pass 5's and Pass 6's notes above) should have made this exact class of mistake unthinkable to repeat, and it had also miscounted Pass 8's own findings (7 HIGH + 7 MEDIUM summing to 14, when 14 was actually the pre-deduplication raw count and the real unique breakdown was 7 HIGH + 4 MEDIUM = 11 — corrected above in the same edit). The two CRITICAL findings: first, Decision 23's own new audit-log write is itself destroyed by a `default`-tenant restore's ordinary Step 5 apply, since `auditLog` was never excluded from `TENANT_BACKUP_ENTITY_KEYS` — the identical self-destruction hazard Decision 23 exists to prevent, one level up, with a system-wide blast radius — closed by extending Decision 8's exclusion list. Second, this project's own constitution (Principle IV, Data Preservation and Backups) requires an explicit, unweakenable typed-confirmation gate before any destructive restore, matching `scripts/restore.js`'s own existing "type نعم to confirm" pattern — `scripts/tenant-restore.js` had never specified this behavior across nine passes, an omission Pass 8's own "swept every constitutional principle" claim had itself missed — closed by new Decision 25. The remaining HIGH/MEDIUM findings closed: a related, distinct HIGH gap the CRITICAL `TENANT_BACKUP_ENTITY_KEYS` fix does not itself close — `default`'s own live application server, if running (the normal case), holds the identical stale-in-memory-cache hazard Decision 19 already documents for tenant data, and can silently overwrite the CLI tool's direct Mongo write to `default`'s `auditLog` on its very next ordinary write, since `saveDB()` marks every top-level key dirty regardless of which key the live process actually touched — acknowledged as a known residual risk in Decision 23, in the same spirit as Decision 19's own deferred write-blocking mechanism, since mandating a `default` server restart after every tenant operation would be operationally disproportionate; a newly-discovered backup-file retention gap (new Decision 24, spec.md FR-038); an incomplete restore-side audit scope (Step 1/Step 2 rejections were never audited, unlike backup's equivalent pre-flights) and a Step-5-vs-Step-6 location contradiction in how the audit call was described; a missing synthetic-`req` instruction, without which every audit event's own `tenantId` field would silently default to `'default'` regardless of the real target tenant; a factual misattribution of what this project's constitution actually records as "technical debt"; disagreeing metadata field lists for the audit event across three documents; a stale `plan.md` tests/ manifest still describing 10 pre-Pass-8 test cases for both new test files; a missing Complexity Tracking row for the new audit-write mechanism; and a pre-existing (since Pass 1) factual inaccuracy in Decision 7 about `validateBackupObject()`'s real behavior. **Pass 10** closes 15 findings (0 CRITICAL, 7 HIGH, 8 MEDIUM) a re-verification found in Pass 9 itself — no new CRITICAL, but a real, HIGH-severity design bug in Pass 9's own new retention mechanism (Decision 24) and a cluster of "Pass 9 claimed X was fixed everywhere, but wasn't" findings, the exact self-congratulatory-completeness-claim failure this document set has been repeatedly bitten by, recurring inside the very pass that explicitly added a review dimension to guard against it. The design bug: Decision 24's per-tenant backup-file retention used a raw string-prefix match with no delimiter safety, so rotating tenant `acme`'s own backups could also match and delete tenant `acme-corp`'s files — closed by switching to an exact-equality match on the fixed-width-stamp-stripped tenantId, propagated to the backup contract and T035a, with a new collision-specific test case (12a/14a). The most consequential completeness-claim failure: Decision 25's new Step 4a confirmation gate silently blocked every pre-existing mandatory automated restore test that was never updated to pass `--yes`/`RESTORE_YES=1` — closed via a deliberate blanket requirement in the restore contract's Test contract and `tasks.md`, rather than per-test enumeration, to avoid repeating the "some fixed, some missed" failure mode itself. Also closed: a stale "Decision 23/25" citation for the audit-event requirement in both `tasks.md` and the restore contract (Decision 25 has nothing to do with auditing); `tasks.md`'s own Dependencies & Execution Order summary and Task Quality Gate section never mentioning Step 4a/Decisions 24/25 at all; T064 (the runbook task) never extended for either of Pass 9's two new requirements; spec.md's own newly-backfilled Pass-8 note still carrying the "7 HIGH, 7 MEDIUM" (=14) arithmetic error Pass 9 claimed to have fixed in three *other* documents but never checked spec.md itself for; a task-count claim off by one ("85" instead of 84); this decision's own ninth-pass correction (directly above) over-generalizing a Mongo-source-only code path to "a tenant-scoped file" unconditionally, when the file-sourced case takes a structurally different branch with fewer causes; and an orphaned paragraph — verbatim Decision-22 content — misfiled at the end of this document instead of inside Decision 22 itself. Two newly-identified, previously-unexamined categories are now documented as acknowledged residual risks rather than architecturally solved, consistent with this document's own established disposition for comparably-scoped gaps: SIGINT/SIGTERM handling during the new Step 4a prompt, and the composition of the pre-restore quiesce window, the restore lock, and the new unbounded confirmation wait, never discussed together across any of the prior nine passes. **Pass 11** closes 19 findings (0 CRITICAL, 3 HIGH, 14 MEDIUM, 2 LOW) a re-verification found in Pass 10 itself — no new CRITICAL, and the most substantive finding is itself a scope-widening rather than a bug: Decision 23's own "exactly one event is still guaranteed" audit claim (ninth pass) had gone stale the moment Decision 25's Step 4a confirmation gate was introduced in that very same pass — an operator's ordinary decline at Step 4a is a fourth genuine terminal outcome of a privileged, lock-holding invocation that the "Step 1, Step 2, or Step 5/6" enumeration never covered, silently leaving a deliberate cancellation unaudited. Closed by widening Decision 23 to a fourth terminal-outcome family with its own `outcome:'cancelled'` value, propagated to the restore contract, both T053a/T056d, data-model.md, and spec.md's FR-037/FR-039. The remaining findings closed a cluster of citation and self-consistency defects, several of them fresh instances of failure classes this document set has repeatedly named: an off-by-one `scripts/restore.js:20-23`→`:21-24` citation for the `ask()` helper, duplicated verbatim in two documents (a copy-paste of one wrong citation into the other); this document's own header citing the new collision test as "(12/14a)" instead of "(12a/14a)"; a stray "(this revision)" tag left on the superseded Pass-9 sentence here, in data-model.md, and in the restore contract, while a fourth document (plan.md) had already correctly moved it — an inconsistent propagation of the exact same fix; Decision 24's own tenth-pass rewrite silently dropping the chronological-sort clause its predecessor paragraph had (restored here); `plan.md`'s Constitution Check table overclaiming Pass 10 as "the first zero-CRITICAL round since Pass 6" when Pass 8 was also zero; two places in `plan.md` mislabeling the Step-4a-blocks-pre-existing-tests finding as CRITICAL, contradicting Pass 10's own "0 CRITICAL" tally and `tasks.md`'s own matching HIGH-labeled bullet for the identical finding; a Task Quality Gate bullet in `tasks.md` claiming a Pass-9 origin it never actually had; a Phase-scoping self-contradiction in `tasks.md`'s own blanket `--yes` requirement (naming Phase-H/I tests while claiming Phase-G-only scope); `plan.md`'s own Documentation manifest never actually updated for data-model.md's or quickstart.md's real Pass-10 edits, despite the commit message's own blanket "gain Pass 10 entries" claim; and spec.md's own Pass-9 note transposing its HIGH/MEDIUM split, the identical severity-count-disagreement failure class Pass 10 itself hunted for and fixed one paragraph above in that same file, in the very next paragraph Pass 10 did not check. Every one of these is, once again, a "the pass that just fixed X claimed completeness but wasn't complete" instance — the meta-pattern this document set's own dedicated review dimension continues to catch reliably, round after round. **Pass 12 (this revision)** closes 15 findings (0 CRITICAL, 10 HIGH, 4 MEDIUM, 1 LOW) a re-verification found in Pass 11 itself — the third consecutive zero-CRITICAL round, but the largest HIGH-count of any round since Pass 9's own two-CRITICAL round, almost entirely instances of the exact same recurring meta-pattern: Pass 11's own new content — written specifically to close a completeness gap and a citation gap — introduced fresh instances of both classes of defect inside itself. Most consequentially: this document's own new "sole exception is a raw signal kill" audit-guarantee framing (the Step 4a widening's own closing sentence) both misattributed its residual-risk citation (claimed as "this same decision's own tenth-pass residual-risk addendum," when the addendum lives entirely under Decision 25, a separate section) and overstated the exception's own scope (a non-signal hard crash — e.g. an unhandled exception — during the identical window has the identical effect, not covered by "signal kill" specifically) — both corrected here, and the correction propagated to the restore contract, data-model.md, tasks.md, and spec.md, everywhere the same narrow framing had been copied. `plan.md` itself carried the largest cluster: a freshly-fabricated "CRITICAL-count history" digit sequence (introduced in the very sentence meant to correct a *different* overclaim) that zeroed out Passes 1-5's real CRITICAL counts (8, 5, 3, 1, 1) against this same document's own accurate Input-line tallies three sections above — corrected; and its own Documentation manifest, having just been praised in Pass 11's own narrative for closing the "manifest claims completeness but isn't" gap for `data-model.md`/`quickstart.md`, reproduced the identical gap for `spec.md`, `quickstart.md` (itself, again — Pass 11's own real edit to this file was never entered), `tasks.md`, and `contracts/tenant-backup-contract.md`, all four backfilled here. The backup contract's own header had an analogous self-referential gap: the eleventh-pass sentence that fixed the header's stale "never mentions Pass 10" omission was itself an eleventh-pass edit the header's own opening pass-list never named — fixed, and propagated into a genuine, previously-unnoticed structural mismatch: `tasks.md`'s T026/T036 numbered this same test suite 1-14-plus-14a (15 items, auditLog exclusion at position 13) while the backup contract's own canonical Test contract numbered it 1-12-plus-8a/12a (14 items, auditLog exclusion at 8a) — the same tests, genuinely different ordinals and a different total count in two documents both meant to be authoritative; `tasks.md` renumbered to match the contract exactly, with no test-content change. An open-ended-sweep finding, not itself a documentation defect: this pass's own new `outcome:'cancelled'` audit value is not yet distinguishable from `'success'` in `public/index.html`'s existing, unmodified Audit Trail viewer (a hardcoded binary badge and a two-option outcome filter) — acknowledged as a fourth known residual risk in Decision 23, deliberately not closed here since it lives entirely in existing UI code outside this feature's own scope and this phase touches no source code of any kind.
**Inputs**: `spec.md`, the P4 architecture audit, twelve rounds of `/speckit-analyze` findings (17, 13, 9, 11, 9, 8, 9, 14, 19, 15, 19, 15 — all source-grounded), and direct re-verification of `lib/database.js`, `lib/tenantMiddleware.js`, `scripts/backup.js`, `scripts/restore.js`, `lib/backupValidation.js`, `lib/idempotency.js`, `lib/auditLog.js`, `models/*`, `server.js`, `public/index.html`, `docs/PRODUCTION_RUNBOOK.md`, `.specify/memory/constitution.md`.

Each decision's own heading states its change category verbatim — UNCHANGED, REVISED, MAJOR REVISION, or NEW (a NEW heading additionally names which later pass extended it, where applicable) — and is the authoritative source for that decision's status; read a decision's own heading and its inline-dated correction notes directly rather than relying on a separate summary sentence here. This line previously attempted its own itemized re-summary of which decisions received which specific correction, twice (fourth pass, then fifth pass), and was found wrong both times: first for wrongly including Decisions 7, 11, 12 among those receiving a fourth-pass citation fix; then, in the very sentence meant to correct that, for wrongly asserting Decisions 13 and 14 were "unchanged in content since their respective introduction" — both in fact carry an explicit, inline-dated second-pass correction, visible in their own bodies above — and for omitting Decisions 7 and 11 (both headed REVISED/NEW and both carrying their own inline-dated corrections) from the summary entirely. Sixth pass: rather than attempt a third itemization, this line is deliberately de-itemized to stop reproducing that exact error class.

---

## Decision 1 — `persistUsers()`/`persistEntityKey()` fix: reuse `_defaultTenantFilter` (UNCHANGED — survived analysis)

Unchanged from the original pass. `persistUsers()`/`persistEntityKey()` (`lib/database.js:424,445`) are fixed by matching on `_defaultTenantFilter` and normalizing `tenantId:'default'` forward on write, exactly as originally designed. No analyze finding touched this decision. Do not redesign it.

## Decision 2 — Tenant config: synchronous cache, asynchronous warm, fail-closed on cold miss, with file-fallback parity (REVISED)

**What was wrong**: the original design had `loadConfig()`'s non-default branch perform an inline `AppConfig.findOne(...).lean()` — a Mongo round-trip — inside a function that is, and per this remediation must remain, synchronous, with ~18 unguarded synchronous callers in `server.js`. It also never defined a `_useFileFallback` branch at all, unlike the sibling `_tenantCaches` mechanism it claimed to mirror "exactly."

**Decision**: mirror `warmTenantCache()`/`loadDB()`'s existing two-part design precisely — an async warming step, then a synchronous read of an already-warm cache:

- **New function `warmTenantConfigCache(tenantId)`** (async), directly analogous to the existing `warmTenantCache(tenantId)` (`lib/database.js:794-799`): no-op for `tenantId==='default'` or when the process has no per-tenant concept to warm; for a non-default tenant, if `_tenantConfigCaches` does not yet have an entry, resolve one — under Mongo mode via `AppConfig.findOne({tenantId,key:'config'}).lean()`, merged over `_defaultConfig`; under `_useFileFallback` via the new per-tenant config file (below) — and cache it.
- **`lib/tenantMiddleware.js:94`** gains one line: `await warmTenantConfigCache(tenantId);` immediately alongside the existing `await warmTenantCache(tenantId);` — both awaited before `next()`, so by the time any route handler runs, both caches are warm.
- **`loadConfig()` stays synchronous** and gains exactly one new branch: for a non-default tenant, if `_tenantConfigCaches.has(tid)`, return the cached, already-merged value; if not — this is a bug, not a legitimate cold path, because `tenantMiddleware` always warms it first — **throw** (`Tenant config not warmed for "<tid>" — warmTenantConfigCache() must run before loadConfig()`), exactly mirroring `loadDB()`'s own existing fail-closed pattern for an uninitialized default cache (`lib/database.js:687`, `if (!_dbCache) throw ...`). This satisfies "fail closed rather than silently return global/default tenant config" without touching any of the ~18 existing synchronous callers — Express's own error handling turns an uncaught synchronous throw inside a route into a 500, which is the correct fail-closed outcome for a genuine bug (a route touching tenant config outside `tenantMiddleware`'s normal flow).
- **Default tenant's own AppConfig calls MUST be scoped too — correction, second `/speckit-analyze` pass**: the statement below ("default tenant is unaffected... no change needed there") was **wrong** in the first remediation pass and is retracted. It described the *cache/warming* mechanism correctly (default's config is populated synchronously at boot, no change to that timing) but incorrectly implied the *Mongo query scoping* was already correct too. It was not: `initConfig()`'s read (`lib/database.js:612`, `AppConfig.findOne({key:'config'})`) and its two legacy-migration writes (`:621-625`, `:633-637`, both `AppConfig.updateOne({key:'config'},...)`), plus `flushToMongo()`'s default-tenant config write (`:490-494`, `AppConfig.updateOne({key:'config'},{$set:{data:_configCache,...}},{upsert:true})`) all filter by `{key:'config'}` alone, with **zero tenant scoping** — exactly the same class of bug Decision 1 fixes for `persistUsers()`/`persistEntityKey()`, left unfixed here because the original remediation rewrite of Phase B replaced the *entire* config task list and dropped the default-tenant scoping task that existed before that rewrite. Once any non-default tenant has its own `AppConfig` document (`{tenantId:'acme',key:'config',...}` — the compound unique index `{tenantId,key}` on `models/AppConfig.js:10` permits this to coexist with `{tenantId:'default',key:'config'}`), these three unscoped call sites can read or overwrite the wrong tenant's config document, since Mongo gives no ordering guarantee for a `{key:'config'}`-only filter matching multiple documents. **Fix, restored from the pre-remediation design**: all three call sites get the identical `_defaultTenantFilter` + normalize-forward treatment as `persistUsers()`/`persistEntityKey()` — `AppConfig.findOne({..._defaultTenantFilter, key:'config'})` for the read, `AppConfig.updateOne({..._defaultTenantFilter, key:'config'}, {$set:{tenantId:'default', ...}}, {upsert:true})` for every write. This is not new scope creep — it is restoring a fix that was correctly specified once already (`contracts/tenant-backup-contract.md`'s own `default`-tenant backup query already independently arrived at the same correct pattern, which is what exposed the runtime-code gap during re-verification) and must not be dropped again.
- **Default tenant's config *warming/caching* is otherwise unaffected**: its config is already synchronously populated by `initConfig()` during `initDB()`, before the server accepts any request — no change to that timing.
- **File-fallback (`DB_FILE_ONLY`) parity**: add a new per-tenant config file path, `_tenantConfigFilePath(tid)` (derived from the same sanitization `_tenantFilePath()` already uses, at a sibling path — `data/tenants/<sanitized-tid>.config.json`, distinct from that tenant's `<tid>.json` data file). `warmTenantConfigCache()` under `_useFileFallback` reads that file if it exists (`fs.readJsonSync`), else caches a fresh copy of `_defaultConfig` (matching `loadDB()`'s own "new tenant starts from sane defaults" behavior). `saveConfig()`'s non-default branch, under `_useFileFallback`, writes to that same path using the existing `_atomicWriteJsonSync` — no Mongo call is ever attempted in file mode, closing the original design's silent gap outright. Default tenant's file-mode config path (`_configFile`/`data/config.json`) is unchanged.
- **Persistence flush (Mongo mode)**: reuse the *same* per-tenant debounce timer `_flushTenantToMongo()` already schedules for entity/user data (`lib/database.js:38-65`) — one added branch checking `_tenantConfigDirty.has(tenantId)` — not a second, parallel timer. `saveConfig()`'s non-default branch marks `_tenantConfigDirty` and calls the existing `_scheduleTenantPersist(tenantId)`. **Correction (second `/speckit-analyze` pass)**: `_flushTenantToMongo()`'s existing early-return guard, `if (!db || dirty.size === 0) return;` (`lib/database.js:41`), checks only the entity-dirty set — a tenant whose *only* pending Mongo-mode change is a `saveConfig()` call (no `saveDB()` in the same debounce window) would hit this guard and return before ever reaching the new config branch, silently dropping the config write and never clearing `_tenantConfigDirty`. The guard MUST change to `if ((!db || dirty.size === 0) && !_tenantConfigDirty.has(tenantId)) return;` — skip only when there is truly nothing to do on either axis.
- **Persistence flush (file mode) — new timer map and shutdown wiring required (second `/speckit-analyze` pass, corrected fourth pass)**: the new `_scheduleTenantConfigFilePersist(tid)` needs its own debounce-timer bookkeeping, exactly mirroring the existing `_tenantFileTimers`/`_scheduleTenantFilePersist` pair (`lib/database.js:691-712`) that already exists for entity-data file persistence — a new `_tenantConfigFileTimers = new Map()`. This new map MUST be added to the P0.11 cross-backend reset block (alongside `_tenantConfigCaches`/`_tenantConfigDirty`, with the `clearTimeout`-first treatment its own live-handle nature requires — third pass). **Shutdown wiring, corrected fourth pass**: the second pass's own instruction to "flush any *pending* tenant config-file timer" on shutdown was wrong — it does not actually mirror the real sibling code. `_flushAllTenantsOnShutdown()`'s real, current file-mode branch (`lib/database.js:831-841`) does **not** iterate `_tenantFileTimers` to decide what to flush at all; it unconditionally writes **every** tenant present in `_tenantCaches.keys()`, full stop — and the Mongo-mode half of the same function states the reasoning explicitly in its own comment (`:845-847`): "flush every tenant with any cached state, not just ones still listed in `_tenantDirty`... being conservative here costs a few redundant upserts at worst, never a lost write." This matters because a tenant's file-write retry (after a transient failure, `:703-709`) reschedules via a **bare, untracked** `setTimeout` whose handle is never stored back into `_tenantFileTimers` — so during that backoff window, a tenant has *no* entry in `_tenantFileTimers` at all even though its data is not yet durable, and a "flush only pending timer entries" shutdown step would miss it entirely. **The corrected instruction**: the config-file shutdown extension MUST unconditionally iterate `_tenantConfigCaches.keys()` (not `_tenantConfigFileTimers.keys()`) and write every entry via `_atomicWriteJsonSync`, exactly matching lines `:831-841`'s real pattern — `_tenantConfigFileTimers` is used only to *cancel* pending debounce timers first (as the reset block already does), never as the source of which tenants to flush. Omitting durable-flush-on-shutdown entirely would reintroduce the exact SIGTERM data-loss bug class this codebase's own P0.5 milestone already closed for entity data (`lib/database.js:817-822`'s own comment); the second pass's narrower "pending only" version would have reintroduced a smaller version of the identical bug for a tenant caught mid-retry.

**Why this satisfies "do not make the ~18 callers async"**: every existing `loadConfig()` call site keeps its exact current signature and calling convention. The only new failure mode (a throw on an unwarmed cache) can only actually occur if a code path reaches `loadConfig()` for a non-default tenant *without* having gone through `tenantMiddleware` first — which every existing route already does (the global `app.use('/api', requireAuth, tenantMiddleware)` mount predates this feature) — so in the entire currently-existing call graph, the throw branch is unreachable in normal operation; it exists purely as a fail-closed guard against a *future* code path that forgets to run through the middleware, exactly the kind of defense FR-002 already requires elsewhere.

## Decision 3 — Telegram/scheduler default-only boundary: five routes, not four (REVISED — list expanded)

Unchanged reasoning from the original pass (`setupBot()`'s inbound handlers have no tenant context of their own; gating the entry points that could inject a non-default identity is sufficient). **Revision**: analysis found a fifth route, `GET /api/monitor/status` (`server.js:9830`), which reads `global._tgBot`/`db.telegramChatId` — the exact process-global state the other four routes protect — but is a *read*, which the original four-route list's "influence the singleton" framing didn't cover. Re-grepped `server.js` in full this pass for every reference to `bot`, `global._tgBot`, `TelegramBot`, `sendMessage`, `runInventoryCheck`, `runMonthlyReport`, `setupBot`, `telegramChatId(s)`, `telegramToken`, and every route path containing `telegram`/`monitor` — no sixth route was found, and there is no webhook-mode Telegram setup (polling-only, confirmed at `server.js:2691`). **Decision**: add `GET /api/monitor/status` as a fifth guarded route, using the *same* `403 TELEGRAM_DEFAULT_TENANT_ONLY` response as the other four — consistency (one predictable contract across all five) was chosen over a softer "safe tenant-local response" specifically because a frontend integrating against this boundary should not need to special-case one of five otherwise-identical routes.

## Decision 4 — No Mongo multi-document transactions (UNCHANGED — survived analysis)

Unchanged. Re-confirmed no `startSession`/`withTransaction`/`replicaSet` usage anywhere in application code; `.specify/memory/constitution.md:186-188` still documents this as accepted. The offline-staging redesign (Decision 6) still requires no transaction of any kind.

## Decision 5 — `default` has no `Tenant`/`Subscription` registry row (UNCHANGED — survived analysis, now simpler)

Unchanged fact. Its practical weight is *reduced* by Decision 10 below: since `Tenant`/`Subscription` are no longer part of tenant-scoped backup/restore's *payload* at all, this fact now only matters for the (unchanged) default-tenant special-casing in the write-isolation fix (Decision 1) and for skipping the `Tenant.findOne()` active-status check Decision 21 (seventh pass) introduces for every *other* tenant — `default` is unconditionally exempt from it, having no row for such a check to ever find.

## Decision 6 — Restore staging: offline/logical, never live-Mongo synthetic tenants (MAJOR REVISION)

**What was wrong, adversarially confirmed**: the original `__restage__<tenant>__<runId>` design inserted a real, synthetic-tenantId copy of the entire backup directly into the live `User`/`EntityChunk`/`AppConfig`/`Tenant`/`Subscription` collections *before* the real tenant's old data was deleted. Three independent, fatal problems followed directly from this one choice: (a) `Tenant.slug`'s standalone unique index (`models/Tenant.js:7`, not compound with `tenantId`) collided with the still-live real tenant's own slug on every restore of an already-registered tenant — the *normal* case; (b) staged documents kept their original Mongo `_id`, colliding with a still-live target tenant's own current documents sharing that `_id` whenever the target's data had been edited since the backup was taken — quickstart.md's own canonical worked example; (c) a staged `Tenant` document with `status:'active'` became a real, reachable authentication identity for the entire staging window, with its exact synthetic tenantId sitting in plaintext in the checkpoint file.

**Decision**: staging is now **entirely offline** — no live-database write of any kind occurs before the final apply step for the real target tenant.

1. **Validate** (unchanged from the original design's Phase E: structural, scope, schema-version, tenant-identity-match, record-count checks — see Decision 7/8 revisions below for what's new inside this step).
2. **Sanitize, in memory**: for every record in every included category (users, accounting entities — see Decision 10 for what "category" now means), strip Mongo-only persistence metadata (`_id`, `__v`). Confirmed by re-reading every relevant schema (`models/User.js`, `models/EntityChunk.js`, `models/AppConfig.js`) that `_id`/`__v` are never used as application-level identity anywhere in this codebase — `id` (User), `key` (EntityChunk/AppConfig), and `tenantId` are the only identity fields any code path queries by — so stripping them is lossless, exactly matching what the existing `scripts/restore.js:40` already does for the whole-instance path.
3. **Compute per-category count + digest**: for each category, `count = array.length`; `digest = computeChecksum(canonicalJson(sanitizedArray))` — reusing the *existing*, unmodified `computeChecksum()` from `lib/backupValidation.js`. **`canonicalJson()` is a new, exported function, defined precisely (second `/speckit-analyze` pass — the first pass left it as an undefined name in prose)**, added alongside `computeChecksum()` in `lib/backupValidation.js`: it (a) sorts every record in the array by its stable identity field first — `id` for `users`, `key` for `entityChunks`/`appConfigs` — **before** serializing, and (b) recursively sorts object keys at every depth within each record. Both halves matter: without (a), two logically-identical record sets returned by MongoDB in a different physical order (no query has an implicit stable order) would hash differently, causing Decision 12's resume-skip check to falsely report "does not match" and trigger an avoidable destructive re-apply — silently degrading the mechanism's purpose without ever producing a wrong end state, but undermining the "closes the crash window" claim. This pair becomes each category's "expected" state for Decision 12's resume logic, and is written into the local staging artifact and the checkpoint (Decisions 14 and 15 *(fourth-pass correction: was "Decision 18," the runbook/success-output decision, unrelated to the checkpoint mechanism itself)*).

   **`_id`/`__v`-stripping MUST happen before every digest computation, at all three points it occurs — not only during restore staging (third-pass correction, CRITICAL)**: the paragraph above computes the staging-side digest over `sanitizedArray` (already `_id`/`__v`-stripped by step 2). But `categoryDigests` is *also* computed once at backup-write time (`tenant-backup.js`, Decision 8/9) and *again* at restore apply-time as a fresh live re-check (Decision 12) — and neither of those two other computation points was previously specified to strip `_id`/`__v` first. Mongo-mode backup queries (`Model.find().lean()`, matching the existing, unmodified `scripts/backup.js:41` precedent) return documents *with* `_id`, and the stored `collections.<cat>` array in the backup file **keeps** `_id` on each record (this does not change — it matches the existing whole-instance convention of stripping `_id` only at restore time, `scripts/restore.js:40`). Left as originally worded, this means the backup file's own `categoryDigests` would be hashed over `_id`-bearing records while the restore-staging recompute (this step) hashes the *same* records *after* `_id` is stripped — two structurally different representations of identical data that can never hash equal, making every Mongo-sourced tenant backup fail its own digest-integrity check on the very first restore, and making Decision 12's live re-check (which queries fresh, `_id`-bearing documents from Mongo) never match `expected` either, permanently defeating the resume-skip mechanism. **Fix**: introduce one shared, exported helper — `computeCategoryDigest(records) = computeChecksum(canonicalJson(records.map(stripMongoMeta)))`, where `stripMongoMeta` strips `_id`/`__v` (a harmless no-op on a record that is already sanitized) — and use *only* this helper at all three computation points: backup-write time (over the raw, `_id`-bearing `collections.<cat>` array, even though that array itself is stored unsanitized in the file, matching precedent), restore-staging recompute (over the already-sanitized staged array — the helper's strip step is a no-op here), and restore apply-time live re-check (over a fresh Mongo query's `_id`-bearing results). All three now hash the same logical content the same way, regardless of which literal representation each one started from. File mode is unaffected (its synthesized records never had `_id` to begin with).
   **Timestamp-field dependency, stated explicitly (second pass)**: sanitization strips only `_id`/`__v`, never `updatedAt`/`createdAt` — this is safe today because `User`/`EntityChunk`/`AppConfig` all declare `{versionKey:false}` with no schema-level `{timestamps:true}` option (confirmed by re-reading all three schemas), so Mongoose never overwrites an explicitly-provided `updatedAt`/`createdAt` value on insert — the digest computed from a backup's records and the digest recomputed after those same records are inserted live will match. **This is a load-bearing assumption**: if any future, unrelated change adds `{timestamps:true}` to any of these three schemas, Decision 12's resume-skip check would silently stop matching (degrading to "always re-apply," never to a wrong result) and this note must be re-verified against the changed schema.
4. **Validate ownership**: confirm every sanitized record's own `tenantId` (or, for `default`, a value `_defaultTenantFilter` would match) is consistent with the declared target tenant — defense in depth beyond the top-level backup-file `tenantId` check, catching an internally-inconsistent or tampered file.
5. **Persist the staged, sanitized, digested representation to a local file** (`backups/.restore-staging/<targetTenantId>.json` — **third-pass correction**: this line still said `<runId>.json` after Decision 15 introduced tenant-keying for exactly this file; every downstream document already said `<targetTenantId>.json` — this was the one place in this document where the correction was missed) — not a database write. Nothing under the real target `tenantId`, and no placeholder/synthetic identity of any kind, exists in Mongo at the end of this step.
6. Only the **apply** step (Decision 12, which spells out the fixed-order, digest-gated delete+insert mechanic this bullet summarizes) ever writes to the live target tenant's collections, directly with the *real* `tenantId` already set on each sanitized record — there is no more "insert under a fake identity, then repoint" two-hop mechanic. This eliminates findings C4, C5, and H4 by construction, not by patching around them. *(Fourth-pass correction: the second-pass "corrected to 18" footnote here was itself wrong — Decision 18 is the runbook/success-output decision, which only reports on this guarantee, it does not define it. The correct target, confirmed by cross-checking every citation in this document, is Decision 12.)*

## Decision 7 — Backup format separation: explicit, not accidental (REVISED)

**What was wrong**: the original design's own safety proof (research.md, the contract, plan.md's Plan Consistency Review) claimed a tenant-scoped file's rejection by the whole-instance `validateBackupObject()` was accidental rather than deliberate — reason enough on its own to make the rejection explicit below, which remains correct. **Ninth-pass correction to this paragraph's own factual claim, found by direct re-reading of `lib/backupValidation.js:21-44`**: this paragraph previously asserted "a tenant-scoped file's `collections` object *does* carry all the keys that check requires... so it actually passes that check" and that "the real, accidental reason was a missing `version` field" — both halves are inaccurate. **Tenth-pass narrowing of the ninth-pass correction itself, found by re-reading the same source once more**: the ninth-pass text stated a tenant-scoped file always fails on three independent causes (missing `version`, missing `collections.tenants`, missing `collections.subscriptions`) — but `lib/backupValidation.js:32-42` branches structurally on `backup.source`, and that three-cause analysis holds only for `backup.source==='mongodb'`. For `backup.source==='file'` (a legitimate, Decision-9-sanctioned tenant-scoped shape, produced whenever `scripts/tenant-backup.js` runs under `DB_FILE_ONLY=true`), the code takes the *other* branch entirely (`lib/backupValidation.js:40-42`) — it never inspects `collections.tenants`/`collections.subscriptions` at all, and instead pushes exactly one problem, `'file backup is missing "database"'`, since the tenant-scoped output shape (T034's `{scope,schemaVersion,tenantId,createdAt,source,recordCounts,categoryDigests,collections}`) has no top-level `database` key regardless of source. So the accurate, source-scoped accounting is: a **Mongo-sourced** tenant-scoped file fails on three independent causes (missing `version`; missing `collections.tenants`; missing `collections.subscriptions`); a **file-sourced** tenant-scoped file fails on two independent causes (missing `version`; missing `database`) — never three, and never for the `tenants`/`subscriptions` reason, since that branch is structurally unreachable for it. This has no functional consequence either way — the fix below (a deliberate, additive `scope==='tenant'` guard at the very top of `validateBackupObject()`, evaluated before the `source`-branching logic) makes the rejection explicit and correct for both sourced shapes uniformly, regardless of how many (or which) accidental reasons the pre-fix code happened to reject each one for — but both the original claim and the ninth pass's own attempted correction of it stood, uncorrected, across this many remediation passes despite each pass's own stated practice of re-verifying prior claims against the actual source, itself another instance of this document set's recurring self-congratulatory-completeness-claim failure pattern.

**Decision**: make the rejection deliberate on both sides, in `lib/backupValidation.js` (the one shared validation module both restore paths already import from):
- `validateBackupObject()` (whole-instance, existing, otherwise **unmodified**) gains exactly one new, additive check at its top: `if (backup.scope === 'tenant') return {ok:false, problems:['this is a tenant-scoped backup file — use scripts/tenant-restore.js']}`. An absent `scope` field (every existing whole-instance backup file, past and future) is unaffected — this is purely additive, zero regression to the legacy format.
- `validateTenantBackupObject()` (new, tenant-scoped) requires `backup.scope === 'tenant'` as its first structural check (unchanged from the original design — this half was already correct).
- Both cross-feed tests (Decision from the original Phase J) must call these *real*, exported functions — not a hand-rolled reimplementation — closing the class of test-quality gap Decision 17 addresses more broadly. *(third-pass correction: was cited as Decision 16 through the second pass — Decision 16 is the default-tenant duplicate pre-flight, unrelated to test-fixture quality.)*

## Decision 8 — Idempotency exclusion: a real filtered key list, not raw `ENTITY_KEYS` (REVISED)

**What was wrong**: the original design said "reuse `ENTITY_KEYS`" for the `entityChunks` backup query *and*, in the same documents, claimed idempotency records were excluded — but `ENTITY_KEYS` (`lib/database.js:111-172`) literally contains the string `'idempotencyRecords'` at line 171, so the two claims directly contradicted each other; following the tasks as written would have captured and later restored stale idempotency claims.

**Decision**: export a new, derived constant from `lib/database.js`: `TENANT_BACKUP_ENTITY_KEYS = ENTITY_KEYS.filter(k => k !== 'idempotencyRecords' && k !== 'auditLog')` *(ninth-pass extension adds `auditLog` — see below)*, and use *that* — never raw `ENTITY_KEYS` — as the single authoritative source for every tenant-backup/restore entity-category query and for record-count computation. The dedicated `IdempotencyRecord` Mongoose model remains, as originally designed, never queried by backup/restore's own capture-or-apply mechanism — it is never read during backup, never staged, and never a target of any restore-apply write, exactly as this decision intends. **Sixth-pass narrowing, to keep this claim accurate rather than merely aspirational**: the one deliberate exception is Decision 20's fifth-pass extension, where `scripts/tenant-restore.js`'s Step 6 runs a single, read-only diagnostic query against this same collection purely to report an operator-facing idempotency-staleness count, strictly after the restore's own destructive apply work is already complete — a read for reporting, not a capture-or-apply operation, and not a contradiction of this decision's actual purpose (keeping the restore's own *write* path from ever touching this collection, which remains true). **New, additive validation**: `validateTenantBackupObject()` gains a check that rejects a backup file whose `collections.entityChunks` array contains any entry with `key==='idempotencyRecords'` — a defensive check against a malformed, hand-edited, or future-version file, not just a promise about how the backup tool itself behaves — satisfying "restore must reject an unexpected idempotency-record payload rather than recreate stale duplicate-posting claims." **Extended, second `/speckit-analyze` pass**: the identical rejection applies to `key==='__restoreLock__'` (Decision 11's reserved lock key) — a backup file that illegitimately carries a planted lock-shaped record must be rejected for exactly the same reason (a malformed file should never be able to inject a document at a key this feature's own tooling treats as reserved/special), not merely excluded from *legitimate* backups by `TENANT_BACKUP_ENTITY_KEYS`.

**Ninth-pass extension, CRITICAL — `auditLog` added to the exclusion filter, closing a self-destruction hazard Decision 23 itself introduced**: found by the ninth re-verification's open-ended sweep. Decision 23 (Pass 8) writes every `tenant.backup`/`tenant.restore` accountability event into `default`'s own `auditLog` specifically *because* a tenant's own restore replaces that tenant's own `entityChunks` category, and Decision 23's whole design exists to keep the accountability record out of the thing being replaced. But `auditLog` was, until this extension, an ordinary, un-excluded `TENANT_BACKUP_ENTITY_KEYS` member — and `default` is itself an explicitly supported, first-class tenant-scoped restore target (Decision 5, Decision 16, FR-021). A restore *of `default`* would therefore delete-and-reinsert `default`'s own `entityChunks` category, `auditLog` key included, silently reverting the centralized accountability log — every tenant's `tenant.backup`/`tenant.restore` events, not only `default`'s own — to whatever stale snapshot the backup file happened to contain. This is the exact self-destruction scenario Decision 23 exists to prevent, recurring one level up, with a larger blast radius. **Fix**: `auditLog` is added to the `TENANT_BACKUP_ENTITY_KEYS` exclusion filter, exactly like `idempotencyRecords` — no tenant's own audit history is ever part of any tenant-scoped backup/restore payload, for any tenant, including `default`. This is independently well-motivated on the same grounds as the `idempotencyRecords` exclusion above (an audit trail records "what actually happened over time," and silently reverting it via a data restore is the same class of problem as silently reverting an idempotency claim) — and, as a direct consequence, it also closes the Decision 23 self-destruction hazard completely, since `default`'s own `auditLog` — the one Decision 23 concentrates every tool's events into — is now never touched by any restore's Step 5 apply, for any target tenant. **Extended validation**: `validateTenantBackupObject()`'s existing rejection (above) is extended to also reject `key==='auditLog'` in a backup file's `collections.entityChunks` array, mirroring the identical defensive rationale already applied to `idempotencyRecords`/`__restoreLock__`.
**Delete-side scoping, stated explicitly (second pass, citation corrected fourth pass — was wrongly "Decision 17," the test-fixture decision, unrelated to this)**: the corresponding restore-apply delete for the `entityChunks` category (Decision 12) MUST be scoped by `key:{$in:TENANT_BACKUP_ENTITY_KEYS}` in addition to `tenantId` — **not** a bare `{tenantId:target}` — because the restore lock (Decision 11) and the tenant's live `idempotencyRecords` document both live in the same collection, under the same real target `tenantId`, as the records restore is replacing. An unscoped-by-key delete would destroy the lock the restore is currently running under (reopening the exact concurrent-restore race Decision 11 exists to close) and permanently discard the tenant's live idempotency-claim store. The insert side was already correctly scoped this way (`TENANT_BACKUP_ENTITY_KEYS`); the delete side must match it exactly.

## Decision 9 — One unambiguous backup schema for both Mongo and file sources (REVISED)

**What was wrong**: `data-model.md`'s format table restricted the `database`/`config` file-mode encoding to "default only," directly contradicting this same design's own requirement (Decision 9, original pass) that non-default tenants support file-mode backup too — leaving the non-default file-mode case with no defined output shape at all.

**Decision**: the *output* backup file format is identical regardless of `source` — always `collections.{users, entityChunks, appConfigs}` (Decision 10 removes `tenants`/`subscriptions` from this list entirely). For `source:'file'`, the tool reads the tenant's raw per-tenant blob (`data/tenants/<tid>.json`, shape = `emptyDBShape()`'s flat fields, per `saveDB()`'s existing non-default file branch) and *transforms* it into the same shape Mongo mode would have produced: `collections.users = blob.users`; `collections.appConfigs = [{tenantId, key:'config', data: <tenant's config file content, from Decision 2's new per-tenant config file, or default's existing config.json>, updatedAt: backupCreatedAt}]` if that config exists, else `[]`.

**`entityChunks` synthesis, corrected (second `/speckit-analyze` pass, tightened third pass)**: `collections.entityChunks = TENANT_BACKUP_ENTITY_KEYS.filter(key => blob[key] != null).map(key => ({tenantId, key, data: blob[key], updatedAt: backupCreatedAt}))` — **filtering out any key absent from the blob, not mapping every key unconditionally.** *(Third-pass tightening: the filter uses `!= null`, not `!== undefined` — loose inequality against `null` excludes both `undefined` and an explicit `null` value, matching Mongoose's own `required:true` validator, which rejects both. `!== undefined` alone would have let an explicit `null` through, reproducing the identical schema-validation failure this fix exists to close, for any field a future code path ever sets to `null` instead of leaving unset.)* The original wording (`TENANT_BACKUP_ENTITY_KEYS.map(...)` with no filter) was wrong: two real `ENTITY_KEYS` members, `passwordResets` and `errorLog` (`lib/database.js:118`), are genuinely absent from `emptyDBShape()`'s object literal (confirmed by re-reading it — it jumps from `doctorExpenses` directly to `lockedPeriods`) and are only ever assigned on a tenant's *first* password reset or logged error respectively — meaning `blob.passwordResets`/`blob.errorLog` are `undefined` for any ordinary tenant that has never triggered either, which is the common case, not an edge case. Mapping unconditionally would produce `{key:'passwordResets', data: undefined, ...}`, which `EntityChunk`'s schema (`data: {type: Mixed, required: true}`) rejects at insert time — and since Mongo mode's own query (`EntityChunk.find(...)`) naturally only ever returns chunks that actually exist, file mode's transform must match that same "only what exists" behavior, not synthesize placeholders for keys that were never written. `updatedAt` for file-mode-synthesized records is the backup run's own `createdAt` timestamp (file storage tracks no per-key write time) — restore-side logic must never treat this field as meaningful per-record provenance for a file-mode-sourced backup, only as "as of this backup."
**`{ordered:false}` on entityChunks insert, stated explicitly (second pass)**: regardless of the filter above, the restore-apply `insertMany()` for the `entityChunks` category MUST pass `{ordered:false}`, exactly mirroring the existing, unmodified whole-instance `scripts/restore.js:41`'s own precedent — so that any single malformed or unexpected record (a defense-in-depth concern, not just the now-fixed file-mode gap above) can never silently abort the insertion of unrelated, legitimate financial records bundled in the same category array.

Restore's apply logic (Decision 12) never needs to know or care which backend the original backup came from — it always consumes the same `collections` shape. *(Fourth-pass correction: cited as Decision 17 through the second pass, then wrongly "corrected" to 18 in the third pass — the actually-correct target, confirmed this pass, is Decision 12, the decision that defines the apply mechanic itself.)*

---

## Decision 10 — `Tenant`/`Subscription` registry rows are out of scope for tenant-scoped backup/restore (NEW)

**The question, posed directly by the remediation instruction**: are `Tenant`/`Subscription` truly part of "the recoverable tenant state," or operational metadata that lives outside it?

**Decision, and why**: **outside it.** Three independent reasons converge on the same answer: (1) `Tenant.slug`'s global uniqueness (Decision 6) makes any live-Mongo staging of it actively dangerous, and even an offline-staged version would need cross-tenant uniqueness validation *before* apply — real complexity for data this feature's own spec already lists billing/subscription work as a non-goal for; (2) `Subscription.stripeCustomerId`/`stripeSubscriptionId` are foreign references into Stripe, whose actual state is the source of truth — silently reverting them via a tenant-data restore (e.g. an operator restoring a corrupted set of journal entries) would surprise-revert billing state that has nothing to do with what the operator was trying to fix; (3) `default` — today's only real tenant — has *no* `Tenant`/`Subscription` row at all (Decision 5), so any design that treats these as required tenant state would need an awkward "except for `default`" carve-out throughout. **Consequence**: tenant-scoped backup/restore now covers exactly three categories — `users`, `entityChunks` (via `TENANT_BACKUP_ENTITY_KEYS`), `appConfigs` — never `tenants`/`subscriptions`. This is stated as a firm scope boundary in `spec.md`'s Out of Scope section, not left implicit.

**Seventh-pass narrowing, to keep "never queried" accurate rather than accidentally over-broad**: "out of scope" here means `Tenant`/`Subscription` **data** is never captured, staged, counted, digested, or restored as part of any backup/restore payload — that guarantee is unchanged and remains absolute. It does **not** mean, and was never intended to mean, that `scripts/tenant-backup.js` may never issue a `Tenant` *existence/status* read for any other purpose. Decision 21 (seventh pass) introduces exactly one such read — a pre-flight check that the target tenant exists and is active, required by spec.md's own original FR-007 — as a narrow, deliberate, explicitly-carved-out exception to this decision's payload-exclusion rule, not a reversal of it.

## Decision 11 — Restore lock: reuse `EntityChunk`'s existing compound unique index, not a new model (NEW)

**The question**: how to make "two restores for the same tenant can't run concurrently" hold across independent OS processes (a bare in-memory flag does not), preferring an existing atomic primitive over new infrastructure.

**Rejected alternative — a new dedicated `RestoreLock` Mongoose model**: works, but is a new schema/index/migration surface for a concern this codebase already has a structurally identical, reusable primitive for.

**Decision**: reuse `EntityChunk`'s existing `{tenantId,key}` compound unique index (`models/EntityChunk.js:12`) as the atomic acquire primitive, under a reserved key that is deliberately *outside* `TENANT_BACKUP_ENTITY_KEYS` (so it is never itself captured by a tenant backup, with no separate exclusion rule needed — it simply isn't in the list a backup query filters by): `key: '__restoreLock__'`.
- **Acquire**: `EntityChunk.create({tenantId: target, key: '__restoreLock__', data: {runId, pid: process.pid, acquiredAt}})` — a plain insert, not an upsert. A concurrent second attempt for the same tenant hits the *existing* unique index and throws a duplicate-key error (Mongo error code `11000`), which is standard, well-defined MongoDB/Mongoose behavior for a unique-index violation on `insertOne`/`create` — always catchable, never a process crash; the restore script catches it, reads the existing lock document, and reports its `runId`/`pid`/age in a clear rejection message before doing anything else — satisfying "acquire atomically before apply/recovery" by acquiring it as the first *state-changing* action after argument parsing, before even validation, which is simplest to reason about and test (validation/staging are read-only/local anyway, so acquiring earlier than strictly necessary costs nothing). **Sixth-pass note**: since Decision 19's fifth-pass extension, the lock is no longer the literal first action of any kind taken after argument parsing — the unconditional pre-restore quiesce warning (Decision 19's new Step -1) now prints before it. This is not a contradiction, only a clarification: the warning is a print with no side effect on database or filesystem state, while this bullet's "very first action" language is, and always was, about the first state-changing/exclusion action specifically. No document anywhere describes the lock as acquired before the Step -1 warning prints.
- **Release**: `EntityChunk.deleteOne({tenantId: target, key: '__restoreLock__', 'data.runId': runId})` — conditioned on the *same* `runId`, so a stale release can never delete a different, newer lock. Released in a `finally` covering both clean success and clean (caught) failure.
- **Stale-lock handling — explicit, no silent auto-expiry, and no automatic liveness detection (second `/speckit-analyze` pass: this section is tightened, not merely restated)**: if the process is killed (not a clean failure), the lock document is never released and blocks all future restores for that tenant by design — this is the safe default. Recovery requires an explicit, deliberate `--force-unlock` flag in **every** case where the prior run did not exit cleanly, with **no exception** — including the "resume after a crash" scenario itself (see the correction below). Two mechanisms that might look like reasonable alternatives are explicitly rejected: an automatic age-based expiry (could race a restore that is merely slow, not dead) and a process-liveness/`pid`-existence check (unreliable across independent hosts — FR-029 requires the lock to hold across independent OS processes, which in a real deployment may mean independent machines entirely, where a local `pid` check is meaningless, and even same-host `pid`s can be reused by an unrelated process after the original exits).
  **Correction, second `/speckit-analyze` pass — the "plain resume after a kill" scenario was inconsistent between documents and is now resolved in favor of the stricter policy**: the mandatory crash-window/digest-resume test (research.md Decision 12, `tasks.md` T058 *(third-pass correction: previously mis-cited as T055, which is the Step-5 apply implementation task, not the test)*, `quickstart.md` step 8) involves a *real* process kill, which by the rule above always leaves the lock held. **The resolution is that this scenario's re-run MUST also pass `--force-unlock`** — every document describing this test/scenario is corrected to include it explicitly. "Resume is supported" therefore means: an operator (or their own orchestration, which knows for certain the prior process is dead because it is the thing that killed it or observed its exit) re-runs the identical command with the identical backup file **and** `--force-unlock` after any non-clean prior exit. This keeps exactly one lock policy, with no hidden second path, and matches this feature's own "no silent auto-expiry" principle applied consistently rather than carved around for the one scenario that happened to be inconvenient to test.
- **Force-unlock is a single, non-looping, conditioned operation — not a bare delete-then-create (second pass correction)**: `--force-unlock` MUST NOT simply `deleteOne()` unconditionally followed by a separate `create()` — that pair is not atomic, and two operators invoking `--force-unlock` at nearly the same moment could otherwise both believe they hold the lock (one deletes the stale lock, creates its own; the second, mid-flight, deletes what it still believes is the stale lock — which may by then be the *first* operator's live one — and creates its own on top). The specified sequence is exactly: (1) attempt `create()`; (2) on failure, **read** the existing lock document and delete it conditioned on **its own specific `runId`** (`EntityChunk.deleteOne({tenantId:target, key:'__restoreLock__', 'data.runId': <the runId just read>})` — a compare-and-delete, not an unconditional one); (3) attempt `create()` exactly once more; (4) if that second attempt also fails, exit non-zero reporting contention against whichever lock now exists. No retry loop — a second failure is reported as genuine contention, never retried automatically. **Third-pass clarification**: correctness here comes from the *second* `create()`'s own unique-index atomicity, not from inspecting the delete's own matched/deleted count — two processes racing this sequence each issue the same runId-conditioned delete (at most one can ever match a given stale lock), but regardless of which one's delete actually matched, exactly one of the two subsequent `create()` calls succeeds and the other hits the duplicate-key error and correctly reports contention. No `deletedCount` check is needed or should be added.
- **File mode**: an equivalent lock **file** at a fixed, per-tenant path (`backups/.restore-checkpoints/<tenantId>.lock`), created with Node's exclusive `{flag:'wx'}` (fails `EEXIST` if held), released via `fs.unlinkSync` in the same `finally`, with the identical explicit-`--force-unlock`-only recovery policy — no atomicity concern equivalent to the Mongo compare-and-delete above, since a lock-file's content is only ever read by an operator/diagnostic, not compared-and-conditionally-deleted the way the Mongo path requires.

## Decision 12 — Resume derives truth from checkpoint AND actual state, never checkpoint alone (NEW)

**The question**: the checkpoint file's own "ground truth" claim is falsifiable by a real crash window (a category's DB write succeeds, then the process dies before that category's checkpoint entry is written) — how to close this without assuming atomicity the architecture doesn't have.

**Decision**: for each category, in the fixed apply order, *before* performing its destructive delete+insert: compute the category's **current actual** count+digest for the target tenant (the same canonicalization used in Decision 6, step 3 — including the identity-key sort that makes the comparison order-independent) and compare it against the category's **expected** count+digest (computed once during staging, carried in the checkpoint). If they already match exactly, the category is treated as complete — *regardless of what `categoriesApplied` in the checkpoint says* — and is not re-applied. If they don't match, the normal tenant-scoped delete+insert proceeds. This makes the checkpoint file a *hint* that lets a normal resume skip already-verified work quickly, not the *sole* source of truth a lying checkpoint could corrupt — a category that was actually swapped just before a crash (but never got its checkpoint entry written) is correctly detected as already-done by its digest matching, and is never redundantly re-deleted-and-reinserted. This closes the crash window completely, using only a count+hash comparison this codebase already has the primitive for (`computeChecksum()`) — no new infrastructure.

**Delete-side scoping for `entityChunks` (second `/speckit-analyze` pass — cross-referenced from Decision 8)**: both the live-digest re-check query and the destructive delete for the `entityChunks` category MUST be `{tenantId:target, key:{$in:TENANT_BACKUP_ENTITY_KEYS}}`, never a bare `{tenantId:target}` — see Decision 8's "Delete-side scoping" note for why (the restore lock and the tenant's live idempotency store share this same collection and tenantId).

**Default-tenant scoping for the live-digest re-check itself (added fifth pass — this was previously stated only in data-model.md, the restore contract, and tasks.md, never here in the decision meant to be their common source)**: for `default`, the live-digest re-check query for *every* category — not only the `entityChunks` delete — MUST substitute `_defaultTenantFilter` for plain `{tenantId:target}`, exactly like the delete side already does (Decision 1's pattern). Without this, a `default` restore's digest check under-counts live legacy no-`tenantId`-field records and reports a spurious mismatch on the tenant's first restore — safe-direction (it only ever triggers an unnecessary-but-correct delete+insert rather than an incorrect skip), but not the intended behavior.

## Decision 13 — Backup fingerprint on resume: content hash, not filename (NEW)

**The question**: nothing previously compared a resumed run's backup file against the one that partially applied — an operator resuming with the *wrong* file (an old backup, a same-named-but-different file from another operator) would silently apply mismatched data with no warning.

**Decision**: the checkpoint records a `backupFingerprint` — the backup file's own sha256 (the *same* value already computed for its `.sha256` sidecar during `tenant-backup.js`, not a separately-invented hash) — alongside `targetTenantId` and `schemaVersion`. **Ordering, corrected (second `/speckit-analyze` pass — the first pass left this contradicted between documents: research.md said "before validation," the contract/data-model/tasks all said "step 1.8, last." The contract's placement is correct and is what this document now states too)**: the fingerprint check runs as the **last** step of the validation gate (Step 1.8), after structural/format/tenant-match/category-shape/idempotency-exclusion/record-count checks — reusing the file hash already computed during Step 1.1's checksum verification rather than hashing the file a second time. If an existing checkpoint is found for the target tenant (Decision 15 makes this lookup a direct, predictable path, not a search) that is not yet `stage:'completed'`, the tool compares the new file's fingerprint against the checkpoint's recorded one. A mismatch is a hard rejection, naming both files, before any further write — "filename alone is not identity" is satisfied because a renamed-but-byte-identical file is correctly treated as the *same* backup (its fingerprint matches), while a different file at the same path is correctly caught.

## Decision 14 — Atomic writes and directory creation for both the checkpoint and the local staging file (NEW, extended second pass, extended sixth pass)

**Decision**: every checkpoint write in `scripts/tenant-restore.js` uses the *existing*, exported `_atomicWriteJsonSync` from `lib/database.js` (tmp-file + `fs.renameSync`) — the same crash-safety primitive this codebase already trusts for `data/database.json` and every per-tenant file — rather than a plain `fs.writeFileSync`. **The same requirement applies to the local staging file (second pass correction — the first pass scoped this decision to the checkpoint only, leaving the staging file, written one step earlier in the sequence, with no equivalent guarantee)**: `_atomicWriteJsonSync` for both, and `fs.mkdirSync(path.dirname(...), {recursive:true})` runs before the *first* write to **each** of `backups/.restore-checkpoints/` and `backups/.restore-staging/` — mirroring `scripts/backup.js:59`/`scripts/restore.js:55,60`'s own established convention for a freshly-provisioned target, for both directories, not just one. A checkpoint read that fails to `JSON.parse` (corrupt/truncated) is treated as "no reliable prior record" — logged as a clear warning, and the tool proceeds as a fresh run (safe, because staging is always freshly recomputed from the backup file regardless, and Decision 12's real-state digest check means a fresh run never blindly re-destroys already-correct data even without a readable checkpoint).

**Sixth-pass addition — the staging file's own lifecycle across success and failure, and a correction to a wrong rationale a Pass 5 document round introduced for it**: on a successful restore (`stage:'completed'`), the local staging file is deleted automatically — it holds the same sanitized tenant records the backup file itself does, including any plaintext secret an `AppConfig` document may carry (e.g. `smtpPass`, `server.js:5544`), and has no further purpose once the apply step it fed has succeeded. On a `failed` exit, the file is deliberately left in place — **not**, as the restore contract's Step 6, `data-model.md`'s `staged`/`failed` bullets, `tasks.md` T056c, and `quickstart.md`'s step 6c all stated in Pass 5, because a `--force-unlock` resume "reads it rather than re-running Step 3." That rationale directly contradicts this very decision's own opening sentence above ("staging is always freshly recomputed from the backup file regardless") and the unconditional `Lock → Validate → Duplicate-pre-flight → Stage → Checkpoint → Apply` run sequence stated in `tasks.md`'s "Dependencies & Execution Order" section: Step 3 runs on *every* invocation, resumed or not, and would simply overwrite the file fresh regardless of whether the prior copy survived — there is no skip-staging-on-resume branch anywhere in this design, and adding one is not proposed here either. The **correct** rationale, recorded here as authoritative: the file is preserved after a failure purely as a forensic/diagnostic artifact, so an operator investigating why a restore failed — before deciding whether to retry with `--force-unlock` — can inspect exactly what was staged; deleting a file that might still be useful for that inspection, when the run did not complete successfully, is an unforced, purposeless risk with no corresponding benefit. The restore contract, `data-model.md`, `tasks.md` T056c, and `quickstart.md` step 6c are all corrected to state this rationale instead.

## Decision 15 — Checkpoint and staging files are keyed by tenant identity, not by a random run id (NEW, second `/speckit-analyze` pass)

**What was wrong**: the first remediation pass keyed both the checkpoint (`backups/.restore-checkpoints/<runId>.json`) and the local staging file (`backups/.restore-staging/<runId>.json`) by a freshly-generated, per-invocation `runId` — but Decisions 12 and 13 both require locating "the existing checkpoint for this tenant" on a resumed invocation, and a resumed invocation generates a *new*, different `runId` than the failed run's, with no directory-scan or index mechanism specified anywhere to bridge the two. As written, every invocation would look like a fresh run with no prior checkpoint to compare against — silently defeating the fingerprint-mismatch protection (Decision 13) entirely. This also created an unnecessary collision risk between two different tenants' concurrent restores if `runId` generation were ever less than fully collision-resistant.

**Decision**: key both files by `tenantId`, exactly like the restore lock already is — `backups/.restore-checkpoints/<tenantId>.json` and `backups/.restore-staging/<tenantId>.json`, one live file of each per tenant. This is not merely consistent with the lock's own naming; it is *implied* by the lock's own guarantee — since only one restore can ever be in-flight per tenant at a time (Decision 11), there is never a reason for more than one live checkpoint or staging file to exist per tenant either. `runId` remains a field *inside* the checkpoint document (for logging/diagnostic correlation with the lock's own `runId`), it simply no longer appears in either file's path. This also resolves the cross-tenant collision concern without needing to specify a particular entropy source for `runId` beyond "unique enough to correlate one run's own lock/checkpoint/log lines," since two different tenants' files can never collide on path (they're keyed by the different tenant ids) and two same-tenant runs can never coexist (the lock prevents it).

## Decision 16 — Default-tenant duplicate detection: a mandatory pre-flight, not a repair (NEW)

**The question**: research.md's original Decision 1 already flagged pre-existing default-tenant duplicate records (legacy no-`tenantId` copy + explicit `tenantId:'default'` copy of the same identity) as a real, deferred risk — analysis sharpened this into a specific, more severe conclusion: such a duplicate makes tenant-scoped *restore* of `default` provably fail outright during staging's digest computation (both copies rewritten to the same identity would violate uniqueness once inserted), and an *untargeted* `_defaultTenantFilter` delete during apply could silently collapse two live duplicates into one, discarding whichever wasn't in the backup, with no operator-visible warning.

**Decision**: add a mandatory pre-flight check, run automatically (not as an opt-in separate script) before a `default`-tenant backup writes its output file, and again before a `default`-tenant restore performs any apply write: for each logical identity (a `User.id`, an `EntityChunk`/`AppConfig` `key`) matched by `_defaultTenantFilter`, count how many *distinct* physical documents exist for it. If any identity has more than one, **hard-fail** — print the exact identity and category, and both conflicting documents' `_id`s for operator reference — and produce no backup file / apply no restore write. This is explicitly a **detection gate**, not a repair: the tool never guesses which copy is canonical, never merges, never silently discards. Resolving a real conflict, if one is ever found, is a separate, deliberate, operator-reviewed action outside this feature (unchanged from the original Decision 1's framing) — the difference this pass makes is that the *tool itself* now refuses to proceed past an undetected conflict, rather than silently producing a corrupted backup or a restore that fails opaquely mid-apply.

## Decision 17 — Legacy no-`tenantId` test fixtures must bypass Mongoose's own schema default (NEW)

**The question**: `User`/`EntityChunk`/`AppConfig` all declare `tenantId: {..., default: 'default'}`. Mongoose applies a field's `default` at document construction for `new Model()`, `Model.create()`, and `Model.insertMany()` alike — the only seeding pattern used anywhere in this codebase's existing Mongo-backed tests. A test that seeds a "legacy, no-`tenantId`-field" fixture through that normal API silently persists an *explicit* `tenantId:'default'`, never a genuinely missing field — meaning every planned legacy-compatibility test would pass even if the fix regressed to the exact naive-equality-filter bug Decision 1 exists to prevent.

**Decision**: every task that seeds a "legacy, no-`tenantId`-field" fixture MUST do so via the raw MongoDB driver collection object — `User.collection.insertOne({...})` / `EntityChunk.collection.insertOne({...})` / `AppConfig.collection.insertOne({...})` (or `mongoose.connection.db.collection(name).insertOne(...)`) — which bypasses Mongoose's schema-default/casting layer entirely, and MUST immediately read the seeded document back (`.findOne(...).lean()` or equivalent) and assert `!('tenantId' in doc)` **before** exercising the behavior under test. This is a proof-of-fixture-validity check, not just a proof-of-behavior check, and is now a binding requirement (spec.md FR-027), not merely a task-writing convention.

## Decision 18 — Operator-facing honesty: runbook update and precise success output (NEW)

**Decision**: `docs/PRODUCTION_RUNBOOK.md` (the actual operational document this codebase's incident response already points to — confirmed to exist and already document the whole-instance `backup.js`/`restore.js` flow at lines 78/153) gains a new section covering: the tenant backup/restore commands, the explicit distinction from the whole-instance commands, full-tenant-replace-only semantics, resume support (yes, same-backup re-invocation, **always requiring `--force-unlock` after any non-clean prior exit — Decision 11's tightened policy**) versus rollback (**not** supported — reverting an already-applied category requires restoring again from an earlier backup, not an automatic undo), the `default`-duplicate hard-block, and the Telegram/scheduler default-only limitation. `scripts/tenant-restore.js`'s own success output explicitly states the target tenant name and that a **tenant-scoped** restore path was used — a claim about the tool's own design guarantee (every write is filtered to that one tenant, by construction, per Decisions 8 and 12 *(fourth-pass correction: was "8/17" — Decision 17 is the test-fixture decision and does not define this guarantee)*), not an empirically-checked claim about every other tenant's state, which the tool does not and should not spend time verifying at runtime.

**Critical caveat, added fourth pass — this design guarantee covers only the *target* tenant's own collections, not the *live application server's* view of them**: see the new Decision 19 below.

## Decision 19 — A tenant restore MUST require the target tenant's live traffic to be quiesced BEFORE it begins and the server process(es) restarted AFTER it completes, because the running server's in-memory cache is never invalidated by an out-of-process Mongo write and can race the restore itself, not just follow it (NEW fourth pass, EXTENDED fifth `/speckit-analyze` pass, CRITICAL)

**The question, found by re-verification, never previously considered in any of the first three passes**: `scripts/tenant-restore.js` is a separate CLI process that writes directly to Mongo. The live application server process, if it is still running (the normal, expected deployment topology — nothing in this design tells an operator to stop it), holds its own long-lived, per-tenant in-memory cache (`_tenantCaches`/`_dbCache`, `lib/database.js:17-22` and `:176`) that is populated once — **eagerly, at process boot, for `default`** (`initDB()`, `lib/database.js:987-995`) **and lazily, on first request, for every other tenant** (`warmTenantCache()`/`_loadTenantDB()`, `lib/tenantMiddleware.js:93-94`) — and is **never** re-read from Mongo after that either way. `loadDB()`'s default-tenant branch (`:686-688`, the tail of the function spanning `:657-689`) unconditionally returns the already-cached `_dbCache`. Every ordinary write for that tenant flows `saveDB()` → dirty-key marking → the existing debounced (or, for `{durable:true}` callers — the majority of this codebase's financial routes — *immediate, synchronous*) `_flushTenantToMongo()`/`flushToMongo()` → `_persistEntityKeyTenant()`/`persistEntityKey()`, all of which read the value to persist **from that same stale in-memory object**, never from a fresh Mongo query. Critically, `saveDB()` marks **every** top-level key of the in-memory object dirty on every call (`lib/database.js:753,779` — `Object.keys(db).forEach(...)`), not only the key that actually changed — so *any* unrelated write for that tenant re-persists the *entire* stale snapshot, including whatever the restore just changed.

**Fifth-pass extension — this is not only a "the next write after the restore finishes" risk, as the fourth-pass version of this decision framed it**: because dozens of this codebase's financial routes call `saveDB(db, {durable:true})` (a *synchronous*, immediate flush, not the debounced path), a live write for the target tenant arriving **during** the restore's own Step 5 apply window can itself race the restore's delete+insert for a category — recreating a document the restore just deleted (via the live write's own upsert), which the restore's subsequent `insertMany` then collides with on `EntityChunk`'s compound unique index, or silently re-persisting a category the restore had already correctly applied earlier in the same run, using the stale whole-tenant snapshot. This is strictly worse than a post-completion overwrite: it can corrupt the restore *while it is still running*, and the restore lock (Decision 11) does nothing to prevent it, since the lock is checked only by `tenant-restore.js` itself — the live server's normal request/write path has no knowledge of it.

**Decision**: this is a hard operational prerequisite on **both ends**, not an edge case to design around with new invalidation or write-blocking infrastructure. Consistent with this codebase's own established pattern (`docs/PRODUCTION_RUNBOOK.md`'s existing table of settings that "require a restart" to take effect, and this codebase's identical, pre-existing, previously-undocumented exposure to this same staleness mechanism for the *whole-instance* `scripts/restore.js`, which has never quiesced live traffic either) and this feature's own "no unnecessary new infrastructure" constraint, the fix is **explicit, loud, and operational on both sides of the restore, not architectural**:
- **Before** any restore begins (printed unconditionally, before Step 0's lock acquisition, on every invocation — not gated behind a flag): `scripts/tenant-restore.js` MUST print a loud warning that the target tenant's live traffic should be quiesced (the server stopped, or that tenant otherwise prevented from accepting writes) before proceeding, because a write arriving during the restore can corrupt it, not merely follow it.
- **After** a restore completes: Step 6 (Finalize)'s success output MUST state, as part of the same success message, that the target tenant's live application server process(es) MUST be restarted before any further write to that tenant is accepted — e.g. *"✅ اكتملت استعادة المستأجر "acme" فقط، عبر فلاتر مخصّصة لهذا المستأجر — لم تُكتب بيانات أي مستأجر آخر. ⚠️ مهم: أي عملية سيرفر تخدم هذا المستأجر حالياً تحتفظ بذاكرة مؤقتة (cache) قديمة ويجب إعادة تشغيلها الآن، قبل أي كتابة أخرى لهذا المستأجر، وإلا فقد تُلغى هذه الاستعادة صمتاً."* (Arabic, seventh pass — see Decision 22 for why every new operator-facing message this feature introduces must be Arabic, matching `scripts/restore.js`'s own existing convention).
- `docs/PRODUCTION_RUNBOOK.md`'s new tenant-restore section (research.md Decision 18, `tasks.md` T064) MUST state both the pre-restore quiesce step and the post-restore restart step as mandatory, numbered steps in the restore procedure itself, not a footnote.
- This applies identically to `default` and to every other tenant — `default`'s cache (`_dbCache`) is exposed to exactly the same staleness mechanism as any other tenant's `_tenantCaches` entry, just populated at a different moment (boot vs. first request).
- This is **not** a new mechanism to build (no admin endpoint, no IPC signal, no cache-invalidation pub/sub, no per-tenant write-blocking middleware) — it is the same "requires a restart"/"run deliberately, manually, with the operator's full attention" operational discipline this codebase and its own runbook already use for other out-of-band state changes and for the existing whole-instance restore, applied here for the first time to a tenant-scoped write path, and extended for the first time to explicitly cover the *pre*-restore window as well as the post-restore one. Building an automatic write-blocking/quiescing mechanism (e.g. a restore-lock-aware `tenantMiddleware` check that rejects writes for a locked tenant) was considered and explicitly rejected as disproportionate for this phase: it would require touching the live request path itself (`tenantMiddleware.js`/`saveDB()`), a materially larger surface than this phase's own backup/restore tooling, for a risk this codebase's existing whole-instance restore has always carried, undocumented, without incident severe enough to have prompted it before. Recorded as a follow-on hardening candidate for a later phase, not silently dropped.
- **Explicitly out of scope, not silently dropped**: multi-instance/horizontal-scaling deployments, where "restart the server"/"quiesce traffic" would mean coordinating across an unknown number of instances or a load balancer, are already outside this codebase's current architecture (Constitution Principle VI) and this feature's own non-goals — this decision does not attempt to solve that harder problem, only to stop the current, real, single-instance case from silently failing or being corrupted mid-restore.

## Decision 20 — Idempotency-claim survival across a restore is a real, acknowledged residual risk, not silently assumed safe (NEW fourth pass, EXTENDED fifth `/speckit-analyze` pass with a restore-time count, HIGH)

**The question, found by re-verification**: Decision 8 correctly excludes `idempotencyRecords` from every tenant-scoped backup/restore, so a restore never touches them — they survive, byte-unchanged, across a restore that otherwise fully replaces the tenant's `users`/`entityChunks`/`appConfigs`. Every prior mention of this exclusion (this document, both contracts, `tasks.md`) frames it purely as a safety win (the delete-scoping fix, Decision 12, exists specifically to make sure of it). None of them consider the other side: `lib/idempotency.js`'s real, already-implemented claim mechanism treats a `COMPLETED` idempotency record as a permanent, unconditional short-circuit for any future retry of the same `(tenantId, operationScope, key)` — it replays the stored `resultReference`/`journalId` snapshot rather than re-executing, with **no check that the referenced entity still exists** in the tenant's current data. A tenant restore that reverts `journalEntries`/similar data to an earlier point can leave a `COMPLETED` idempotency record referencing a journal entry that the restored data no longer contains — and any legitimate retry of that original operation (a webhook redelivery, a client's own retry logic) will be silently told "already done" and replay a now-dangling reference, never re-creating the entry.

**Decision**: this is a genuine, real limitation of restoring `users`/`entityChunks`/`appConfigs` while deliberately leaving `idempotencyRecords` untouched (Decision 8) — not a defect in Decision 8's own exclusion logic, which remains correct (restoring idempotency claims *would* reintroduce the stale-duplicate-posting risk Decision 8 was written to prevent in the *other* direction). Rather than silently accepting this residual risk unacknowledged, it MUST be documented explicitly: `spec.md` gains a new Edge Case entry, and `docs/PRODUCTION_RUNBOOK.md`'s new tenant-restore section (`tasks.md` T064) MUST state plainly that a tenant restore does not roll back idempotency claims — with the documented remedy being operator inspection of `idempotencyRecords`, not an automatic mechanism (which would reintroduce exactly the kind of "restore auto-repairs a discrepancy it notices" behavior spec.md's own FR-012 already forbids). **The remedy's own inspection guidance must name both `sourceId` and `journalId`, not only `journalId` (fifth-pass correction)**: at least one real, already-wired operation scope (`vendor:create`, `server.js:5898`) completes its claim with `journalId:null` and identifies the created entity only via `sourceId` — an operator checking only `journalId` would miss this class of claim entirely.

**Fifth-pass extension — a cheap, restore-time signal, not just a static paragraph**: unlike a general "go audit everything" instruction, a precise, restore-time-computable count is available at negligible cost and MUST be surfaced by the tool itself, not left entirely to a runbook paragraph an operator might never open. Because a tenant-scoped restore is a full replace and the backup file's own `createdAt` timestamp is already loaded and validated before Step 6 runs, `scripts/tenant-restore.js`'s Step 6 (Finalize) success output MUST also print a count of the target tenant's `idempotencyRecords` with `status:'COMPLETED'` and a `completedAt` later than the backup's own `createdAt` — every such record is, by construction, guaranteed not to be reflected in the just-restored data. This is read-only reporting (a `countDocuments()`-class query, no write, no repair), consistent with FR-012, and mirrors the precedent Decision 19 already set for surfacing an operational caveat directly in the tool's own output rather than only in the runbook. If the count is `0`, the success output MUST print that explicitly — **in Arabic (research.md Decision 22; corrected eighth pass — this line was missed by Pass 7's own sweep despite Decision 22's claim to have corrected every example in this document)**, e.g. `ℹ️ لا يوجد أي سجل idempotency معرّض للخطر (0).` — rather than omit the line — an omitted line reads as "not checked," not "checked and clean."

**Sixth-pass clarification — which physical representation this count actually queries**: `lib/idempotency.js` maintains idempotency state in two genuinely divergent physical forms in Mongo mode, confirmed by direct re-reading. (1) The dedicated `IdempotencyRecord` Mongoose collection (`models/IdempotencyRecord.js`), with its own queryable `status`/`completedAt` fields, populated only by `finalizeIdempotencyClaimMongo()` — which every caller invokes *after* that same request's `saveDB(db,{durable:true})` has already succeeded. (2) The `EntityChunk{key:'idempotencyRecords'}`-embedded array (`db.idempotencyRecords[]`, itself deliberately excluded from tenant backup/restore by this very decision's key-filter above), which `completeIdempotencyClaim()` marks `COMPLETED` synchronously and in-memory *before* that same `saveDB` call. Between those two points in a single request's lifecycle, the two representations can briefly disagree. This count MUST query representation (1), the dedicated `IdempotencyRecord` collection — `IdempotencyRecord.countDocuments({tenantId:target, status:'COMPLETED', completedAt:{$gt: backup.createdAt}})` — since it is the representation with its own indexed, queryable `completedAt` field this count's comparison depends on, and the one whose write is already ordered strictly after the durable save the idempotency guarantee itself depends on. In `DB_FILE_ONLY` mode there is no dedicated `IdempotencyRecord` collection at all — the equivalent count there is computed by loading the target tenant's own file-mode data (exactly as Step 6 already must, to report anything about that tenant) and counting entries in its `idempotencyRecords[]` array whose `status==='COMPLETED'` and `completedAt` postdates `backup.createdAt`; no representation-choice ambiguity exists in file mode, since the array is the only representation that backend has.

## Decision 21 — Tenant-scoped backup MUST reject a nonexistent or inactive target tenant, via a narrow, deliberate exception to Decision 10's payload-exclusion rule (NEW seventh pass, CRITICAL; file-mode behavior corrected eighth pass)

**The question, found by the seventh re-verification's open-ended sweep, closing a gap present since before any remediation pass began**: `spec.md`'s FR-007 — an original requirement, not introduced by any remediation pass — requires: "Requesting a tenant-scoped backup for a tenant identifier that does not exist, or is not active, MUST fail with a clear error and produce no artifact." Decision 10 (Pass 1, above) excludes `Tenant`/`Subscription` from tenant-scoped backup/restore's *payload*, describing them as "never queried" — a policy correctly aimed at keeping billing/registry data out of the backup file itself. No decision, contract, or task anywhere in this document set ever actually implemented FR-007's own, separate requirement: a lightweight existence/active-status *check* against the `Tenant` registry, not a data capture, for a non-`default` target tenant. Left as originally scoped, Decision 10's "never queried" language technically forecloses even this single read, leaving FR-007 unimplementable as written and directly contradicted by User Story 2's own Acceptance Scenario 3, which restates FR-007 almost verbatim. (Decision 5's own text already alluded to "skipping any `Tenant.findOne()` active-status check that might otherwise gate a `default` restore" — implying such a check exists for *other* tenants elsewhere in this design; it never did, anywhere, until now.)

**Decision**: for any `--tenant=` other than `default`, and **only when running in Mongo mode** (`MONGO_URI` set — see the file-mode note below), `scripts/tenant-backup.js` MUST perform exactly one additional read as its very first substantive step, before any entity-data query and before the default-duplicate pre-flight's non-default equivalent (there is none — this check is what stands in that place for non-default tenants): `const tenant = await Tenant.findOne({tenantId}).lean(); if (!tenant || tenant.status !== 'active') { <hard-fail, write nothing> }` — reusing `lib/tenantMiddleware.js:72-73`'s own existing, already-proven pattern verbatim, not inventing a new one. This is a narrow, explicit, deliberate exception to Decision 10's payload-exclusion rule, not a reversal of it: Decision 10 remains fully correct and unchanged about backup/restore *payload* content — `Tenant`/`Subscription` data is still never captured, staged, counted, digested, or restored — this is a single existence-check read, discarded immediately after the pass/fail decision, that never becomes part of any backup artifact. For `--tenant=default`: this check is skipped entirely and unconditionally — `default` has no `Tenant` registry row at all (Decision 5), and is always treated as existing and active, consistent with every other place in this design `default` is exempted from registry-dependent logic (e.g. FR-021's restore-side carve-out).

**File mode — corrected eighth pass, this decision's own original file-mode fallback was itself unsafe**: file-mode backup (`DB_FILE_ONLY`, no `MONGO_URI`) has no `Tenant`/`Subscription` registry to query at all — these are Mongo-only concepts in this codebase's current architecture. This decision originally (seventh pass) proposed treating `_tenantFilePath(tenantId)` not existing on disk as "does not exist," hard-failing the backup. **That heuristic is wrong and is withdrawn**: `loadDB()`'s non-default-tenant branch (`lib/database.js:657-689`) creates a tenant's in-memory sandbox *lazily*, on first access, and persists it to disk only lazily too, via the debounced/durable `saveDB()` path — there is no eager file-creation step at tenant provisioning. A genuinely real, active, freshly-onboarded tenant that has logged in and browsed but never yet triggered a durable write would have no file on disk, and the withdrawn heuristic would have rejected it as nonexistent — reproducing, one layer lower, the *exact* failure mode FR-035 itself exists to rule out ("never by inferring nonexistence from an absence of backed-up records... which would incorrectly reject a real, newly-onboarded tenant with zero data yet"), and directly contradicting this spec's own pre-existing Edge Case for that scenario. **Corrected decision**: file mode does not enforce FR-007's existence/active-status check in this phase — a tenant-scoped backup for any non-`default` `--tenant=` in file mode proceeds without this check, and simply produces a backup with zero records if the tenant has genuinely never persisted anything, exactly as the "brand-new signup" Edge Case already requires. This is a deliberate, narrower interpretation of FR-007 for this specific backend, not an oversight: building a parallel, reliable status-tracking mechanism for file mode — a backend this codebase already treats as a local/offline fallback rather than the primary multi-tenant deployment target, and one this feature's own Decision 22 and elsewhere already treats as secondary to Mongo mode — is out of scope for this phase, and a broken heuristic is worse than no check at all, since it produces a confident, wrong rejection rather than an honestly-unenforced gap.

## Decision 22 — Every new operator-facing message this feature introduces MUST be Arabic, matching this codebase's own existing convention (NEW seventh pass, MEDIUM)

**The question, found by the seventh re-verification's open-ended sweep**: every illustrative example message drafted across this document, both restore/backup contracts, and `tasks.md` for `scripts/tenant-backup.js`/`scripts/tenant-restore.js`, across all six prior remediation passes, was written in English. This directly conflicts with the already-shipped sibling script `scripts/restore.js`, whose every operator-facing `console.log`/`console.error` string is Arabic (confirmed by direct re-reading, e.g. `"❌ يجب تحديد الوجهة صراحة: --target=<اسم> ..."`, `"✅ اكتملت الاستعادة إلى..."`), and with this same feature's own Telegram/monitor 403 boundary (`telegram-scheduler-boundary-contract.md`'s error body is specified in Arabic).

**Decision**: every new operator-facing string `scripts/tenant-backup.js`/`scripts/tenant-restore.js` print — errors, warnings, progress, and success messages alike — MUST be Arabic, in the same register and convention `scripts/restore.js` already established: a leading emoji (❌ hard failure, ⚠️ a warning that does not itself stop execution, ✅ success, ℹ️ informational/reporting-only text, ↻ resume/recovery guidance, 🎯/🗄️ target/source echoes), concise imperative Arabic sentences, and English kept only for identifiers that are not natural to translate (`tenantId`, `MONGO_URI`, `IdempotencyRecord`, `--force-unlock`, file paths) — mirroring `scripts/restore.js`'s own existing mixed-language convention exactly, not a full English-free translation that would make technical identifiers unrecognizable to an operator already familiar with this codebase's other scripts. Every illustrative example message in this document, both contracts, and `tasks.md` was intended to be corrected to this Arabic convention as part of the same pass — **eighth-pass correction: this claim was itself not fully accurate; one example (the Step 6 zero-count idempotency message, this document's own Decision 20) and two operator-facing quickstart.md verification steps were missed by the seventh pass's own sweep and are corrected in the eighth**. A dedicated test requirement (extending T040a's/T056a's existing content-assertion pattern) asserts the actual Arabic substring for every safety-critical message this decision covers, not merely "some warning printed" — and, as of the eighth pass, the *same* Arabic text verbatim wherever a message is quoted in more than one document (research.md, a contract, and `tasks.md` must never diverge in wording for what is meant to be one literal string).

**Why this was missed for six passes**: every one of Passes 1-6 focused on the *mechanics* of tenant isolation and backup/restore safety (the live-Mongo staging hazard, cache staleness, digest correctness, lock atomicity) — none included a linguistic/message-convention consistency check against this codebase's own established operator-facing text. The discovery of `scripts/restore.js`'s and the Telegram contract's own concrete, in-repository Arabic precedent settles this independently of any broader question about what CLAUDE.md's own reporting-language instruction was intended to cover. *(Tenth-pass correction: this paragraph — verbatim, about this decision's own topic — was previously misfiled at the very end of this document, after Decision 25's unrelated "why this was missed for nine passes" paragraph, instead of here in its own decision. Relocated, not reworded.)*

## Decision 23 — `scripts/tenant-backup.js`/`scripts/tenant-restore.js` MUST record one audit event per invocation via the canonical mechanism, per this project's own binding constitution (NEW eighth pass, HIGH; self-destruction hazard closed, scope widened, and mechanics corrected ninth pass)

**The question, found by the eighth re-verification's open-ended sweep, closing a gap present in all seven prior passes**: this project's constitution (`.specify/memory/constitution.md`, Principle XI, Auditability) states, unconditionally: "New privileged or financial functionality MUST use the canonical audit mechanism (`appendAuditEvent()` in `lib/auditLog.js`) unless a documented exception is recorded in the spec." `scripts/tenant-restore.js` is new, privileged, destructive functionality by every measure this feature itself uses to justify its own safety machinery (a full-tenant-data-replace operation gated by a restore lock, a mandatory pre-restore quiesce warning, and a checkpoint/resume mechanism, precisely because of how consequential it is); `scripts/tenant-backup.js` is new and privileged, if far less destructive. Across seven passes, no decision, contract, or task has ever required either tool to call `appendAuditEvent()`, and `spec.md`'s Out of Scope section records no documented exception — the one condition the constitution's own text says would excuse this. `plan.md`'s own Constitution Check table has certified "XI. Auditability | PASS | Unchanged" in every single pass, including the seven that came before this one, without ever actually re-deriving that status against this feature's own new capability — the "Unchanged" basis was asserted, not analyzed, every time. **Ninth-pass correction, to a claim in this same paragraph**: this decision previously claimed "the constitution itself records [`scripts/backup.js`/`scripts/restore.js`'s lack of `appendAuditEvent()`] as pre-existing... technical debt... not a pattern to copy into new code" — that overstated what the constitution actually says. Direct re-reading of `.specify/memory/constitution.md`'s Principle XI section and its "Known Architectural Risks & Non-Goals" register confirms the only "recorded technical debt" the constitution names is a *different*, unrelated set of direct `db.auditLog` writes (password-reset, startup auto-repair, COA fix-codes) that still bypass `appendAuditEvent()` — it never mentions `scripts/backup.js`/`scripts/restore.js` at all. Those two scripts' total absence of audit logging is an unexamined gap, not documented debt. The underlying scoping decision below (retrofitting those two unmodified files is out of scope for this feature) still stands on its own merits — this feature's standing "never touch those two files" boundary (Decision 7) — but not on the misattributed constitutional citation.

**Decision**: `scripts/tenant-restore.js` MUST call `appendAuditEvent()` (`lib/auditLog.js`) exactly once for every invocation that reaches **any** terminal outcome — **ninth-pass widening**: this previously said "reaches Step 6 (Finalize)," which is inaccurate on two counts corrected here. First, Step 5's own error-handling already fully owns and exits a failed apply *within* Step 5 itself (release lock, exit non-zero) — it never separately "reaches Step 6" as a distinct code path, so describing the audit call as living inside Step 6 for both outcomes contradicted this contract's own Step 5 prose. Second, and more substantively, "Step 6 only" left every hard-fail at Step 1 (the validation gate — malformed, wrong-version, tenant-mismatched, or digest/fingerprint-mismatched backup) and Step 2 (the `default`-duplicate pre-flight) with **no** audit event at all, even though both run after the lock is already held, both are genuine terminal outcomes of a privileged invocation, and `scripts/tenant-backup.js`'s own equivalent pre-flight rejections *are* audited under this same decision — an unjustified asymmetry. **Corrected scope**: the audit call fires exactly once, at whichever step is the *first* one that produces a terminal exit for that invocation — Step 1, Step 2, or Step 5/Step 6's own success-or-failure exit — never more than once, since only one such step is ever reached per invocation. `scripts/tenant-backup.js` MUST call it exactly once per invocation that reaches a terminal outcome (a written backup file, or a hard-fail from the default-duplicate pre-flight, the non-default existence/active-status pre-flight, or any other validation gate) — this half was already correctly scoped in the eighth pass. Neither tool calls it for a plain, successful early exit with no attempted action (e.g. `--tenant=` missing, before Step -1/Step 0 begin).

**Eleventh-pass widening, HIGH — Step 4a's own decline path is a fourth genuine terminal outcome this scope statement omitted**: the ninth-pass "Step 1, Step 2, or Step 5/Step 6" enumeration was written before Decision 25's own Step 4a confirmation gate existed (that gate was introduced in this same ninth pass, elsewhere in this document, but this sentence was never revisited against it). An operator who reaches Step 4a and declines — types anything other than `نعم`, or gives an empty response — is a genuine, ordinary (non-crash) terminal outcome of an already-lock-holding, already-staged, already-checkpointed, privileged invocation: the lock is released and the process exits 0, per Decision 25's own text. This is not Step 1, Step 2, or Step 5/6, so under the scope as previously stated, **no audit event fires for it at all** — silently contradicting both this decision's own "exactly one event is still guaranteed regardless of which [step]" claim (now corrected below) and Principle XI's own unconditional "new privileged functionality MUST use the canonical audit mechanism" text, which draws no distinction between "declined" and "failed" as reasons an operator never let a privileged operation complete. **Corrected scope, now four terminal-outcome families**: Step 1, Step 2, Step 4a's own decline, or Step 5/Step 6's own success-or-failure exit — call `appendAuditEvent()` exactly once at whichever is reached first, with a new `outcome:'cancelled'` value for the Step 4a case specifically (distinct from `'failure'`, since a deliberate operator decline is not itself an error — nothing was attempted, matching the same distinction Step 4a's own text already draws for its own exit-code choice). **The one deliberate exception, not silently dropped**: a raw process kill *during* the Step 4a prompt — a SIGINT/SIGTERM interrupt (Decision 25's own tenth-pass residual-risk addendum, not this decision's own — **twelfth-pass citation correction**: this paragraph previously, wrongly, cited it as "this same decision's own tenth-pass residual-risk addendum, above," but that addendum lives entirely under Decision 25, a separate section; see Decision 25 for the full reasoning), or, equally, an unhandled exception thrown before the confirmation is captured — still produces **no** audit event, since the process dies before any write can occur, and this decision's own standing boundary (no signal handler, mirroring `scripts/restore.js`'s own equally-unhandled gap) is unchanged by this widening. **Twelfth-pass broadening**: this exception is not specific to *signals* — any hard process termination during the narrow window between Step 4a's decline being recognized and the audit write completing has the identical effect, for the identical reason (nothing can write once the process is gone); "signal kill" was too narrow a description of the actual boundary. "Exactly one event is guaranteed" now means "for every terminal outcome the process survives long enough to record," not unconditionally for every invocation — a qualification equally true, if less practically relevant, for Steps 1/2/5/6 as well (an unhandled crash mid-Step-1, for instance, would identically prevent that step's own audit write), not a new category introduced solely by Step 4a.

Reusing `lib/tenantMiddleware.js:74-89`'s own existing precedent for exactly this kind of situation — "the rejected tenant has no trustworthy log of its own... this goes to the default tenant's audit log, the one an admin/support user can actually read" — every event from both tools is written to **`default`'s own `auditLog`**, never to the target tenant's own log: the target tenant's `entityChunks` category is exactly what a restore's own Step 5 apply is about to delete-and-reinsert, so a record of the restore stored *inside* the thing being replaced could itself be destroyed by the very operation it documents. **Ninth-pass, CRITICAL**: this reasoning has a gap one level up that the eighth pass missed entirely — `default` is itself a normal, fully-supported tenant-scoped restore target (Decision 5, Decision 16, FR-021), and until this pass, `auditLog` was an ordinary, un-excluded `TENANT_BACKUP_ENTITY_KEYS` member, meaning a restore *of `default`* would delete-and-reinsert `default`'s own `entityChunks` category — `auditLog` included — silently reverting the very centralized log this decision concentrates every tool's accountability events into, wiping out every tenant's audit trail system-wide, not only `default`'s own. **Fixed at the root, not patched here**: Decision 8's ninth-pass extension adds `auditLog` to the `TENANT_BACKUP_ENTITY_KEYS` exclusion filter, so no tenant-scoped restore — including one targeting `default` — ever touches any tenant's `auditLog`, closing this self-destruction hazard completely; see Decision 8 for the fix itself.

**Mechanics, corrected ninth pass**: `buildAuditEvent()` (`lib/auditLog.js`) derives its own `tenantId` field from `req.tenantId || user.tenantId || 'default'`, where `req = opts.req || {}` — but a CLI script has no real Express `req` object. **This decision was previously silent on this, which would have left every event's own `tenantId` silently defaulting to `'default'` regardless of the real target tenant, defeating this decision's own "self-describing" claim below.** Both tools MUST construct and pass a synthetic `req: {tenantId: <real target tenantId>}` as `opts.req` to `appendAuditEvent()`/`buildAuditEvent()` — this is the only way the event's own `tenantId` field genuinely reflects the tenant the operation concerned, even though the document it lives in is `default`'s. Minimum required `metadata` fields, unified ninth pass (previously stated inconsistently across this decision and its two implementing contracts): `backupFingerprint` and the backup's own `createdAt` — both available to `tenant-backup.js` (the fingerprint it just computed for its own `.sha256` sidecar) and to `tenant-restore.js` (already loaded and validated at Step 1) — plus, additionally, `recordCounts`/`categoryDigests` for `tenant.backup` and `categoriesApplied` for `tenant.restore`, each tool's own natural extra context beyond the shared minimum. **Mongo-mode write mechanics**: read the current `EntityChunk{tenantId:'default', key:'auditLog'}` document (or treat as `{data:[]}` if absent), call `appendAuditEvent({auditLog: doc.data || []}, opts)` (the exported builder, reused for its event-shape/sanitization guarantees — never a hand-rolled event object), then `EntityChunk.findOneAndUpdate({tenantId:'default',key:'auditLog'}, {$set:{data: <the mutated array>}}, {upsert:true})` — a direct, minimal read-modify-write using the same `EntityChunk` primitive this feature's own restore lock already uses, not the full `loadDB()`/`saveDB()` in-memory-cache machinery a short-lived CLI process has no reason to instantiate. **File mode**: the equivalent direct read-modify-write against `default`'s own `data/database.json`'s `auditLog` array.

**A second, distinct known residual risk, explicitly acknowledged, not silently dropped (ninth pass)**: this decision writes the audit event directly to Mongo, bypassing `loadDB()`/`saveDB()` entirely — but `default`'s own live application server process, if still running (the normal, expected case, since `default` is this codebase's primary production tenant), holds the identical stale-in-memory-cache hazard Decision 19 already documents in full for tenant data, applied here to `default`'s own `auditLog` specifically: `saveDB()`'s default-tenant branch marks *every* top-level key of the cached object dirty on every call (`lib/database.js:779`), not only the key an ordinary request actually changed, and `flushToMongo()` unconditionally re-persists every dirty key straight from that same, now-stale in-memory object. The very next ordinary write `default`'s live server accepts (a journal entry, a payroll run — anything) will silently overwrite this decision's own direct Mongo write to `auditLog` with the server's own stale, pre-event copy, erasing the audit trail entry this decision exists to guarantee. **This is not fixed by the `TENANT_BACKUP_ENTITY_KEYS` exclusion above** — that fix protects `auditLog` from a *restore's* Step 5 apply specifically; it does nothing about the live server's own unrelated, ordinary write path. Closing this properly would require either invalidating/re-warming `default`'s own live cache after every tenant-backup/restore invocation (new infrastructure this feature's own "no unnecessary new infrastructure" constraint, and Decision 19's own precedent, both argue against), or requiring a `default` server restart after every tenant operation (operationally disproportionate — `default` is the primary production tenant, and restarting it after every *other* tenant's routine backup would be unacceptable, unlike Decision 19's narrower "restart only the tenant just restored" requirement). **Disposition, matching Decision 19's own established pattern for a real but disproportionate-to-fully-close-in-this-phase gap**: this residual risk is documented, not silently dropped, and not mitigated by any new mechanism in this phase. An operator who needs the audit trail's own durability guaranteed with certainty for a specific operation can restart `default`'s server process afterward, exactly as Decision 19 already asks them to do for the target tenant — this is not mandated by this decision, only noted as available.

**A third known residual risk, explicitly acknowledged, not silently dropped (ninth pass)**: this read-modify-write is not atomic. Two concurrent invocations of either tool (e.g. an operator or orchestration script backing up two different tenants at once — `tenant-backup.js` acquires no lock at all, and the restore lock is scoped per-*target*-tenant, not per-`default`) can each read the same starting `auditLog` array, append their own event locally, and write back; whichever write lands last silently discards the other's event — a lost update. This is a real, acknowledged gap in FR-037's own "exactly one event per invocation" guarantee under concurrent use, analogous in spirit to (but distinct in mechanism from) the exact race class Decision 11's atomic lock exists to close for the restore lock itself. Closing it properly (e.g. via MongoDB's own atomic `$push` with `$slice` for the size cap, replacing the read-then-`$set` pattern) is recorded as a follow-on hardening candidate for a later phase, not silently dropped — the same disposition this document set already gives Decision 19's own deferred write-blocking mechanism, for the same reason: a real but narrow-window gap, disproportionate to fully close in this phase relative to this feature's core tenant-isolation/backup-safety scope.

**A fourth known residual risk, explicitly acknowledged, not silently dropped (twelfth pass — found by an open-ended sweep, out of scope to close since it lives entirely in existing, unmodified, out-of-feature source code)**: `outcome:'cancelled'` (eleventh pass, above) is a genuinely new value this decision introduces — before this pass, every `appendAuditEvent()` call site anywhere in this codebase wrote only `'success'`/`'failure'`. `public/index.html`'s own existing Audit Trail viewer — the very "admin/support user can actually read" consumer this decision's own opening paragraph cites as the reason events are written to `default`'s own `auditLog` in the first place — renders its outcome badge with a binary ternary (`outc === 'failure' ? ... 'فشل' ... : ... 'نجاح' ...`, both in its summary-table row and its per-event detail modal) and offers only two `<option>`s (`success`/`failure`) in its outcome filter. Under this existing, unmodified logic, a `'cancelled'` event — an operator's ordinary decline at Step 4a — would render identically to a genuine `'نجاح'` (Success) badge and would not be reachable via the outcome filter at all, undermining exactly the accountability distinction this pass's own widening exists to create: an admin reviewing the audit trail could not visually distinguish "the restore actually completed" from "the operator saw the prompt and declined." **Disposition, not silently dropped, and deliberately not closed in this phase**: `public/index.html` is pre-existing, unmodified UI, entirely outside this feature's own scope (this feature's standing boundary is backup/restore *tooling*, never the existing admin UI), and this design-remediation phase does not touch source code of any kind, this file included. A future phase adding UI support for the new outcome value — a third badge state and a third filter option — is recorded as a follow-on hardening candidate, not designed or scheduled here.

## Decision 24 — Tenant-scoped backup files get the same retention/rotation policy as whole-instance backups, per-tenant (NEW ninth pass, MEDIUM; matching logic corrected tenth pass, HIGH)

**The question, found by the ninth re-verification's open-ended sweep, the first genuinely new category examined since Pass 7 (language) and Pass 8 (constitutional compliance)**: `scripts/tenant-backup.js` writes a new, uniquely-timestamped `backups/tenant-<tenantId>-<stamp>.json` (+ `.sha256`) file on **every** invocation. No decision, contract, or task across nine passes ever specified a retention, rotation, or cleanup policy for these files — unlike the offline *staging* file (Decision 14, Pass 5/6), which is deliberately deleted on success specifically because it holds the same class of plaintext secrets (bcrypt password hashes in every `users` record, any `AppConfig.smtpPass`) the *backup* file itself also holds, indefinitely, with no lifecycle management at all. The existing whole-instance `scripts/backup.js` already rotates its own output (`BACKUP_KEEP`, default 30, `scripts/backup.js:92-98`) — but its rotation filter (`f.startsWith('backup-') && f.endsWith('.json')`) structurally never matches the new `tenant-<tenantId>-...json` naming pattern, so tenant-scoped backups are excluded from that existing mechanism too, not merely uncovered by a new one.

**Decision**: `scripts/tenant-backup.js` gets its own rotation step, run at the end of every successful invocation, mirroring `scripts/backup.js:91-98`'s own mechanism exactly but scoped **per target tenant** (so tenant A's backup count never affects tenant B's retention): `const TENANT_BACKUP_KEEP = Number(process.env.TENANT_BACKUP_KEEP || 30)`; list `backups/` for files matching this tool's own naming pattern; sort (the ISO-stamp naming already sorts chronologically, exactly like the whole-instance tool); delete every file beyond the newest `TENANT_BACKUP_KEEP` for the exact target tenant, plus its `.sha256` sidecar. **Eleventh-pass correction, MEDIUM**: the tenth-pass rewrite of this sentence, replacing the original prefix-matching filter description with a reference to "this tool's own naming pattern," silently dropped the sort step — restored here; the tenth-pass paragraph immediately below specifies *which* files qualify (exact tenantId match) but was never meant to also replace *this* paragraph's own chronological-ordering step, without which "the newest `TENANT_BACKUP_KEEP`" is undefined. This is not new infrastructure — it is the exact same, already-proven mechanism this codebase already runs for whole-instance backups, applied with a tenant-scoped filter and its own independent env-var override (defaulting to the same value, `30`, for consistency), never touching `scripts/backup.js` itself (this feature's standing boundary, Decision 7). A rotation failure (e.g. permissions) is logged as a warning, not escalated to a non-zero exit — the backup itself already succeeded, mirroring `scripts/backup.js:95`'s own `try {} catch {}` precedent.

**Tenth-pass correction, HIGH — the ninth-pass matching logic could delete a different tenant's own backups**: this decision originally specified matching by raw string prefix — `` `tenant-${tenantId}-` `` as a `startsWith()` prefix. That is unsafe: `_tenantFilePath()`'s own sanitization (`lib/database.js`) only replaces `[^a-zA-Z0-9_-]` with `_` — hyphens pass through untouched, and real tenant identifiers (`server.js:1252-1254`'s own generation, `<slug-derived-base>-<base36-timestamp>`) routinely contain them. For tenants `acme` and `acme-corp`, rotating `acme` (prefix `tenant-acme-`) would also match `tenant-acme-corp-<stamp>.json`, since that filename literally starts with `tenant-acme-` — silently deleting a different tenant's own disaster-recovery backup, the exact opposite of what this feature exists to prevent. **Corrected matching, delimiter-safe by construction, not by convention**: the backup-write stamp (`stamp()`, mirroring `scripts/backup.js`'s own helper) is always exactly 19 characters (`YYYY-MM-DDTHH-mm-ss`, from `new Date().toISOString().replace(/[:.]/g,'-').slice(0,19)`). For a candidate file matching `tenant-*.json`, strip the fixed `tenant-` prefix and `.json` suffix, then strip exactly the trailing 20 characters (the 19-character stamp plus its own separating hyphen) — the remainder is the file's own embedded tenantId, unambiguously, regardless of how many hyphens it itself contains. Compare this extracted value to the target `tenantId` with exact string equality (`===`), never `startsWith()` or any other partial match. This fixed-width extraction is robust by construction, not by convention — it never needs to guess where the tenantId ends and the timestamp begins, because the timestamp's own length is a known constant.

**Explicitly not addressed, not silently dropped**: encryption-at-rest for the backup file's own plaintext secrets is out of scope for this phase — the same secrets already exist, unencrypted, in the existing whole-instance backup file today, and this feature's own scope is tenant isolation and safe backup/restore *mechanics*, not a new at-rest-encryption capability for a pre-existing exposure this codebase has always had. Rotation bounds how long the exposure persists; it does not eliminate it.

## Decision 25 — `scripts/tenant-restore.js` MUST require an explicit, unweakenable typed-confirmation gate before Step 5's apply, per this project's own binding constitution (NEW ninth pass, CRITICAL)

**The question, found by the ninth re-verification's open-ended sweep — the second constitutional principle nine passes never checked**: this project's constitution (`.specify/memory/constitution.md`, Principle IV, "Data Preservation and Backups") states explicitly: "Restore is never a casual debugging mechanism — it requires explicit target verification and owner approval, matching the existing `--target=<name>` + typed-confirmation gate in `scripts/restore.js`. This gate MUST NOT be weakened, defaulted, or bypassed by a feature that finds it inconvenient." `scripts/restore.js`'s real, existing gate (`scripts/restore.js:104-107`) requires the operator to type the literal word `نعم` before any write, unless `--yes`/`RESTORE_YES=1` is passed. Across all nine passes and every decision in this document, `scripts/tenant-restore.js`'s `--yes` flag is *mentioned* (the Invocation section, `tasks.md` T040) but its own behavior — what happens in `--yes`'s absence, what the prompt says, what the operator must type — was never once specified, and no test anywhere in `tasks.md`'s Phase E/H or the restore contract's Test contract covers it. This is a textually unambiguous constitutional requirement, missed even by Pass 8's own explicit claim to have swept every constitutional principle for the first time.

**Decision**: `scripts/tenant-restore.js` MUST require the operator to type `نعم` before Step 5's apply begins — placed after Step 4 (checkpoint written) and before the first destructive write, the latest point that is still before any data is touched — unless `--yes` is passed or `RESTORE_YES=1` is set (mirroring `scripts/restore.js:104`'s own exact escape hatch, needed for this feature's own mandatory automated tests, e.g. T057/T058's failure-injection scenarios, which cannot pause for interactive input). The prompt MUST echo the target tenant identifier and the operator-supplied `--target=` label before asking for confirmation, exactly mirroring `scripts/restore.js:98-99`'s own target/source echo, so "I meant to restore into staging but pointed this at the wrong tenant" is visible before the typed confirmation, not after. This gate MUST NOT be defaulted, weakened, or silently bypassed by any other flag or environment condition — per the constitution's own explicit, unconditional text — and applies identically regardless of which tenant (including `default`) is the restore target.

**Why this was missed for nine passes**: Passes 1-6 focused on mechanics (isolation, staging, locking, digests); Pass 7 checked language convention; Pass 8 checked one constitutional principle (Auditability) but, by its own account, swept "every principle" — an overstatement this pass corrects by finding a second, textually explicit principle (IV) that sweep missed. The confirmation gate is easy to overlook precisely because `--yes`/`--target=` already *exist* as flags in the Invocation section from Pass 1 onward — their mere presence reads as "already handled," when in fact only their names, not their actual gating behavior, had ever been specified.

**Acknowledged residual risk, tenth pass — signal handling during the prompt is unspecified**: found by the tenth re-verification's open-ended sweep, a category no prior pass examined. Neither this decision nor the restore contract's own Step 4a section says what happens if the operator sends SIGINT (Ctrl-C) or SIGTERM while the `نعم` prompt is awaiting input — and `scripts/restore.js`'s own `ask()` helper (`scripts/restore.js:21-24` — **eleventh-pass citation correction**: previously cited as `:20-23`, one line short, omitting the function's own closing brace and instead starting on the blank line above it), which Step 4a's `readline` mechanics deliberately mirror, registers no signal handler either, so an interrupt there falls through to Node's own default behavior (immediate process exit) with the restore lock (Decision 11) still held and no cleanup run. Mirroring `scripts/restore.js` verbatim therefore inherits this exact gap rather than introducing a new one — consistent with this feature's own precedent of reusing proven mechanics rather than reinventing them (Decision 19, Decision 24). **Disposition, not silently dropped**: not architecturally solved in this pass. A stuck lock left by an interrupted confirmation prompt is already recoverable via the existing `--force-unlock` operator escape hatch (Decision 11), the same recovery path a crash at any other pre-apply step already requires — so the gap is bounded, not open-ended, even though it is not gracefully handled. A future pass MAY add an explicit `SIGINT`/`SIGTERM` handler that releases the lock and prints an Arabic cancellation message before exiting (mirroring Step 4a's own `'أُلغيت الاستعادة.'`-equivalent messaging); this pass records the gap rather than closes it, since closing it well requires touching `scripts/restore.js`'s own equivalent gap too for consistency, which is outside this feature's standing boundary of never modifying that file (Decision 7).

**Acknowledged residual risk, tenth pass — the quiesce + lock + confirmation-wait composition has no bounded window**: also found by the tenth re-verification's open-ended sweep. Decision 19's pre-restore quiesce warning, Decision 11's restore lock, and this decision's own confirmation prompt have each been specified and reasoned about individually, but never together as a composed sequence: Step -1 quiesces the target tenant's live traffic, Step 0 acquires the lock, and — new as of this pass — Step 4a now sits between the checkpoint (Step 4) and the apply (Step 5), meaning the already-quiesced, already-locked window can be extended by an arbitrary, human-response-dependent amount while the operator reads the prompt and types `نعم`. No document across all ten passes discusses how long a tenant may reasonably sit quiesced-and-locked-but-not-yet-restored, nor whether an operator who steps away mid-prompt should be timed out. **Disposition, not silently dropped**: not architecturally solved in this pass, for the same reason as the signal-handling gap above — `scripts/restore.js`'s own equivalent prompt (`scripts/restore.js:104-107`) has an identical unbounded wait with no precedent for a timeout to mirror, and inventing a novel timeout mechanism here (with its own new failure modes — what happens on timeout? auto-cancel? auto-proceed? neither is obviously safer) is a disproportionate scope expansion for a single re-verification round to resolve unilaterally. Operator guidance — do not leave a confirmation prompt unattended, and treat `--yes`/`RESTORE_YES=1` as the intended mechanism for any unattended/scripted invocation, which this feature already provides — is the practical mitigation until a future pass, if ever, decides a hard timeout is warranted.

**Explicitly out of scope, not silently dropped**: audit logging for a rejection at Step -1 (the quiesce warning) or Step 0 (the restore lock) is not required — those are pre-flight checks with no destructive action attempted yet, and Principle XI's own concern is "privileged functionality," not every early-exit code path. Retrofitting the existing, unmodified `scripts/backup.js`/`scripts/restore.js` with audit logging is explicitly out of scope for this feature, consistent with this feature's own established, repeated boundary of never touching those two files (research.md Decision 7, "Whole-Instance Separation").
