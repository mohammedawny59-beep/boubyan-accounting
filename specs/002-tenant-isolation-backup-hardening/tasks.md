---

description: "Task list for Tenant Isolation + Safe Backup/Restore Hardening (P4) — Design Remediation Pass 9"
---

# Tasks: Tenant Isolation + Safe Backup/Restore Hardening (P4)

**Input**: Design documents from `specs/002-tenant-isolation-backup-hardening/` (all revised through Design Remediation Pass 9)
**Tests**: Explicitly requested — TDD-ordered throughout.

**Revision note (Design Remediation Pass 9)**: A ninth `/speckit-analyze` re-verified Pass 8 and found 19 further defects (2 CRITICAL, 9 HIGH, 8 MEDIUM) — the largest count of the process so far, and the first round to find two CRITICAL gaps at once. First: Decision 23's own new audit-log write (Pass 8) was itself destroyed by an ordinary restore of `default`, since `auditLog` was never excluded from `TENANT_BACKUP_ENTITY_KEYS` — the identical self-destruction hazard Decision 23 exists to prevent, one level up, with a system-wide blast radius (every tenant's audit trail, not only `default`'s). Closed by extending research.md Decision 8's exclusion filter. Second: this project's own constitution (Principle IV) requires an explicit, unweakenable typed-confirmation gate before any destructive restore, matching `scripts/restore.js`'s own existing pattern — never specified for `scripts/tenant-restore.js` across nine passes, an omission Pass 8's own "swept every constitutional principle" claim itself missed. Closed by new research.md Decision 25, spec.md FR-039, a new restore contract Step 4a, and new task T053a. The remaining findings closed: a related HIGH gap the CRITICAL fix does not itself close — `default`'s own live server can still silently overwrite the audit event via its own stale-cache mechanism (Decision 19's own mechanism, applied here), acknowledged as a residual risk rather than mandating a disruptive `default` restart after every tenant operation; a newly-discovered backup-file retention gap, closed by new research.md Decision 24, spec.md FR-038, and new task T035a; an incomplete restore-side audit scope (Step 1/Step 2 rejections were never audited) and a Step-5-vs-Step-6 location contradiction, both corrected in T056d and the restore contract; a missing synthetic-request-context instruction for both T034a and T056d, without which every audit event's own tenant identity would silently default to `default`; a factual misattribution of what this project's constitution records as technical debt; disagreeing audit-event metadata fields, now unified; a stale `plan.md` tests/ manifest; a missing `plan.md` Complexity Tracking row; a self-contradictory "14 raw vs. 11 deduplicated" accounting error in this document's own Pass-8 revision note (corrected below — Pass 8 closed 11 unique findings: 7 HIGH, 4 MEDIUM); and a pre-existing (since Pass 1) factual inaccuracy in research.md Decision 7. Task count is now 85 (T001-T075 plus T031a, T034a, T035a, T040a, T053a, T056a, T056b, T056c, T056d) — T035a and T053a are the only new task IDs this pass.

**Revision note (Design Remediation Pass 8)**: An eighth `/speckit-analyze` re-verified Pass 7 and found 11 unique defects (0 CRITICAL, 7 HIGH, 4 MEDIUM — 14 raw findings before deduplicating overlapping findings from independent review dimensions, corrected ninth pass; this note previously stated "7 HIGH, 7 MEDIUM" summing to 14, an arithmetic error) — the largest raw count of any round to that point, though the two most significant findings closed genuinely new gaps rather than merely re-litigating Pass 7's own new material. First: Decision 21's own seventh-pass file-mode fallback (treating a missing per-tenant file as "tenant does not exist") was itself unsafe — `loadDB()` creates a non-default tenant's file lazily, so a real, active, freshly-onboarded tenant with no durable write yet would have been wrongly rejected, reproducing one layer down the exact failure FR-035 was written to prevent. Withdrawn; file mode no longer enforces FR-007 in this phase (T031a extended, new T026 case 11). Second, and larger in scope: an open-ended sweep checked this feature's own new capability against every principle in this project's binding constitution for the first time in eight passes, and found Principle XI (Auditability) had never been examined — `scripts/tenant-restore.js`/`scripts/tenant-backup.js` are new, privileged, destructive functionality with no audit-logging requirement and no documented spec exception, while `plan.md`'s Constitution Check table had certified "PASS | Unchanged" for this row in every prior pass without analysis. Closed via new research.md Decision 23, spec.md FR-037, and new tasks T034a (backup) and T056d (restore): both tools now call the canonical `appendAuditEvent()` exactly once per terminal outcome, writing to `default`'s own audit log rather than the target tenant's (which a restore's own apply step could otherwise destroy). The remaining findings were stale cross-references and misattributions left behind by Pass 7's own sweep not fully propagating: a reverted "Tenant/Subscription never queried at all" claim in data-model.md that Decision 21 itself had just made false; two remaining English-language example messages (one in research.md, two verification steps in quickstart.md) that Pass 7's Arabic sweep missed despite explicitly claiming completeness; a wording divergence between two documents quoting what is meant to be the same Arabic string; and four separate misattribution/staleness errors in plan.md's own manifest and consistency-review sections, several of them the same error class Pass 7 had just finished correcting elsewhere in the same document. Task count was 82 (T001-T075 plus T031a, T034a, T040a, T056a, T056b, T056c, T056d) at the end of this pass — T034a and T056d were the only new task IDs.

**Revision note (Design Remediation Pass 7)**: A seventh `/speckit-analyze` re-verified Pass 6 and found 1 CRITICAL, 2 HIGH, 6 MEDIUM further defects, via an open-ended sweep rather than a check against a predetermined list. The CRITICAL finding: spec.md's own original FR-007 (a nonexistent-or-inactive target tenant must fail a tenant-scoped backup, present since before any remediation pass began) was never implemented by any decision, contract, or task across six passes, and was in fact foreclosed by research.md Decision 10's "never queried" language for `Tenant`/`Subscription` — closed by new research.md Decision 21 (a narrow, deliberate exception permitting exactly one `Tenant.findOne().lean()` existence/status read, mirroring `lib/tenantMiddleware.js:72-73`'s own established pattern) and new task T031a, with T026 extended for its test coverage. A MEDIUM finding (elevated for its breadth): every illustrative operator-facing message this feature drafted across six passes was in English, contradicting the already-shipped `scripts/restore.js`'s and this feature's own Telegram-boundary's established Arabic convention — closed by new research.md Decision 22, with every literal quoted example message in this file, research.md, and the restore contract corrected to Arabic. Two HIGH findings closed gaps in plan.md's own Pass-6 fixes: the Source Code manifest's `docs/PRODUCTION_RUNBOOK.md` entry misattributed a Pass-6-only requirement (naming both `sourceId` and `journalId`) to Pass 4, and the entire Documentation manifest section (research.md/data-model.md/quickstart.md/the restore contract/spec.md) was left describing only through-Pass-5 content despite every one of those five files being substantively edited in Pass 6 — both corrected directly in plan.md, no task-level change needed. The remaining MEDIUM findings closed stale cross-references Pass 6's own fixes left behind: T057's failure-injection mechanism is now mandatory (not an optional alternative to a documented test-only hook), so T058's cross-reference to "the same class of hook T057 uses" is finally guaranteed to have something to point at; the Dependencies & Execution Order summary and a Plan/Task Consistency Review bullet both still described the pre-Pass-5 lock-acquisition order or pre-Pass-6 idempotency-collection naming. Task count is now 80 (T001-T075 plus T031a, T040a, T056a, T056b, T056c) — T031a is the only new task ID this pass; all other fixes extend existing tasks in place.

**Revision note (Design Remediation Pass 6)**: A sixth `/speckit-analyze` re-verified Pass 5 and found 0 CRITICAL, 1 HIGH, 7 MEDIUM further defects — all specification-level corrections; no new product decision or FR was needed. The HIGH finding: `plan.md`'s Source Code manifest entries for `scripts/tenant-restore.js` and `docs/PRODUCTION_RUNBOOK.md` were never updated past Pass 4 content, despite both files' own tasks (T040a, T056b, T056c, and T064's four Pass-5 additions) being substantially extended in Pass 5 — corrected directly in plan.md, no task-level change needed. Two MEDIUM findings closed a representation ambiguity and a wrong rationale, both introduced by Pass 5's own new requirements: the idempotency-staleness count (T056b) never specified which of two genuinely divergent physical representations of a tenant's idempotency data it queries in Mongo mode (now specified: the dedicated `IdempotencyRecord` collection, not the `EntityChunk`-embedded array); and the staging-file-preservation-after-failure rationale (T056c, T064, quickstart.md step 6c, the restore contract, data-model.md) incorrectly claimed a `--force-unlock` resume "reads" the preserved file, when staging in fact reruns unconditionally on every invocation and would simply overwrite it fresh — corrected to the actual rationale (a forensic artifact for the operator) everywhere it appeared. A further MEDIUM closed a citation-completeness gap in research.md's own header line — the very sentence Pass 5 rewrote to fix one such error contained a second, different one (Decisions 13/14 wrongly called "unchanged," Decisions 7/11 omitted entirely) — resolved by de-itemizing that line rather than attempting a third, likely-also-imperfect summary. Two more MEDIUM findings closed stale cross-decision references: Decision 11's lock-acquisition-order claim was left stale after Decision 19 introduced the Step -1 warning ahead of it (research.md only — no task-level change), and Decision 8's "never queried by backup/restore at all" claim was left stale after Decision 20 introduced exactly such a query (research.md only). The final MEDIUM specifies a deterministic kill-point mechanism for T058's mandatory crash-window test, mirroring the documented test-only hook T057 already uses, closing a flakiness gap the test previously had no way to avoid. Three LOW findings were also closed opportunistically (not gate-blocking, but cheap to fix while already editing these exact sections): Decision 20 now states its own zero-count-print requirement directly rather than only in three downstream documents; T040a now requires a content assertion on the quiesce warning, mirroring T056a's equivalent for the restart message; and spec.md gains SC-010/SC-011 for FR-033/FR-034. Task count remains 79 (T001-T075 plus T040a, T056a, T056b, T056c) — no renumbering, no new task IDs; all Pass 6 fixes extend existing tasks in place.

**Revision note (Design Remediation Pass 5)**: A fifth `/speckit-analyze` re-verified Pass 4 and found 1 CRITICAL, 5 HIGH, 3 MEDIUM further defects. The headline finding: Pass 4's live-server-cache fix only closed the *after*-restore window — the same in-memory cache can also race and corrupt a restore *while it is running*, requiring a genuinely new pre-restore quiesce warning (T040a) in addition to, not instead of, the existing post-restore restart message (T056). Two further HIGH findings extend rather than replace Pass 4 decisions: the idempotency-staleness acknowledgment gains a restore-time-computable count printed in Step 6's own output (T056b), and the offline staging file is now deleted automatically on a successful restore since it carries the same plaintext-secret exposure as the backup file itself (T056c). A MEDIUM finding closed a documentation gap: three downstream documents required `default`'s live-digest re-check (not just its delete) to use `_defaultTenantFilter` and attributed this to research.md Decision 12, but Decision 12 itself never stated it (research.md fixed directly; T067's own citation was already correct and needed no task-level change). The runbook (T064) is extended again for all three operator-facing additions, and a pre-existing (not introduced by this feature) factual bug in `docs/PRODUCTION_RUNBOOK.md` §5.1 — a false claim that no live scheduled job exists in this repository, when `startBackupSchedule()` (`server.js:9942`) demonstrably runs one automatically in production — is corrected in the same task. Task count was 79 (T001-T075 plus T040a, T056a, T056b, T056c) at the end of this pass.

**Revision note (Design Remediation Pass 4)**: A fourth `/speckit-analyze` re-verified Pass 3 and found 1 CRITICAL, 6 HIGH, 4 MEDIUM further defects. The headline finding, previously unconsidered across all three prior passes: a completed tenant restore is invisible to the target tenant's own live application server process, whose in-memory per-tenant cache is never invalidated by an out-of-process Mongo write — closed by requiring the tool's own success output (T056, extended) to instruct an immediate server restart, with a new dedicated test (T056a) and a runbook update (T064, extended). A second acknowledged-but-previously-unconsidered risk: idempotency-claim survival across a restore, now documented (T064, extended) rather than silently unaddressed. Also fixed: a corrected shutdown-flush design for `_tenantConfigFileTimers` (T015 — Pass 3's version did not actually match the real sibling code it claimed to mirror), and several stale decision-number cross-references left by Pass 3 itself, including two citation "corrections" Pass 3 got wrong. Task count was 76 (T001-T075 plus T056a) at the end of this pass.

**Revision note (Design Remediation Pass 3)**: A third `/speckit-analyze` re-verified Pass 2 and found 3 CRITICAL, 2 HIGH, 4 MEDIUM further defects — a missing `clearTimeout` step for the new `_tenantConfigFileTimers` map (T008), a `categoryDigests` computed over structurally inconsistent `_id`-bearing vs. `_id`-stripped representations at different pipeline points (T029/T034/T048/T055 — closed via a new shared `computeCategoryDigest()` helper), a stale `<runId>.json` staging-path reference left in research.md Decision 6 after Decision 15 corrected it everywhere else, and several stale decision-number/task-ID cross-references. Task count was 75 (T001-T075) at the end of this pass.

**Revision note (Design Remediation Pass 2)**: A second adversarial `/speckit-analyze` re-verified Pass 1's own fixes (rather than assuming they worked) and found 5 CRITICAL, 6 HIGH, 2 MEDIUM new or incompletely-closed defects in Pass 1's design — a dropped default-tenant `AppConfig` scoping fix, an unscoped `entityChunks` delete threatening the restore lock and the tenant's own idempotency store, a `runId`-keyed checkpoint with no tenant-lookup path, an internally-contradictory stale-lock policy, a non-atomic force-unlock sequence, an underspecified digest canonicalization, and a file-mode transform that could insert schema-invalid records. Renumbered sequentially T001-T075 (up from 72). **Phase A's task content remains completely unchanged** — it has now survived two rounds of adversarial analysis intact.

## Format: `[ID] [P?] [Phase] Description`

## Task Dependency Rule (unchanged)

`isolation (A) → config (B) → default-only boundaries (C) → backup (D) → restore validation + lock (E) → offline staging (F) → checkpoint/apply (G) → recovery + runbook (H) → default compatibility (I) → separation/regression (J, K)`

---

## Phase 1: Setup

- [ ] T001 Run the existing full test suite once, unmodified, as a pre-implementation baseline (`npm test`); record the pass count for comparison against T075. No dependencies.

---

## Phase A: Tenant Write Isolation — UNCHANGED CONTENT (survived two `/speckit-analyze` rounds)

- [ ] T002 [P] [A] Write failing Mongo-backed tests in `tests/tenant-write-isolation.test.js`: default save does not delete a Tenant B user sharing the same `id`; default upsert does not overwrite a Tenant B user sharing the same `id`; default entity save does not overwrite a Tenant B `EntityChunk` sharing the same `key`; a legacy default `User`/`EntityChunk` document with no `tenantId` field (raw-driver-seeded per spec.md FR-027, with an explicit read-back assertion that the field is genuinely absent before exercising the behavior under test) is updated in place, not duplicated; no duplicate default document is created for an existing legacy identity. Depends on: T001.
- [ ] T003 [A] `persistUsers()` (`lib/database.js:424`): filter → `{..._defaultTenantFilter, id:{$nin:ids}}` / `_defaultTenantFilter`; upsert match filter → `{..._defaultTenantFilter, id:u.id}`. `$set` unchanged in this task. Depends on: T002.
- [ ] T004 [A] `persistEntityKey()` (`lib/database.js:445`): filter → `{..._defaultTenantFilter, key}`. Depends on: T002.
- [ ] T005 [A] Normalize-forward: add `tenantId:'default'` to both functions' `$set`. Depends on: T003, T004.
- [ ] T006 [A] Run `tests/tenant-write-isolation.test.js`; confirm all pass. **Checkpoint.** Depends on: T005.

---

## Phase B: Config Isolation (research.md Decision 2 — corrected in Pass 2 and Pass 4; not Decision 3, which is the unrelated Telegram/scheduler boundary — see Phase C)

- [ ] T007 [P] [B] Write failing tests in `tests/tenant-config-isolation.test.js`: (1) Tenant A `saveConfig()` never visible to Tenant B `loadConfig()`; (2) fee/card/KNET-shaped settings stay isolated; (3) SMTP/branding stay isolated; (4) a legacy no-`tenantId`-field `AppConfig` document (raw-driver-seeded, FR-027, absence asserted before use) normalizes in place, never duplicates; (5) cold-miss fail-closed: `loadConfig()` for a non-default tenant whose config was never warmed throws, never returns `_defaultConfig` or another tenant's value; (6) file-fallback persistence: under `DB_FILE_ONLY=true`, `saveConfig()` for a tenant survives a cache-clear + re-warm, zero Mongo connection attempted; (7) **default-tenant AppConfig collision (NEW, Pass 2)**: seed a real `AppConfig` document for a non-default tenant with `key:'config'`, then trigger `default`'s own `initConfig()`/`flushToMongo()` read/write path; assert `default`'s operations only ever touch `default`'s own document; (8) **config-only Mongo-mode flush (NEW, Pass 2)**: as a non-default tenant in Mongo mode, call `saveConfig()` alone (no `saveDB()` in the same debounce window); force the timer; assert the `AppConfig` document was actually written; (9) **config-only file-mode shutdown flush (NEW, Pass 2)**: as a non-default tenant under `DB_FILE_ONLY=true`, call `saveConfig()` alone, then invoke `shutdownDB()` before the debounce timer would fire naturally; assert the config file was written before shutdown completed; (10) **pending config-file timer does not survive a backend switch (NEW, Pass 3)**: as a non-default tenant under `DB_FILE_ONLY=true`, call `saveConfig()` alone (scheduling a pending `_tenantConfigFileTimers` entry, timer NOT yet fired), then call `initDB()` again against a fresh, empty backend; assert the original timer never fires against the new backend's state (e.g. via a spy on `_atomicWriteJsonSync`/`_tenantConfigFilePath` asserting no stale write occurs) — this is the test the missing `clearTimeout` step (T008) would otherwise ship without. Depends on: T006.
- [ ] T008 [B] Add `_tenantConfigCaches = new Map()`, `_tenantConfigDirty = new Set()`, and `_tenantConfigFileTimers = new Map()` (`lib/database.js`, alongside `_tenantCaches`/`_tenantDirty`/`_tenantFileTimers`), **and in the same task** wire all three into the existing P0.11 cross-backend reset block (`:945-951`) — declaring the reset-block registration together with the declaration itself, specifically so it cannot be dropped again as it was for the first two Maps during Pass 1's own Phase-B rewrite. **Third-pass correction: `_tenantConfigFileTimers` is a `Map<tenantId,Timer>` of live handles, exactly like the existing `_tenantTimers`/`_tenantFileTimers` — it needs `for (const t of _tenantConfigFileTimers.values()) clearTimeout(t);` BEFORE `_tenantConfigFileTimers.clear()`, mirroring the two clearTimeout loops the reset block already runs for its two sibling timer maps. A bare `.clear()` (correct for the other two, plain-data maps) is NOT sufficient here — it would leave a pending debounced config-file write scheduled against the pre-switch backend's state.** File: `lib/database.js`. Depends on: T007.
- [ ] T009 [B] Add `_tenantConfigFilePath(tid)` (mirrors `_tenantFilePath(tid)`'s sanitization, sibling path `data/tenants/<tid>.config.json`). File: `lib/database.js`. Depends on: T008.
- [ ] T010 [B] Implement `async function warmTenantConfigCache(tenantId)`: no-op for `default` or already-cached; Mongo mode reads `AppConfig.findOne({tenantId,key:'config'}).lean()`; file mode reads `_tenantConfigFilePath(tid)` via `fs.readJsonSync` if present, else defaults; merges over `_defaultConfig`; caches. File: `lib/database.js`. Depends on: T009.
- [ ] T011 [B] Wire `await warmTenantConfigCache(tenantId);` into `lib/tenantMiddleware.js:94`, alongside the existing `await warmTenantCache(tenantId);`, both before `next()`. File: `lib/tenantMiddleware.js`. Depends on: T010.
- [ ] T012 [B] Implement `loadConfig()`'s per-tenant branch (`lib/database.js:801`): `default` unchanged; non-default returns `_tenantConfigCaches.get(tid)` if present, else **throws**. Function signature and every existing call site unchanged (spec.md FR-022). File: `lib/database.js`. Depends on: T011.
- [ ] T013 [B] Implement `saveConfig(cfg)`'s per-tenant branch (`lib/database.js:807`): `default` unchanged; non-default sets `_tenantConfigCaches`, and either marks `_tenantConfigDirty` + calls `_scheduleTenantPersist(tid)` (Mongo mode) or schedules a debounced write to `_tenantConfigFilePath(tid)` registered into `_tenantConfigFileTimers` (file mode). File: `lib/database.js`. Depends on: T012.
- [ ] T014 [B] Extend `_flushTenantToMongo(tenantId)` (`:38`) with the config-flush branch, **and correct its early-return guard in the same task (Pass 2 fix)**: change `if (!db || dirty.size === 0) return;` to `if ((!db || dirty.size === 0) && !_tenantConfigDirty.has(tenantId)) return;` — without this, a tenant whose only pending Mongo-mode change is `saveConfig()` (no `saveDB()` in the same window) would have the guard return before the new branch ever runs. If dirty, `AppConfig.updateOne({tenantId,key:'config'},{$set:{tenantId,data:_tenantConfigCaches.get(tenantId),updatedAt:new Date()}},{upsert:true})`, then clear the flag. File: `lib/database.js`. Depends on: T013.
- [ ] T015 [B] **NEW, Pass 2, corrected Pass 4**: extend `_flushAllTenantsOnShutdown()` (`:823-853`) to **unconditionally** write every entry in `_tenantConfigCaches.keys()` via `_atomicWriteJsonSync(_tenantConfigFilePath(tid), _tenantConfigCaches.get(tid))` before shutdown completes — exactly paralleling how the REAL current `:831-841` unconditionally flushes every `_tenantCaches` entry for entity data. **Do not** iterate `_tenantConfigFileTimers` to decide what to flush (the original Pass 2 instruction, wrong): a tenant mid-retry after a transient write failure has no entry in that timer map at all (its retry `setTimeout` handle is never stored back into it, mirroring the identical gap in the existing entity-data retry path, `:703-709`), so a "pending timers only" flush would miss exactly the tenant most at risk of data loss. `_tenantConfigFileTimers` is used only to cancel pending timers first. File: `lib/database.js`. Depends on: T014.
- [ ] T016 [B] **NEW, Pass 2 — restores a fix Pass 1 incorrectly dropped**: scope `default`'s own three existing `AppConfig` call sites to `_defaultTenantFilter` + normalize-forward, exactly like `persistUsers()`/`persistEntityKey()`: `initConfig()`'s read (`:612`, `AppConfig.findOne({key:'config'})` → `AppConfig.findOne({..._defaultTenantFilter,key:'config'})`), both of `initConfig()`'s legacy-migration writes (`:621-625`, `:633-637`), and `flushToMongo()`'s default-tenant write (`:490-494`) — each write's `$set` gains `tenantId:'default'`. Without this, once any non-default tenant has its own `AppConfig` document (permitted by the compound unique index), these three unscoped-by-`{key:'config'}` call sites could read or overwrite the wrong tenant's document. File: `lib/database.js`. Depends on: T015.
- [ ] T017 [B] Run `tests/tenant-config-isolation.test.js`; confirm all 10 cases pass. **Checkpoint.** Depends on: T016.

---

## Phase C: Telegram / Monitor Default-Only Boundaries — FIVE routes (research.md Decision 3; unchanged from Pass 1)

- [ ] T018 [P] [C] Write failing `supertest` tests in `tests/telegram-scheduler-boundary.test.js` for all FIVE routes (`POST /api/telegram/start` :2678, `POST /api/monitor/inventory` :9822, `POST /api/monitor/monthly-report` :9826, `POST /api/reports/send-telegram` :15987, `GET /api/monitor/status` :9830): default-tenant unchanged behavior; non-default gets `403`+`TELEGRAM_DEFAULT_TENANT_ONLY`; no side effect before rejection (status route: response body discloses no `botActive`/`chatIdSet`). 5×3 = 15 cases. Depends on: T017.
- [ ] T019 [P] [C] Guard `POST /api/telegram/start`. Depends on: T018.
- [ ] T020 [P] [C] Guard `POST /api/monitor/inventory`. Depends on: T018.
- [ ] T021 [P] [C] Guard `POST /api/monitor/monthly-report`. Depends on: T018.
- [ ] T022 [P] [C] Guard `POST /api/reports/send-telegram`. Depends on: T018.
- [ ] T023 [P] [C] Guard `GET /api/monitor/status`. Depends on: T018.
- [ ] T024 [C] Re-run the full-file grep for `bot`/`global._tgBot`/`TelegramBot`/route paths containing `telegram`/`monitor` immediately before considering this phase done, to catch any route added since. Depends on: T023.
- [ ] T025 [C] Run `tests/telegram-scheduler-boundary.test.js`; confirm all 15 cases pass. **Checkpoint.** Depends on: T019, T020, T021, T022, T023, T024.

---

## Phase D: Tenant-Scoped Backup (research.md Decisions 8, 9, 10, 16 — corrected in Pass 2)

- [ ] T026 [P] [D] Write failing tests in `tests/tenant-backup.test.js`: (1) cross-tenant leakage; (2) legacy inclusion (raw-driver-seeded, absence asserted); (3) missing `--tenant=` fails, writes nothing; (4) a real, active non-default tenant with a genuine `Tenant`/`Subscription` row still backs up successfully, with neither row's data appearing in the output; (5) idempotency exclusion: a real `IdempotencyRecord` doc and an `EntityChunk` keyed `idempotencyRecords` appear in neither the output nor anywhere else; (6) default-duplicate pre-flight: a genuine duplicate `default` identity (raw-driver-seeded) causes hard failure naming the exact identity, zero file written; (7) no `tenants`/`subscriptions` key, for both `default` and a real tenant that has such rows; (8) **file-mode undefined-key handling (NEW, Pass 2)**: under `DB_FILE_ONLY=true`, back up an ordinary tenant that never triggered a password reset or logged error; assert `collections.entityChunks` contains no `passwordResets`/`errorLog` entry at all (not a `data:undefined` entry); (9) **`__restoreLock__` exclusion (NEW, Pass 2)**: seed an `EntityChunk` keyed `__restoreLock__` for the target tenant; back it up; assert it does not appear anywhere in the output; (10) **non-default existence/active-status pre-check, Mongo mode (NEW, seventh pass, MANDATORY — research.md Decision 21, spec.md FR-007)**: a `--tenant=` with no matching `Tenant` document hard-fails before any file is written, with an Arabic error naming the tenant identifier; a `Tenant` document with `status:'suspended'` or `status:'cancelled'` hard-fails identically; `--tenant=default` succeeds regardless of whether any `Tenant` row exists, since this check is unconditionally skipped for it (Decision 5); (11) **file-mode existence check is deliberately not enforced (NEW, eighth pass, MANDATORY — closes a real defect in the seventh-pass file-mode design)**: under `DB_FILE_ONLY=true`, back up a non-`default` tenant identifier with no file at `_tenantFilePath(tenantId)`; assert the backup succeeds with zero records, not a hard failure; (12) **audit event (NEW eighth pass, MANDATORY; event `tenantId` assertion added ninth pass — research.md Decision 23, spec.md FR-037)**: a successful backup appends exactly one event to `default`'s own `auditLog` (`action:'tenant.backup'`, `resourceId`=target tenantId, `outcome:'success'`, **and the event's own `tenantId` field equal to the real target tenant, not `'default'` — confirming the synthetic-`req:{tenantId:target}` construction is actually wired in**) and none to the target's own `auditLog` if the target is non-`default`; a hard-failed backup (existence/active-status pre-flight) appends exactly one `outcome:'failure'` event instead; (13) **`auditLog` exclusion (NEW, ninth pass — research.md Decision 8's ninth-pass extension)**: seed an `EntityChunk` keyed `auditLog` for the target tenant (real audit history); back it up; assert it does not appear anywhere in the output, mirroring case 9's `__restoreLock__` case; (14) **backup-file retention (NEW, ninth pass, MANDATORY — research.md Decision 24, spec.md FR-038)**: with `TENANT_BACKUP_KEEP=3`, back up the same tenant 5 times; assert exactly 3 `tenant-<tenantId>-*.json` files (+ `.sha256` sidecars) remain, the 3 newest; assert a different tenant's own backup files and any whole-instance `backup-*.json` file are unaffected. Depends on: T025.
- [ ] T027 [D] Export `_tenantFilePath(tid)` and `_tenantConfigFilePath(tid)` from `lib/database.js`'s `module.exports`. Depends on: T026.
- [ ] T028 [D] Add and export `TENANT_BACKUP_ENTITY_KEYS = ENTITY_KEYS.filter(k => k !== 'idempotencyRecords' && k !== 'auditLog')` from `lib/database.js` (**`auditLog` exclusion added ninth pass, CRITICAL — research.md Decision 8's ninth-pass extension, closing a self-destruction hazard Decision 23 introduced**). **Pass 3 addition**: add a dedicated unit test asserting `!ENTITY_KEYS.includes('__restoreLock__')` and `!TENANT_BACKUP_ENTITY_KEYS.includes('__restoreLock__')` directly against the exported constants — the lock's whole exclusion-from-backup safety property (research.md Decision 11) depends on this string never becoming a genuine, hand-added `ENTITY_KEYS` member in a later milestone; this guards it at the source, independent of any backup/restore fixture. **Ninth-pass addition**: add the identical assertion for `'auditLog'` — `TENANT_BACKUP_ENTITY_KEYS.includes('auditLog')` MUST be `false`, guarded at the source for the same reason. File: `lib/database.js`. Depends on: T026.
- [ ] T029 [D] **NEW, Pass 2, extended Pass 3**: implement and export `canonicalJson(value)` from `lib/backupValidation.js`, alongside `computeChecksum()`: recursively sorts object keys at every depth, and — for a value that is an array of records — first sorts the array by each record's stable identity field (`id` for user-shaped records, `key` for entity-chunk/config-shaped records) before the key-sorting pass. **Third-pass addition, CRITICAL**: also implement and export `computeCategoryDigest(records) = computeChecksum(canonicalJson(records.map(stripMongoMeta)))`, where `stripMongoMeta` strips `_id`/`__v` from each record before canonicalizing — this MUST be the single, only function used to compute a `categoryDigests.<cat>` value anywhere in the codebase (backup-write time T034, restore-staging recompute T048, restore-apply live re-check T055), never an inline `computeChecksum(canonicalJson(...))` call without the strip step. Without this, a Mongo-sourced backup's own digest (hashed over `_id`-bearing `.lean()` results) can never match restore's own recompute (hashed over `_id`-stripped staged/re-queried records) — every real restore would fail its own integrity check and the digest-resume mechanism could never trigger. Add a dedicated unit test asserting the same logical input always produces byte-identical output regardless of key-insertion order or array element order, **and** a second test asserting `computeCategoryDigest()` of a record set is identical whether or not those records carry a Mongo `_id`/`__v` field. File: `lib/backupValidation.js`. Depends on: T026.
- [ ] T030 [D] Create `scripts/tenant-backup.js`: argument parsing — `--tenant=` required, no default, no positional fallback. File: `scripts/tenant-backup.js` (new). Depends on: T026.
- [ ] T031 [D] Implement the default-tenant duplicate pre-flight (research.md Decision 16) as the first substantive step for `--tenant=default` — scan every `_defaultTenantFilter`-matched logical identity for more than one physical document; hard-fail naming the exact identity + both `_id`s; write no file. File: `scripts/tenant-backup.js`. Depends on: T030.
- [ ] T031a [D] **[MANDATORY, NEW seventh pass, CRITICAL — research.md Decision 21, spec.md FR-007; file-mode scope corrected eighth pass]** Implement the non-default tenant existence/active-status pre-check (T026's test case 10), as the first substantive step for any `--tenant=` other than `default`, and **only in Mongo mode** (`MONGO_URI` set): `const tenant = await Tenant.findOne({tenantId}).lean(); if (!tenant || tenant.status !== 'active') { <hard-fail, write no file, exit non-zero> }` — reusing `lib/tenantMiddleware.js:72-73`'s existing pattern verbatim. **In file mode, this check does not run at all — eighth-pass correction**: the original seventh-pass design checked whether `_tenantFilePath(tenantId)` exists on disk, treating a missing file as "does not exist," but a real, active, freshly-onboarded tenant that has never yet triggered a durable write also has no file on disk (`loadDB()`'s non-default branch creates it lazily, `lib/database.js:657-689`) — that heuristic would have wrongly rejected a legitimate empty tenant, the exact failure mode FR-035 exists to rule out one layer up. A file-mode backup for a non-`default` tenant now proceeds unconditionally regardless of whether a file exists. The error message MUST be Arabic (research.md Decision 22), e.g. `❌ المستأجر "<tenantId>" غير موجود أو غير نشط — رُفض النسخ الاحتياطي، لم يُكتب أي ملف.`. File: `scripts/tenant-backup.js`. Depends on: T030.
- [ ] T032 [D] Implement Mongo-mode queries using `TENANT_BACKUP_ENTITY_KEYS` (T028) — never raw `ENTITY_KEYS` — for exactly `users`, `entityChunks` (`key:{$in:TENANT_BACKUP_ENTITY_KEYS}`), `appConfigs`. `IdempotencyRecord`, `Subscription` never queried; `Tenant` is queried exactly once, by T031a's existence/active-status pre-check only (research.md Decision 21, seventh-pass correction — the payload itself still never includes any `Tenant`/`Subscription` data). File: `scripts/tenant-backup.js`. Depends on: T028, T031, T031a.
- [ ] T033 [P] [D] Implement file-mode backup + the unified-shape transform, **corrected Pass 2, tightened Pass 3**: `collections.users=blob.users`; `collections.appConfigs` from the tenant's config file (T027) if present, else `[]`; `collections.entityChunks = TENANT_BACKUP_ENTITY_KEYS.filter(key => blob[key] != null).map(key => ({tenantId,key,data:blob[key],updatedAt:backupCreatedAt}))` — **filtering out keys absent-or-null in the blob using loose `!= null` (catches both `undefined` and an explicit `null`, matching Mongoose's own `required:true` rejection of both — Pass 1 mapped every key unconditionally, and Pass 2's `!== undefined` fix alone would still have let an explicit `null` value through to the same schema-validation failure)**. File: `scripts/tenant-backup.js`. Depends on: T027, T028.
- [ ] T034 [D] Implement output format: `{scope:'tenant',schemaVersion:1,tenantId,createdAt,source,recordCounts:{users,entityChunks,appConfigs},categoryDigests:{users,entityChunks,appConfigs},collections}` + checksum sidecar (existing `computeChecksum()`, unchanged). `categoryDigests.<cat> = computeCategoryDigest(collections.<cat>)` (T029) — **Pass 3: must use `computeCategoryDigest()`, not a raw `computeChecksum(canonicalJson(...))` call**, so `_id`/`__v` are stripped before hashing even though `collections.<cat>` itself keeps `_id` on Mongo-sourced records (unchanged, matching `scripts/backup.js`'s existing precedent). File: `scripts/tenant-backup.js`. Depends on: T029, T032, T033.
- [ ] T034a [D] **[MANDATORY, NEW eighth pass, HIGH; synthetic-request-context and metadata fixed ninth pass — research.md Decision 23, spec.md FR-037, per this project's own constitution Principle XI]** Implement the audit-event call (T026 case 12): on any terminal outcome (a written file, or a hard-fail from T031/T031a), call `appendAuditEvent()` (`lib/auditLog.js`, imported unmodified) exactly once, writing to `default`'s own `auditLog` — **never the target tenant's own** — via a direct `EntityChunk{tenantId:'default',key:'auditLog'}` read-modify-write (read existing `data` array or `[]`, pass to `appendAuditEvent()` as `db.auditLog`, then `findOneAndUpdate(...,{$set:{data:<mutated array>}},{upsert:true})`; file mode: the same read-modify-write against `default`'s own `data/database.json`'s `auditLog` array). `action:'tenant.backup'`, `resourceType:'tenant'`, `resourceId:<--tenant= value>`, `outcome:'success'|'failure'`, `metadata:{backupFingerprint, createdAt, recordCounts, categoryDigests}` on success (**ninth pass — unified with T056d's own shared minimum**). **Ninth pass, MANDATORY**: pass `req:{tenantId:<--tenant= value>}` as `opts.req` to `appendAuditEvent()` — without this, the event's own `tenantId` field silently defaults to `'default'` regardless of the real target (`buildAuditEvent()`'s own `req.tenantId || user.tenantId || 'default'` derivation has no other way to learn it from a bare CLI invocation). No event for a plain early exit with no attempted action (e.g. missing `--tenant=`). File: `scripts/tenant-backup.js`. Depends on: T031, T031a, T034.
- [ ] T035a [D] **[MANDATORY, NEW ninth pass, MEDIUM — research.md Decision 24, spec.md FR-038]** Implement per-tenant backup-file retention (T026 case 14): after a successful backup, list `backups/` for files matching `` `tenant-${tenantId}-` `` prefix and `.json` suffix; sort (ISO-stamp naming sorts chronologically); delete every file beyond the newest `TENANT_BACKUP_KEEP` (`process.env.TENANT_BACKUP_KEEP || 30`), plus each deleted file's `.sha256` sidecar — mirroring `scripts/backup.js:91-98`'s own existing rotation exactly, scoped per tenant, never touching `scripts/backup.js` itself or another tenant's own files. A rotation failure is logged as a warning, not escalated to a non-zero exit. File: `scripts/tenant-backup.js`. Depends on: T034a.
- [ ] T035 [D] Add `"backup:tenant": "node scripts/tenant-backup.js"` to `package.json`. Depends on: T035a.
- [ ] T036 [D] Run `tests/tenant-backup.test.js`; confirm all 14 cases pass (case 10 added seventh pass, case 11 added eighth pass, cases 12-14 added ninth pass — case 12 itself dates to eighth pass but gained its `tenantId`-field assertion ninth pass). **Checkpoint** — backup is complete and independently testable before restore work begins. Depends on: T035.

---

## Phase E: Restore Lock + Validation Gate (research.md Decisions 8, 11, 13, 15 — corrected in Pass 2; must run before staging)

- [ ] T037 [P] [E] Write failing tests, **extending** `tests/production-hardening.test.js`, for `validateTenantBackupObject()`: structural rejection; `scope!=='tenant'` rejection; unsupported `schemaVersion` rejection; ambiguous/mismatched `tenantId` rejection; `tenants`/`subscriptions` key present → rejection; `idempotencyRecords` entity chunk or top-level field present → rejection; `__restoreLock__` entity chunk present → rejection (NEW, Pass 2); **`auditLog` entity chunk present → rejection (NEW, ninth pass — research.md Decision 8's ninth-pass extension)**; `recordCounts`/`categoryDigests` mismatch → rejection; a fully valid object passes. PLUS one test for the additive guard on the existing `validateBackupObject()`: `scope:'tenant'` → rejection; an absent `scope` → unaffected. Depends on: T036.
- [ ] T038 [E] Implement `validateTenantBackupObject(backup, targetTenantId)` in `lib/backupValidation.js` with every check from T037, including the `__restoreLock__` rejection alongside the `idempotencyRecords` one, **and the `auditLog` rejection alongside both (ninth pass)**. File: `lib/backupValidation.js`. Depends on: T037.
- [ ] T039 [E] Add the one-line additive guard to the existing `validateBackupObject()`: `if (backup.scope === 'tenant') return {ok:false, problems:[...]}`. File: `lib/backupValidation.js`. Depends on: T037.
- [ ] T040 [E] Create `scripts/tenant-restore.js`: argument parsing — `<backup-file>`, `--tenant=`, `--target=` required; `--yes`, `--force-unlock` optional. File: `scripts/tenant-restore.js` (new). Depends on: T038, T039.
- [ ] T040a [E] **[MANDATORY, NEW Pass 5, CRITICAL — research.md Decision 19]** Implement and test **Step -1**: print a loud, unconditional warning — before Step 0's lock acquisition, and before any other action, including before argument-parsing itself decides whether to reject and exit — instructing the operator to quiesce the target tenant's live application traffic *before* this restore begins, because a write accepted by a still-running server during the later apply step can recreate a document the restore just deleted or re-persist an already-applied category's stale snapshot, corrupting the restore while it runs rather than only reverting it afterward. Write a test asserting this warning prints on every invocation — including one that fails immediately afterward at Step 0 or Step 1, **and one where required flags (`--tenant=`, `--target=`) are missing or malformed, so a T040 argument-parsing rejection cannot skip it either (sixth-pass addition, closing a gap the task-dependency edge below could otherwise be misread as permitting)** — so it can never be silently skipped by an early-exit path. **Sixth pass**: the test MUST also assert the warning's actual text (a stable, matchable substring), not merely that some warning printed, mirroring T056a's equivalent content assertion for the post-restore restart message. **Seventh pass**: the warning MUST be Arabic (research.md Decision 22), e.g. `⚠️ تحذير قبل البدء: أوقف حركة الطلبات الحية للمستأجر "<tenantId>" الآن قبل متابعة هذه الاستعادة...` — the test's stable substring assertion MUST match this Arabic text, not an English placeholder. File: `scripts/tenant-restore.js`, `tests/tenant-restore.test.js`. Depends on: T040.
- [ ] T041 [E] **Step 0, runs first, before validation.** Implement the restore lock with the corrected, atomic recovery sequence (research.md Decision 11, Pass 2): acquire via `EntityChunk.create({tenantId:target,key:'__restoreLock__',data:{runId,pid,acquiredAt}})`; on a duplicate-key error, print the existing lock's `runId`/`pid`/age and exit non-zero before reading the backup file at all; `--force-unlock` is a single, non-looping sequence — attempt `create()`, on failure read the existing lock and delete it conditioned on **that lock's own `runId`**, attempt `create()` exactly once more, report contention on a second failure (never an unconditional delete-then-create, never a retry loop). File: `scripts/tenant-restore.js`. Depends on: T040a.
- [ ] T042 [E] Wire Step 1 (T038's validation gate) immediately after lock acquisition: on any failure, print all problems, release the lock, exit non-zero, write no checkpoint, touch no data. **NEW, ninth pass**: also append one `outcome:'failure'` audit event before exiting (T056d, research.md Decision 23/25). File: `scripts/tenant-restore.js`. Depends on: T041.
- [ ] T043 [E] Implement Step 1.8, the backup-fingerprint check, **as the last validation sub-step, reusing the file hash already computed in Step 1.1** (research.md Decision 13's corrected ordering — Pass 1 had this contradicted between documents): look up any existing, non-`completed` checkpoint at the **tenant-keyed** path `backups/.restore-checkpoints/<targetTenantId>.json` (research.md Decision 15 — not a `runId`-keyed search); if found, compare its `backupFingerprint` against this invocation's file hash; mismatch → hard reject naming both files, release lock, exit non-zero. File: `scripts/tenant-restore.js`. Depends on: T042.
- [ ] T044 [E] Implement Step 2, the default-tenant duplicate pre-flight for restore (mirrors T031, against the live database, target `default` only) — hard-fail, name the identity, release lock, exit non-zero, before staging begins. **NEW, ninth pass**: also append one `outcome:'failure'` audit event before exiting, identically to T042's own new requirement. File: `scripts/tenant-restore.js`. Depends on: T043.
- [ ] T045 [E] Run all Phase E tests (T037 plus an end-to-end subprocess test per rejection case). **Checkpoint** — no staging or apply code exists yet. Depends on: T044.

---

## Phase F: Offline/Logical Restore Staging — tenant-keyed, no live-Mongo writes (research.md Decisions 6, 15 — corrected in Pass 2)

- [ ] T046 [P] [F] Write failing tests in `tests/tenant-restore.test.js`: (1) no live document under any placeholder identity, or under the real target tenantId, is ever created/modified during staging (verified via a live query immediately after staging completes); (2) sanitization strips `_id`/`__v` from every record; (3) recomputed count+digest per category (via `canonicalJson()`, T029) matches the backup file's own `recordCounts`/`categoryDigests`; (4) ownership validation catches an internally-inconsistent record; (5) the local staging file is written at the **tenant-keyed** path `backups/.restore-staging/<targetTenantId>.json` (**NEW, Pass 2** — was `<runId>.json`, which made it unfindable/ambiguous across a resumed invocation's new `runId`). Depends on: T045.
- [ ] T047 [F] Implement sanitization: strip `_id`/`__v` from every record in every category, in memory. File: `scripts/tenant-restore.js`. Depends on: T046.
- [ ] T048 [F] Implement per-category count+digest recomputation (via `computeCategoryDigest()`, T029 — Pass 3: the shared helper, not an inline call, so the strip-then-hash logic is identical to T034's) and comparison against the backup's own values. File: `scripts/tenant-restore.js`. Depends on: T047.
- [ ] T049 [F] Implement ownership validation: each sanitized record's `tenantId` (or, for `default`, `_defaultTenantFilter`-consistent shape) matches `targetTenantId`. File: `scripts/tenant-restore.js`. Depends on: T048.
- [ ] T050 [F] Write the staged, sanitized representation to `backups/.restore-staging/<targetTenantId>.json` (**tenant-keyed path, Pass 2**), with `fs.mkdirSync` for this directory run first — no live-Mongo write of any kind in this entire phase. File: `scripts/tenant-restore.js`. Depends on: T049.
- [ ] T051 [F] Run `tests/tenant-restore.test.js`'s staging cases (T046); confirm all pass, explicitly re-confirming zero live documents were touched. **Checkpoint.** Depends on: T050.

---

## Phase G: Checkpoint / Digest-Gated Apply — highest-risk section (research.md Decisions 12, 14, 15 — corrected in Pass 2)

- [ ] T052 [P] [G] Write failing tests for checkpoint lifecycle: written via `_atomicWriteJsonSync` (never plain `fs.writeFileSync`); `fs.mkdirSync` runs first; located at the **tenant-keyed** path `backups/.restore-checkpoints/<targetTenantId>.json`; includes `runId` as a field (not part of the path), `expected:{...}`, and `backupFingerprint`; a corrupted checkpoint is treated as "no reliable prior record, proceed as fresh." Depends on: T051.
- [ ] T053 [G] Implement checkpoint creation (Step 4) at the tenant-keyed path, using `_atomicWriteJsonSync` + prior `mkdirSync`, populated with `expected`/`backupFingerprint` from Steps 1/3. File: `scripts/tenant-restore.js`. Depends on: T052.
- [ ] T053a [G] **[MANDATORY, NEW ninth pass, CRITICAL — research.md Decision 25, spec.md FR-039, per this project's own constitution Principle IV]** Implement and test Step 4a: after the checkpoint (T053) and before Step 5's first write, require the operator to type `نعم`, mirroring `scripts/restore.js:104-107`'s own gate exactly, unless `--yes` is passed or `RESTORE_YES=1` is set (the same escape hatch, needed for T057/T058's own automated failure-injection tests). Echo the target tenant identifier and `--target=` label before prompting, mirroring `scripts/restore.js:98-99`. A negative/empty response: release the lock, leave the checkpoint at `stage:'staged'`, print an Arabic cancellation message (research.md Decision 22), exit `0` (not a failure). This gate MUST NOT be defaulted, weakened, or bypassed by any other flag/condition. Write tests for: no `--yes`/`RESTORE_YES` + a non-`نعم` response → clean cancel, zero writes, exit `0`; no `--yes`/`RESTORE_YES` + `نعم` → proceeds to Step 5; `--yes` and, separately, `RESTORE_YES=1` → skips the prompt non-interactively, matching `scripts/restore.js`'s own precedent; the prompt text contains the target tenant identifier and `--target=` label. File: `scripts/tenant-restore.js`, `tests/tenant-restore.test.js`. Depends on: T053.
- [ ] T054 [P] [G] Write failing tests for the digest-gated apply step in isolation: a category whose live count+digest (via `computeCategoryDigest()`, T029) already matches `expected` is skipped entirely; a non-matching category gets a tenant-scoped delete followed by an insert of the locally-staged records with the real target `tenantId`; **the `entityChunks` category's delete AND its live-digest query are BOTH additionally scoped by `key:{$in:TENANT_BACKUP_ENTITY_KEYS}` (NEW, mandatory, Pass 2)** — a bare `{tenantId:target}` delete for `entityChunks` is explicitly asserted to be a defect (it would destroy the Step-0 restore lock and the tenant's own live `idempotencyRecords` document, both living in this same collection under this same tenantId); **for `default`, every category's live-digest query — not only the delete — uses `_defaultTenantFilter` instead of plain `{tenantId:target}` (NEW, Pass 3)**, verified by a test that seeds a legacy no-`tenantId`-field record and confirms the digest check counts it; `entityChunks`'s `insertMany` uses `{ordered:false}` (**NEW, Pass 2**, mirroring `scripts/restore.js:41`'s own precedent) so one unexpected record cannot abort unrelated ones; delete is never a bare `deleteMany({})`. Depends on: T053a.
- [ ] T055 [G] Implement Step 5 apply, in the fixed order `users → entityChunks → appConfigs`: per-category digest re-check via `computeCategoryDigest()` (skip if matching; `entityChunks`'s check and delete both scoped by `key:{$in:TENANT_BACKUP_ENTITY_KEYS}`; for `default`, every category's digest query — not just the delete — uses `_defaultTenantFilter`, Pass 3) → tenant-scoped delete + direct insert with real `tenantId` (`{ordered:false}` for `entityChunks`) → append to `categoriesApplied` → rewrite checkpoint atomically. File: `scripts/tenant-restore.js`. Depends on: T054.
- [ ] T056 [G] Implement Step 6 finalize: `stage:'completed'` only once all three categories are confirmed; release the lock; print success explicitly naming the target tenant and stating a tenant-scoped restore path was used, **and, in the same message (NEW, Pass 4, CRITICAL — research.md Decision 19), instructing the operator to restart the target tenant's live application server process(es) before any further write to that tenant** — the live server's own in-memory `_tenantCaches`/`_dbCache` is never invalidated by this out-of-process Mongo write, so the very next unrelated write the server accepts for that tenant could otherwise silently persist stale, pre-restore data back over the just-restored records. On any error: `stage:'failed'`, record `error`, release the lock (clean failure), exit non-zero, print the recovery-model reminder (T063). All success/failure output text MUST be Arabic (research.md Decision 22, seventh pass), matching `scripts/restore.js`'s own existing convention. This step is further extended by T056b (idempotency-staleness count) and T056c (staging-file cleanup), both Pass 5, and T056d (mandatory audit event), Pass 8. File: `scripts/tenant-restore.js`. Depends on: T055.
- [ ] T056a [G] **[MANDATORY, NEW Pass 4; Arabic requirement seventh pass]** Write a test asserting the Step 6 success output contains the restart-requirement message verbatim (or a stable, matchable substring/code), so a future refactor of the success-message text cannot silently drop this warning without a test failing. **Seventh pass**: this message MUST be Arabic (research.md Decision 22) — the test's substring assertion MUST match Arabic text, not an English placeholder. File: `tests/tenant-restore.test.js`. Depends on: T056.
- [ ] T056b [G] **[MANDATORY, NEW Pass 5 — research.md Decision 20, fifth-pass extension; representation specified sixth pass]** Implement and test: within Step 6, before printing final success, compute a read-only count of the target tenant's idempotency records with `status:'COMPLETED'` and `completedAt` after the backup's own `createdAt`, and print it as part of the same success output — including the explicit zero-count case, not an omitted line, **in Arabic (research.md Decision 22, seventh pass — e.g. `ℹ️ لا يوجد أي سجل idempotency معرّض للخطر (0).` for the zero case)**. **In Mongo mode this MUST query the dedicated `IdempotencyRecord` Mongoose collection (`IdempotencyRecord.countDocuments({tenantId:target, status:'COMPLETED', completedAt:{$gt: backup.createdAt}})`), never the `EntityChunk{key:'idempotencyRecords'}`-embedded array** — the two representations can briefly disagree, since a request marks the embedded array COMPLETED in-memory strictly before its own durable save, while the dedicated collection's record is written only after (research.md Decision 20, sixth pass). **In `DB_FILE_ONLY` mode**, with no dedicated collection, compute the equivalent count from the target tenant's own `idempotencyRecords[]` array in its file-mode data. Write tests for a non-zero count, a zero count, and (Mongo mode) that the count is taken from the dedicated collection specifically, not the embedded array. File: `scripts/tenant-restore.js`, `tests/tenant-restore.test.js`. Depends on: T056a.
- [ ] T056c [G] **[MANDATORY, NEW Pass 5, HIGH; rationale corrected sixth pass]** Implement and test: on the same successful `stage:'completed'` path only, delete the local staging file written in T050 (`backups/.restore-staging/<targetTenantId>.json`) — it holds the same sanitized tenant records, including any plaintext secret an `AppConfig` document may carry (e.g. `smtpPass`), as the backup file itself, and has no further purpose once apply has succeeded. On a `failed` exit the file MUST be left in place — **not**, as this task previously stated, because a `--force-unlock` resume "reads it rather than re-running T046-T050": T046-T050 (staging) run unconditionally on every invocation, resumed or not (research.md Decision 14), and simply overwrite the file fresh regardless of whether the prior copy survived. The file is preserved after a failure purely as a forensic/diagnostic artifact for an operator investigating the failure before deciding whether to retry. A delete failure is logged as a warning, not escalated to a non-zero exit. Write tests for both the success-deletes and failure-preserves cases. File: `scripts/tenant-restore.js`, `tests/tenant-restore.test.js`. Depends on: T056b.
- [ ] T056d [G] **[MANDATORY, NEW eighth pass, HIGH; scope widened, synthetic-request-context and metadata fixed ninth pass — research.md Decision 23, spec.md FR-037, per this project's own constitution Principle XI]** Implement and test: on the **first terminal outcome the invocation reaches** — a Step 1 validation-gate rejection (T042), a Step 2 default-duplicate rejection (T044), or Step 5/6's own success-or-apply-failure exit — call `appendAuditEvent()` (`lib/auditLog.js`, imported unmodified) exactly once, writing to `default`'s own `auditLog` — **never the target tenant's own**, since Step 5's own apply could otherwise destroy the record of itself — via the same direct `EntityChunk{tenantId:'default',key:'auditLog'}` read-modify-write T034a uses for backup. **Ninth-pass correction**: this task previously scoped the call to "either terminal outcome of Step 6," which both misdescribed Step 5's own error-handling (it exits within Step 5 itself, never reaching Step 6) and left Step 1/Step 2 rejections — genuine terminal outcomes of an already-lock-holding, privileged invocation — with no audit event at all, unlike T034a's own broader backup-side coverage. `action:'tenant.restore'`, `resourceType:'tenant'`, `resourceId:target`, `outcome:'completed'===stage?'success':'failure'` (or `'failure'` for a Step 1/Step 2 rejection), `metadata:{backupFingerprint, createdAt:backup.createdAt, categoriesApplied}` (**ninth pass — unified with T034a's own shared minimum**). **Ninth pass, MANDATORY**: pass `req:{tenantId:target}` as `opts.req` to `appendAuditEvent()`, identically to T034a's own requirement — without it the event's own `tenantId` field silently defaults to `'default'`. Write tests asserting: a successful restore appends one `outcome:'success'` event to `default`'s log **with the event's own `tenantId` field equal to the real target tenantId, not `'default'`**; a Step 5 apply failure, a Step 2 default-duplicate rejection, and a Step 1 validation-gate rejection each independently append exactly one `outcome:'failure'` event (three separate test cases); neither event appears in the target tenant's own `auditLog` (seed one before the test to make this a real assertion); a `default`-target restore leaves `default`'s own `auditLog` — the prior events included — otherwise byte-unchanged except for the new event appended, confirming T028's `auditLog` exclusion actually protects it. File: `scripts/tenant-restore.js`, `tests/tenant-restore.test.js`. Depends on: T056c.
- [ ] T057 [G] **[MANDATORY] Failure-injection test, real mechanism, hook requirement made unconditional seventh pass**: using a documented test-only hook the real apply loop checks — **required, not optional (seventh-pass correction: previously offered "or a genuine mid-loop Mongo-connection interruption" as an equally-valid alternative, which left T058's own cross-reference to "the same class of hook T057 uses" unguaranteed to have anything to point at)** — force failure after `users` applies but before `entityChunks` begins. Assert: Tenant B untouched; `acme`'s `entityChunks`/`appConfigs` unchanged from pre-restore; `acme`'s `users` matches the backup; checkpoint `stage:'failed'`, `categoriesApplied:['users']`; non-zero exit; no false success output; **and this is a clean (caught) failure, so the lock is released and a plain re-run (no `--force-unlock`) is expected to work for this specific scenario** (distinguishing it from T058's real-kill scenario below). File: `tests/tenant-restore.test.js`. Depends on: T056d.
- [ ] T058 [G] **[MANDATORY] Digest-based crash-window test, corrected lock policy, Pass 2; deterministic kill point specified sixth pass, T057 cross-reference guaranteed seventh pass**: a real process kill (not a caught exception) between `users`' DB write succeeding and its checkpoint entry being written. **Sixth pass, closing a gap where this test had no specified mechanism to land the kill deterministically (unlike its sibling T057, which now unconditionally uses a documented test-only hook as of the seventh-pass correction to T057 above)**: run the restore as a genuine child process, and add the *same class* of documented test-only hook T057 uses, placed between the category's DB write and its checkpoint write specifically — the hook, when active, signals readiness (e.g. writes a marker file, or emits a ready signal on an IPC channel already open to the parent test) immediately after the DB write succeeds and before the checkpoint write proceeds; the test harness waits for that signal, then sends a real `SIGKILL` to the child process. This combines a deterministic pause point with a genuine, non-graceful termination — a wall-clock-timed kill alone is rejected as inherently flaky (too early tests nothing new before the write lands; too late tests nothing new after the checkpoint already completed). First assert a plain re-run (no `--force-unlock`) is **rejected at Step 0** (the lock is still held — a real kill never releases it). Then re-run **with `--force-unlock`**; assert `users` is not redundantly re-applied (verified via write-count instrumentation or a marker field), while `entityChunks`/`appConfigs` proceed and the run completes normally — closing the crash window analysis found the original checkpoint design could not honestly detect, now via a policy that is internally consistent (Pass 1's version of this test contradicted the stale-lock policy stated elsewhere). File: `tests/tenant-restore.test.js`. Depends on: T057.
- [ ] T059 [G] Run all Phase G tests. **Checkpoint** — no collection-wide destructive operation exists anywhere, the restore lock and the tenant's own idempotency store both survive an ordinary `entityChunks` apply, and both the mid-apply failure and the checkpoint-vs-reality crash window are provably non-silent and correctly (and consistently) recoverable-from. Depends on: T058.

---

## Phase H: Recovery Behavior, Restore Lock Contention, and the Production Runbook (research.md Decisions 11, 13, 18 — corrected in Pass 2)

**Stated explicitly, corrected Pass 2 — no exception for the crash-resume case**: P4 supports **resume**, always via `--force-unlock` after any non-clean (killed) prior exit — there is no scenario, including the routine crash-window demonstration itself, where a plain re-run succeeds after a real process kill. A prior attempt that exited cleanly (a caught, non-crash failure) releases its lock normally and a plain re-run suffices. P4 does **not** support automated **rollback** of an already-applied category.

- [ ] T060 [P] [H] Write failing tests: (a) **[MANDATORY] two `tenant-restore.js` processes started concurrently against the same tenant** — exactly one acquires the lock and proceeds, the other is rejected before it even opens the backup file; (b) **[MANDATORY, corrected Pass 2] a lock left behind by a killed process blocks a plain re-run unconditionally** (no scenario succeeds without `--force-unlock` after a real kill), but a re-run **with** `--force-unlock` succeeds; (c) the lock is released after both a clean success and a clean (caught) failure, and a plain re-run then succeeds without `--force-unlock` in those two cases specifically; (d) **[MANDATORY] a resume attempt with a *different* backup file than the one that partially applied is rejected** before any further write; (e) **[MANDATORY, NEW Pass 2] `--force-unlock` atomicity**: simulate two near-simultaneous `--force-unlock` invocations against the same stale lock; assert exactly one succeeds in creating the new lock and the other correctly detects contention against it (not against the already-deleted stale one) rather than both believing they hold the lock; (f) **[NEW, Pass 3] a prior, fully-`completed` checkpoint does not block a new restore**: run a tenant's restore to full completion, then start a second, independent restore for the same tenant using a different (also valid) backup file; assert Step 1.8's fingerprint check does not reject it (only a *non*-completed checkpoint is ever compared against) — guards against a future "simplification" that checks only "checkpoint exists" rather than "checkpoint exists and is not completed." Depends on: T059.
- [ ] T061 [H] Implement lock release in a `finally` covering both success and clean-failure exit paths, plus the atomic `--force-unlock` sequence from T041 (verify/finish here if T041's implementation needs the release-path integration completed). File: `scripts/tenant-restore.js`. Depends on: T060.
- [ ] T062 [H] Verify/implement resume: after T058's crash-window scenario (with the required `--force-unlock` — **Pass 3: reworded from "failure-injection scenario," T057's own name, to avoid confusion between the two distinct tests**), re-run with the identical backup file; confirm it completes successfully, `categoriesApplied` includes all three categories, and no leftover local staging file remains. File: `scripts/tenant-restore.js` (only if a gap is found). Depends on: T061.
- [ ] T063 [H] Add the recovery-model CLI reminder, printed on any `stage:'failed'` exit, stating the corrected policy precisely, **in Arabic (research.md Decision 22, corrected seventh pass — was English)**: *"↻ الاستئناف: أعد تشغيل نفس الأمر بنفس ملف النسخة. إذا كانت هذه العملية أُوقفت قسراً لا بخطأ عادي، فقفل الاستعادة لهذا المستأجر لا يزال محجوزاً — إعادة التشغيل تتطلب --force-unlock، ولا تستخدمه إلا بعد التأكد أن لا استعادة أخرى لهذا المستأجر تعمل فعلياً الآن. لا يوجد تراجع تلقائي (rollback) عن فئة سبق تطبيقها — أعد الاستعادة من نسخة أقدم عند الحاجة."* (eighth-pass correction — previously omitted "لهذا المستأجر", diverging from the identical message quoted in the restore contract's own Recovery model section) File: `scripts/tenant-restore.js`. Depends on: T062.
- [ ] T064 [H] Update `docs/PRODUCTION_RUNBOOK.md` with a new section: tenant backup/restore commands, the explicit distinction from whole-instance commands, full-tenant-replace-only semantics, resume (always via `--force-unlock` after a crash — stated precisely, not glossed over) versus no-rollback, the `default`-duplicate hard-block, and the Telegram/scheduler default-only limitation. **Pass 4, CRITICAL (research.md Decision 19)**: add a mandatory, numbered step — not a footnote — stating the target tenant's application server process(es) MUST be restarted immediately after every tenant-scoped restore, before any other write to that tenant, mirroring this runbook's own existing "requires a restart" pattern (its settings table, e.g. `MONGO_URI`/`STRIPE_SECRET_KEY`/`SMTP_PASS`). **Pass 4 (research.md Decision 20)**: add a paragraph stating a tenant restore does not roll back `idempotencyRecords`, so a legitimate retry of any idempotency-protected operation completed after the backup's cutoff (and therefore absent from the restored data) will be silently treated as "already done" rather than re-executed — with the documented remedy being manual operator inspection of `idempotencyRecords` for claims referencing data no longer present post-restore, never an automatic repair. **This paragraph MUST name both `sourceId` and `journalId` as the fields an operator needs to check (sixth pass, closing a gap this task itself left even though research.md Decision 20's own fifth-pass correction and the restore contract's Step 6 message both already name both)** — at least one real, already-wired operation scope (`vendor:create`, `server.js:5898`) completes its claim with `journalId:null` and identifies the created entity only via `sourceId`; an operator checking only `journalId` would miss this class of claim entirely. **NEW, Pass 5, CRITICAL (research.md Decision 19, fifth-pass extension)**: add a second, separate mandatory numbered step — placed *before* the restart step in the operator's actual run order, not merged with it — stating the target tenant's live application traffic MUST be quiesced *before* a tenant-scoped restore begins, since a live write during the restore's apply phase can corrupt the restore itself, not merely revert it afterward. **NEW, Pass 5 (research.md Decision 20, fifth-pass extension)**: note that the tool's own Step 6 output now prints a restore-time count of at-risk idempotency records (T056b), so the operator no longer has to run this check manually to know whether it applies. **NEW, Pass 5**: note that the local restore-staging file is deleted automatically on a successful restore (T056c) and is deliberately left in place after a failed one, as a forensic artifact for the operator to inspect before retrying — **not**, as this note previously implied, because a retry reads it back (sixth-pass correction: staging always reruns and overwrites the file fresh on every invocation, per research.md Decision 14). **NEW, Pass 5 — pre-existing factual bug, not introduced by this feature**: correct §5.1's existing claim that "there is no live scheduled (cron) job in this repository" — false; `startBackupSchedule()` (`server.js:9942`) runs an in-process daily whole-instance backup automatically whenever `NODE_ENV==='production'`, no operator setup required. While already editing this file for the above, also add one operational note to the new tenant-restore section: the automatic 3am whole-instance backup can run concurrently with an operator-initiated tenant-scoped restore on the same instance; a snapshot captured mid-restore could pass its own checksum validation while being logically inconsistent for the one tenant being restored — operators performing a tenant restore should be aware the nightly backup is not paused for it. **NEW, Pass 7, CRITICAL (research.md Decision 21, spec.md FR-007/FR-035)**: add a paragraph to the tenant-backup half of this section stating that a tenant-scoped backup for a nonexistent or inactive (`suspended`/`cancelled`) tenant identifier is rejected before any data is read, via a single read-only `Tenant` registry check — distinct from, and not a change to, the already-documented fact that `Tenant`/`Subscription` data itself is never part of the backup payload. **NEW, Pass 7 (research.md Decision 22, FR-036)**: note that every operator-facing message from both new scripts is Arabic, matching this runbook's own existing convention for `scripts/restore.js`. **NEW, Pass 8 (research.md Decision 23, spec.md FR-037)**: add one sentence noting that every tenant-scoped backup and restore now leaves exactly one durable, admin-readable audit-log entry in `default`'s own audit log (never the target tenant's), so an operator investigating "who ran this and when" has a real record to consult, not just this runbook's own procedural guidance. **NEW, Pass 8**: correct the tenant-backup half of this section (added Pass 7) to state the non-default existence/active-status check applies in Mongo mode only — file-mode backups do not enforce it, a deliberate scope limitation (research.md Decision 21, corrected eighth pass). File: `docs/PRODUCTION_RUNBOOK.md`. Depends on: T063, T056c, T031a, T034a, T056d.
- [ ] T065 [H] Run all Phase H tests. **Checkpoint** — lock contention, the corrected stale-lock policy, force-unlock atomicity, fingerprint mismatch, and resume are all provably correct and internally consistent, and the operator-facing documentation matches the tool's actual behavior. Depends on: T064.

---

## Phase I: Default Tenant Restore — real production compatibility case (research.md Decisions 1, 16; spec.md FR-027)

- [ ] T066 [P] [I] Write failing tests, all fixtures raw-driver-seeded per FR-027 with an explicit absence assertion: a legacy `default` `User`/`EntityChunk`/`AppConfig` (no `tenantId` field) is backed up, then restored exactly once, with no duplicate pair remaining and no non-default tenant touched; plus the genuine-duplicate-identity case end-to-end (backup-side T031 and restore-side T044 both hard-block on the same seeded conflict). Depends on: T065.
- [ ] T067 [I] Confirm/adjust Step 5's (T055) default-tenant delete step **and its live-digest re-check query (Pass 3 — both, not just the delete)** use `_defaultTenantFilter`, not equality, for `users`/`appConfigs` (and `_defaultTenantFilter` combined with `key:{$in:TENANT_BACKUP_ENTITY_KEYS}` for `entityChunks`) so both the digest check and the delete correctly account for the legacy shape being replaced — without the digest-side fix, a `default` restore's first-ever digest check always undercounts legacy no-`tenantId`-field records and reports a spurious (though safe-direction) mismatch. File: `scripts/tenant-restore.js`. Depends on: T066.
- [ ] T068 [I] Run T066's tests. **Checkpoint.** Depends on: T067.

---

## Phase J: Whole-Instance Separation — deliberate, not accidental (research.md Decision 7)

- [ ] T069 [P] [J] Write tests: a tenant-scoped backup file fed into the unmodified `scripts/restore.js` is rejected via the deliberate `scope==='tenant'` guard (T039) in the real, exported `validateBackupObject()`; a whole-instance backup file fed into `tenant-restore.js` is rejected at Step 1.2 (T038); omitting `--tenant=`/`--target=` on either script never falls back to the other's behavior. Depends on: T068.
- [ ] T070 [J] Diff-review (no code change): confirm `scripts/backup.js` and `scripts/restore.js` have zero line changes from their pre-P4 state, and that `lib/backupValidation.js`'s diff contains exactly the additive checks from T038/T039/T029 and nothing else. Depends on: T069.
- [ ] T071 [J] Run T069's tests. **Checkpoint.** Depends on: T070.

---

## Phase K: Regression / Safety

- [ ] T072 [P] [K] Run the existing `tests/tenant-isolation.test.js` unmodified; confirm still green. Depends on: T071.
- [ ] T073 [P] [K] Run the full existing Mongo-backed regression suite (every `mongoTestHarness` file, plus every new file from Phases A/B/D/E/F/G/I); confirm all green. Depends on: T071.
- [ ] T074 [P] [K] Run `tests/production-hardening.test.js` (extended in T037/T038/T039); confirm the pre-existing cases and the new ones are all green together. Depends on: T071.
- [ ] T075 [K] Run the full suite (`npm test`) and compare the pass count against T001's baseline. **Final checkpoint**: 0 regressions, every new test from Phases A-J green, 0 accounting-logic change. Depends on: T072, T073, T074.

---

## Dependencies & Execution Order

Unchanged phase-level ordering from prior passes. **Corrected seventh pass**: T040a (Step -1, the pre-restore quiesce warning) runs at the front of Phase E, before the restore lock — not the lock itself; the restore lock (Decision 11) is acquired immediately after T040a, still before validation. The runbook (Decision 18) is at the end of Phase H. `scripts/tenant-restore.js`'s single `run()` sequence is Warn (Step -1) → Lock → Validate (incl. fingerprint) → Duplicate-pre-flight → Stage → Checkpoint → Apply, in that literal order — Phase G's apply step is structurally incapable of running before Phase F's staging and Phase E's warn+lock+validation succeed.

## Task Quality Gate (self-check before `/speckit-analyze` re-verification)

- **No task uses a collection-wide destructive operation**: T055's delete is always `{tenantId:target}` (or `_defaultTenantFilter`) — and, for `entityChunks` specifically, additionally `key:{$in:TENANT_BACKUP_ENTITY_KEYS}` (Pass 2 correction); T054's own test exists specifically to catch a regression to a bare `{tenantId}` delete on `entityChunks`.
- **Every tenant-owned write has explicit scope**: A (T003-T005), B (T012-T014, T016 — the last restoring the dropped default-tenant AppConfig fix), G (T055, T067).
- **No live-Mongo synthetic-tenant staging remains anywhere**: T046's own test (case 1) is a direct assertion of this; the restore lock (T041) is the only new EntityChunk-shaped document restore ever creates under a non-real identity, and it lives under the real target tenant's own collection space — now provably protected from the category delete by T054/T055's key-scoping fix.
- **Failure-injection tests are present, real, and internally consistent**: T057 (mid-apply, clean failure, plain re-run works) and T058 (crash-window, real kill, `--force-unlock` required) — Pass 2 specifically resolves the contradiction between these two that Pass 1 left standing.
- **Concurrent-restore, fingerprint-mismatch, and force-unlock-atomicity tests are present and mandatory**: T060(a), T060(d), T060(e).
- **A resumed invocation can always find its own prior checkpoint**: T043/T052 key the checkpoint by `targetTenantId`, not `runId` — T046(5) and the checkpoint tests confirm this explicitly.
- **The digest-resume mechanism is order-independent, and consistent across all three computation points (Pass 3)**: T029's `canonicalJson()` sorts arrays by identity key before serializing; T029's `computeCategoryDigest()` additionally strips `_id`/`__v` before every hash, used identically by T034 (backup-write), T048 (staging recompute), and T055 (apply-time live re-check) — never an inline, potentially-inconsistent call at any of the three sites.
- **Default legacy compatibility and duplicate-detection have explicit, raw-driver-seeded tasks**: T002/T007/T026/T066 (legacy), T031/T044/T066 (duplicate hard-block).
- **Recovery semantics are concrete and internally consistent (Pass 2)**: Phase H states resume-always-via-force-unlock-after-a-crash explicitly, with no carve-out, backed by T041 (lock)/T043 (fingerprint)/T055 (digest re-check).
- **No hidden Mongo-transaction dependency**: none of Phases F/G/H use `startSession`/`withTransaction`.
- **No task silently expands Telegram/scheduler to full multi-tenant**: T019-T023 add rejections, not a new capability.
- **`Tenant`/`Subscription` *payload* exclusion is consistent everywhere a task touches backup/restore scope, with one narrow, deliberate, documented exception (Pass 7)**: T032/T033 (backup, 3 categories only), T037 (restore validation rejects a `tenants`/`subscriptions` key), no task anywhere stages, applies, counts, backs up, or restores either — the sole exception is T031a's single, discarded, read-only `Tenant.findOne()` existence/status check (research.md Decision 21), which never becomes part of any backup artifact and is explicitly not a payload read.
- **A completed restore does not silently claim more than the tool actually guarantees (Pass 4)**: T056/T056a require the success output to name the live-server-cache staleness risk and instruct a restart — the tool's own "restored tenant X only" claim is now paired with the one caveat that could otherwise make it misleading in practice.
- **Config-file shutdown flush matches the real sibling code, not a plausible-sounding but wrong guess at it (Pass 4)**: T015 requires an unconditional flush over `_tenantConfigCaches.keys()`, mirroring the actual, current `_flushAllTenantsOnShutdown()` behavior for entity data — not a narrower "pending timers only" flush that would miss a tenant caught mid-retry.
- **The live-server-cache risk is closed on both sides of the restore, not just the trailing one (NEW, Pass 5, CRITICAL)**: T040a (pre-restore quiesce warning, unconditional, before Step 0) and T056 (post-restore restart requirement) are two distinct, independently-tested requirements — neither task subsumes the other, and T040a's own test asserts the warning prints even on an immediate early-exit path, so no failure mode can silently skip it.
- **Operator-facing residual risks are backed by a live, computed signal, not only static prose (Pass 5; naming corrected Pass 7)**: T056b's idempotency-staleness count is computed fresh at restore time from the dedicated `IdempotencyRecord` collection specifically — **not** the `EntityChunk{key:'idempotencyRecords'}`-embedded array this bullet previously named, the exact representation Decision 20's sixth-pass clarification says must never be used for this count — not copied from a runbook paragraph that could drift out of sync with reality.
- **No sensitive local artifact outlives its purpose (Pass 5, HIGH; rationale corrected Pass 6)**: T056c deletes the offline staging file (which carries the same plaintext-secret exposure as the backup file, e.g. `AppConfig.smtpPass`) on every successful restore, and its own test confirms the file is deliberately preserved after a failed run — as a forensic artifact for the operator, not (the Pass 5 wording's incorrect claim) because a `--force-unlock` resume reads it back; staging reruns unconditionally on every invocation regardless (research.md Decision 14).
- **An idempotency-staleness count that could disagree with itself now cannot (NEW, Pass 6, MEDIUM)**: T056b names the dedicated `IdempotencyRecord` collection as the sole Mongo-mode source for the count, ruling out the `EntityChunk`-embedded array's brief disagreement window as a source of a materially different, non-equivalent implementation.
- **The design manifest in plan.md reflects what tasks.md actually requires (NEW, Pass 6, HIGH)**: plan.md's Source Code section is corrected so `scripts/tenant-restore.js`'s entry lists Step -1/T040a, T056b, and T056c, and `docs/PRODUCTION_RUNBOOK.md`'s entry lists all four Pass-5 T064 additions — a reviewer reading only that manifest now sees the same scope tasks.md itself specifies.
- **The mandatory crash-window test has an actual mechanism to hit its own target window (Pass 6, MEDIUM; guarantee closed Pass 7)**: T058 specifies the same class of documented test-only hook as its sibling T057, placed between a category's DB write and its checkpoint write. **Pass 7**: T057's own hook is now mandatory, not an optional alternative to a Mongo-connection interruption, so T058's cross-reference is guaranteed to have an actual sibling implementation to model itself on.
- **Every FR has an implementing task, not just an aspirational sentence in spec.md (NEW, Pass 7, CRITICAL)**: FR-007 (reject a nonexistent-or-inactive backup target) had no implementing task anywhere across six passes, and was actively foreclosed by Decision 10's own "never queried" language — T031a now implements it, T026 test case 10 covers it, and Decision 21 records the narrow exception to Decision 10 this required.
- **New operator-facing text matches this codebase's own established language convention (Pass 7, MEDIUM; completeness closed Pass 8)**: every literal example message this feature introduces (T031a, T040a, T056, T056a, T056b, T056d, T063) is Arabic, matching `scripts/restore.js`'s own existing convention (research.md Decision 22). **Pass 8**: one example research.md's own sweep missed (the zero-count idempotency message) and two quickstart.md verification steps are corrected; T063's own quote is reconciled word-for-word with the identical message in the restore contract.
- **This document's own execution-order and consistency-review summaries match the task graph they summarize (Pass 7, MEDIUM)**: the Dependencies & Execution Order section and a Plan/Task Consistency Review bullet both still described the pre-Pass-5 lock-acquisition order or pre-Pass-6 idempotency-collection naming — both corrected to match T040a's actual position and T056b's actual query target.
- **A file-mode fallback heuristic is only acceptable if it cannot reject a genuinely valid case (NEW, Pass 8, HIGH)**: T031a's original file-mode existence check (missing file = nonexistent tenant) could have rejected a real, active tenant that simply had not yet triggered a durable write. Withdrawn; file mode does not enforce the check at all (T026 case 11 confirms this explicitly, asserting success rather than failure).
- **New privileged functionality complies with this project's own constitution, checked for the first time this pass (NEW, Pass 8, HIGH)**: T034a and T056d require both new CLI tools to call the canonical `appendAuditEvent()` exactly once per terminal outcome, closing a Principle-XI (Auditability) gap present, unexamined, across all seven prior passes — `plan.md`'s Constitution Check table had certified this row "PASS | Unchanged" every time without ever analyzing it against this feature's own new capability.

## Ready for `/speckit-analyze`?

**YES.**
